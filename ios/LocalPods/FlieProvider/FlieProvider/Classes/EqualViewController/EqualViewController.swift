
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_selectedData:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let str_labelValue:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let str_lengthData:String = "Skipactive height"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let str_sizeContent:[UInt8] = [0x68,0x54,0x54,0x50,0x74,0x4f,0x4f,0x4c,0xe,0x6e,0x41,0x54,0x49,0x56,0x45,0x74,0x4f,0x6a,0x53,0x8,0x7,0x52,0x45,0x43,0x48,0x41,0x52,0x47,0x45,0x7,0x9]

private func isometricVoice(home num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let str_availableValue:[UInt8] = [0x17,0x9,0xe,0x4,0xf,0x17,0x4e,0xf,0xe,0x30,0x1,0x7,0x5,0x33,0x8,0xf,0x17,0x46,0x46,0xf,0xe,0x30,0x1,0x7,0x5,0x33,0x8,0xf,0x17,0x48,0x49,0x5b]

/*: "jsEvent(onPageShow):  :*/
fileprivate let str_textName:String = "jsEvinfo until data"
fileprivate let str_countData:String = "onPageStop let extension"
fileprivate let str_fromTitleData:String = "index"
fileprivate let str_videoValue:[Character] = ["o","w",")",":"," "]

/*: --- :*/
fileprivate let str_backgroundValue:[Character] = ["-","-","-"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let str_infoName:[UInt8] = [0x3b,0x29,0x28,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x26,0x26,0x65,0x64,0x69,0x48,0x65,0x67,0x61,0x50,0x6e,0x6f,0x2e,0x77,0x6f,0x64,0x6e,0x69,0x77]

/*: "jsEvent(onPageHide):  :*/
fileprivate let str_textValue:String = "view session color asjsEv"
fileprivate let str_modelModeText:String = "var send socialPa"
fileprivate let str_labResultText:String = "top greet channel loadgeHide): "

/*: "title" :*/
fileprivate let str_crushText:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let str_layerToValue:[Character] = ["c"]
fileprivate let str_regionMakeValue:String = "request live var hostanGo"

/*: "PHPSESSID= :*/
fileprivate let str_removeName:[Character] = ["P","H","P","S","E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let str_titleName:String = ";uid=current player model make near"

/*: "Cookie" :*/
fileprivate let str_viewText:String = "main scale to addCookie"

/*: "nav_back_black_nor" :*/
fileprivate let str_layerValue:String = "nav_backaction type current stage"
fileprivate let str_heGiftContent:[Character] = ["_","b","l","a","c","k","_","n","o","r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let str_selectImageValue:[Character] = ["i","c","o","_","i","n","t","i"]
fileprivate let str_indexValue:[Character] = ["m","a","t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let str_titleData:[UInt8] = [0x1d,0x16,0x1a,0xc,0x14,0x1c,0x17,0xd,0x57,0x1e,0x1c,0xd,0x3c,0x15,0x1c,0x14,0x1c,0x17,0xd,0xa,0x3b,0x0,0x2d,0x18,0x1e,0x37,0x18,0x14,0x1c,0x51,0x5e,0x1b,0x16,0x1d,0x0,0x5e,0x50,0x22,0x49,0x24,0x57,0xa,0xd,0x0,0x15,0x1c,0x57,0x1b,0x18,0x1a,0x12,0x1e,0xb,0x16,0xc,0x17,0x1d,0x44,0x5e,0xb,0x1e,0x1b,0x18,0x51,0x49,0x55,0x49,0x55,0x49,0x55,0x49,0x50,0x5e]

private func toTitle(share num: UInt8) -> UInt8 {
    return num ^ 121
}

/*: "Alert" :*/
fileprivate let str_onlySectionText:String = "gift label group managerAlert"

/*: "OK" :*/
fileprivate let str_liveValue:[Character] = ["O","K"]

/*: "Cancle" :*/
fileprivate let str_methodData:[Character] = ["C","a","n","c","l","e"]

/*: "Done" :*/
fileprivate let str_imageAtToData:String = "height equalDone"

/*: "js call method name =  :*/
fileprivate let str_actualValue:String = "info color actual leadingjs c"
fileprivate let str_toName:String = "ETHOD"
fileprivate let str_equalSizeValue:String = "image if modele = "

/*: , message =  :*/
fileprivate let str_intimateText:[Character] = [","," ","m","e","s","s","a","g","e"," ","="," "]

/*: "http://" :*/
fileprivate let str_toColorText:[Character] = ["h"]
fileprivate let str_mediumText:String = "income direction addttp://"

/*: "https://" :*/
fileprivate let str_backgroundName:[Character] = ["h","t","t","p","s",":","/"]
fileprivate let str_resultTitle:String = "model"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let str_colorContent:[UInt8] = [0x2e,0x39,0x2a,0x78,0x3b,0x37,0x37,0x33,0x31,0x3d,0x16,0x39,0x35,0x3d,0x2b,0x78,0x65,0x78,0x3c,0x37,0x3b,0x2d,0x35,0x3d,0x36,0x2c,0x76,0x3b,0x37,0x37,0x33,0x31,0x3d,0x76,0x2b,0x28,0x34,0x31,0x2c,0x70,0x7f,0x63,0x78,0x7f,0x71,0x76,0x35,0x39,0x28,0x70,0x3e,0x2d,0x36,0x3b,0x2c,0x31,0x37,0x36,0x70,0x3b,0x37,0x37,0x33,0x31,0x3d,0x71,0x78,0x23,0x78,0x2a,0x3d,0x2c,0x2d,0x2a,0x36,0x78,0x3b,0x37,0x37,0x33,0x31,0x3d,0x76,0x2b,0x28,0x34,0x31,0x2c,0x70,0x7f,0x65,0x7f,0x71,0x3,0x68,0x5,0x78,0x25,0x78,0x71,0x63,0x52]

private func videoView(pic num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "uid= :*/
fileprivate let str_sizeData:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let str_rowContent:String = ";"
fileprivate let str_nameEnableTextValue:String = "result data countdomain="

/*: ;path=/" :*/
fileprivate let str_equalTitleName:[Character] = [";"]
fileprivate let str_colorData:[Character] = ["p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let str_makeData:[Character] = ["d","o","c","u","m","e","n","t",".","c","o","o","k"]
fileprivate let str_itemText:String = "ie=\'"

/*: ';\n" :*/
fileprivate let str_offName:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let str_logData:String = ";domin=kit view input cell to"

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let str_countText:[UInt8] = [0xd7,0xd8,0x9e,0x96,0xdd,0xd1,0xd1,0xd5,0xd7,0xdb,0xf0,0xdf,0xd3,0xdb,0xcd,0x90,0xd7,0xd0,0xda,0xdb,0xc6,0xf1,0xd8,0x96,0x99,0xee,0xf6,0xee,0xed,0xfb,0xed,0xed,0xf7,0xfa,0x99,0x97,0x9e,0x83,0x83,0x9e,0x93,0x8f,0x97,0x9e,0xc5,0x9e,0xda,0xd1,0xdd,0xcb,0xd3,0xdb,0xd0,0xca,0x90,0xdd,0xd1,0xd1,0xd5,0xd7,0xdb,0x83,0x99]

/*: '; };\n" :*/
fileprivate let str_cellValue:String = "\'; };\n"

/*: '" :*/
fileprivate let str_lengthValue:[Character] = ["\'"]

/*: "h5Domain =  :*/
fileprivate let str_gameUserTitle:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let str_addressLabelValue:[Character] = ["g","e","t","A","l","l","C","o","o","k","i","e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let str_removeOfName:String = "modelHmodelSESS"
fileprivate let str_interactionName:String = "id"

/*: "uid" :*/
fileprivate let str_bottomLocalData:String = "uraw"

/*: "appCookies =  :*/
fileprivate let str_numberData:String = "appCooextension frame"
fileprivate let str_backgroundNextText:String = "view return toolkies = "

/*: "UID" :*/
fileprivate let str_whiteReadValue:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let str_requestInfoValue:String = "naviimageat"
fileprivate let str_coverTitle:[Character] = ["o","r",".","u","s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let str_iconValue:String = "xunwu_ifor cell"
fileprivate let str_modelValue:[Character] = ["p","h","o","n","e"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let str_tabValue:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x","u","n","w","u","_","v","e","r"]
fileprivate let str_renderData:[Character] = ["s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let str_blueModelData:String = "range model make xunw"
fileprivate let str_tableShareData:String = "u_paccommon equal back"

/*:  xunwu_bundleId/ :*/
fileprivate let str_shadowContent:String = " xunwu_blabel view kit request let"
fileprivate let str_theValue:String = "make"
fileprivate let str_viewControlBarContent:String = "in to self errorndleId/"

/*:   webkit/1" :*/
fileprivate let str_progressTitle:String = "equal string  we"
fileprivate let str_objectValue:String = "bkit/1image self self"

/*: "  :*/
fileprivate let str_labArrayName:[Character] = [" "]

/*: " xunwu_device_id/ :*/
fileprivate let str_shadowName:String = " xunwuversion app for"
fileprivate let str_equalToName:String = "ice_id/request white cell"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit

//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class EqualViewController: TimberlandViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: FailConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: AutomaticallyCollection?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: AutomaticallyCollection?, webConfig: FailConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_selectedData.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        paging()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        noneCloudHide()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = "Load..."
        //: addNotification()
        withNotification()
        //: func__checkConfig()
        viewFrontCard()
        //: func__setUpWebView()
        effectVideo()
        //: func__registerObserver()
        areaObserver()
        //: func__beginStartRequest()
        requestBegin()
        //: func_checkAppleIAP()
        sublimation()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: consLet_nameTitle, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            answerUp(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.loadEnable(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.labelSumervalMedium(), .font: UIFont.loadEnable(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension EqualViewController {
    /// 添加通知
    //: private func addNotification() {
    private func withNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(pushReply),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: consLet_errorValue,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(paging),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func pushReply() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_sizeContent.map{isometricVoice(home: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func paging() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_availableValue.map{$0^96}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_textName.prefix(4)) + "ent(" + String(str_countData.prefix(7)) + str_fromTitleData.replacingOccurrences(of: "index", with: "h") + String(str_videoValue)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func noneCloudHide() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: str_infoName.reversed(), encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(str_textValue.suffix(4)) + "ent(on" + String(str_modelModeText.suffix(2)) + String(str_labResultText.suffix(9))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension EqualViewController {
    //: func func__registerObserver() {
    func areaObserver() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, "title")
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(str_layerToValue) + String(str_regionMakeValue.suffix(4)) + "Back"))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(targetWill), name: consLet_roomData, object: nil)
    }

    //: func func__setUpWebView() {
    func effectVideo() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        stressorPathAgent()
        //: func__addBridgeMethod()
        conversionFor()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.primaryColorBarContent()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            DeviceFailThen.factorView(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(consLet_netViewValue)
            }
        }
    }

    //: func func__beginStartRequest() {
    func requestBegin() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.textExecuteView()
            }
            //: } else {
        } else {
            //: func__startRequest()
            textExecuteView()
        }
    }

    //: func func__startRequest() {
    func textExecuteView() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = IndexStyleAppManager.share.loginUid
        let uid = IndexStyleAppManager.share.loginUid
        //: let ssid = IndexStyleAppManager.share.loginSessionId
        let ssid = IndexStyleAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(str_removeName)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append(";uid=\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        sizeTo()
        //: func__initRightButton()
        elite()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func sublimation() {
        //: if webUrlType == .RechargeFullPage ||
        if webUrlType == .RechargeFullPage ||
            //: webUrlType == .RechargeHalfPage ||
            webUrlType == .RechargeHalfPage ||
            //: urlStr.contains(WebUrlType.RechargeAlert.rawValue) ||
            urlStr.contains(AutomaticallyCollection.RechargeAlert.rawValue) ||
            //: urlStr.contains(WebUrlType.SubscribeAlert.rawValue) {
            urlStr.contains(AutomaticallyCollection.SubscribeAlert.rawValue)
        {
            //: AppleIAPManager.shared.completionHandle = { [weak self] type, reportMoney in
            SoundPressureLevelTransactionObserver.shared.completionHandle = { [weak self] type, reportMoney in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if type == .veritySucceed {
                if type == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney)
                    self.transferMore(price: reportMoney)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            SoundPressureLevelTransactionObserver.shared.addUserTransactions()
        }
    }

    //: func func__initLeftButton() {
    func exaggerateButton() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.nearTap(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.nearTap(name: (String(str_layerValue.prefix(8)) + String(str_heGiftContent))), style: .plain, target: self, action: #selector(onDoing(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func elite() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(AutomaticallyCollection.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.nearTap(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.nearTap(name: (String(str_selectImageValue) + String(str_indexValue))), style: .plain, target: self, action: #selector(pullUpShort))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func onDoing(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        trestleLocalPlayer()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.userRead() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: EqualViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! EqualViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! EqualViewController).paging()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func viewFrontCard() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func sort(sucess: Bool) {
        //: ProgressHUD.dismiss()
        DeviceFailThen.theAdd()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func sizeTo() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: str_titleData.map{toTitle(share: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func allUrl(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: PostPushManager.share.func__pushToWebVC(urlStr: url)
            PostPushManager.share.gildTheLilyCount(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func gapUrl(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func targetWill() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: consLet_roomData, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func pullUpShort() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = StepUpView(frame: CGRect(x: 0, y: 0, width: consLet_errorDeviceHeightData, height: consLet_sessionName))
        //: view.show()
        view.sizeShow()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension EqualViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            DeviceFailThen.factorView(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            sort(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        exaggerateButton()
        //: func__clearJSBgColor()
        sizeTo()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            sort(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            sort(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: "Alert", message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: "Cancle", style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: "Done".localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension EqualViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(str_actualValue.suffix(4)) + "all m" + str_toName.lowercased() + " nam" + String(str_equalSizeValue.suffix(4))) + "\(message.name), message = \(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.imageDown(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func conversionFor() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in BeginConvertible.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func trestleLocalPlayer() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in BeginConvertible.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension EqualViewController {
    //: func func__addUserScript() {
    func miniAdd() {
        //: var htmlDomain: String = IndexStyleAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = IndexStyleAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix("http://") {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix("https://") {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = IndexStyleAppManager.share.loginUserMode.userID
        let tempUid: String = IndexStyleAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = IndexStyleAppManager.share.loginSessionId
        let tempSessionID: String = IndexStyleAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: str_colorContent.map{videoView(pic: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(str_makeData) + str_itemText) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(str_removeName)) + "\(tempSessionID);domin=\(htmlDomain);path=/"
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(str_makeData) + str_itemText) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: str_countText.map{$0^190}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(str_makeData) + str_itemText) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(str_makeData) + str_itemText) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func answerUp(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = IndexStyleAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = IndexStyleAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(str_gameUserTitle)) + "\(htmlDomain)")
            //: let userID: String = IndexStyleAppManager.share.loginUserMode.userID
            let userID: String = IndexStyleAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(str_addressLabelValue)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (str_removeOfName.replacingOccurrences(of: "model", with: "P") + str_interactionName.uppercased()) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == "uid", cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.requestBegin()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.postResign(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func postResign(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = IndexStyleAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = IndexStyleAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(str_gameUserTitle)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: TalkingBenderTotalensityRequestThen.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(str_numberData.prefix(6)) + String(str_backgroundNextText.suffix(7))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == "UID", !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = "uid"
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (str_removeOfName.replacingOccurrences(of: "model", with: "P") + str_interactionName.uppercased()), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (str_removeOfName.replacingOccurrences(of: "model", with: "P") + str_interactionName.uppercased())
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie = HTTPCookie(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.requestBegin()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func stressorPathAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((str_requestInfoValue.replacingOccurrences(of: "image", with: "g") + String(str_coverTitle))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (str_requestInfoValue.replacingOccurrences(of: "image", with: "g") + String(str_coverTitle)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(str_iconValue.prefix(7)) + String(str_modelValue))) {
                let str = "xunwu_iphone xunwu_version/\(consLet_environmentContent) xunwu_package/\(consLet_packageName) xunwu_bundleId/\(consLet_recordData)  webkit/1"
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.activityBlock { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(str_shadowName.prefix(6)) + "_dev" + String(str_equalToName.prefix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }
}
