
//: Declare String Begin

/*: "cmd" :*/
fileprivate let str_colorValue:[UInt8] = [0x75,0x7b,0x72]

private func viewSize(enable num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "cancelCall" :*/
fileprivate let str_countName:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "data" :*/
fileprivate let str_cellData:[UInt8] = [0x6e,0x6b,0x7e,0x6b]

private func viewGift(table num: UInt8) -> UInt8 {
    return num ^ 10
}

/*: "logId" :*/
fileprivate let str_nameTitle:String = "color array var var controllogId"

/*: "#322870" :*/
fileprivate let str_indexData:String = "#3content87"
fileprivate let str_maxCellData:String = "0"

/*: "Tap to start video chatting" :*/
fileprivate let str_labelTitle:String = "Tap second name"
fileprivate let str_cornerValue:String = "rt vilab user color to"
fileprivate let str_popText:[Character] = ["d","e","o"," ","c","h","a"]
fileprivate let str_enableData:String = "ttintitle"

/*: "icon_close_nor" :*/
fileprivate let str_modelValue:[Character] = ["i","c","o","n","_","c","l"]
fileprivate let str_managerLabelData:String = "OS"
fileprivate let str_whiteValue:String = "view model inside gender reasone_nor"

/*: "icon_views_number" :*/
fileprivate let str_sizeData:String = "imn"
fileprivate let str_dataContent:[Character] = ["_","n","u","m","b","e","r"]

/*: "%@ chatting" :*/
fileprivate let str_normalValue:[Character] = ["%","@"," ","c","h","a"]
fileprivate let str_viewText:String = "rangerangeing"

/*: "icon_lounge" :*/
fileprivate let str_feeData:[Character] = ["i","c","o","n","_","l","o","u"]
fileprivate let str_logData:[Character] = ["n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let str_frameValue:[Character] = ["i","c","o","n","_","r","a","n","k","_","c"]
fileprivate let str_genderHomeTitle:String = "oirequest"

/*: "vip_btn_top" :*/
fileprivate let str_infoTitle:String = "equal for model datavip_btn"
fileprivate let str_pathToTitle:String = "_topmake inside image"

/*: "btn_nearby_settings_nor" :*/
fileprivate let str_indexTitle:String = "btn_nview fill var title finish"
fileprivate let str_byCellTitle:String = "string cell let_set"
fileprivate let str_pointFreeContent:[Character] = ["_","n","o","r"]

/*: "%@ Coins/min" :*/
fileprivate let str_hostMenuData:[Character] = ["%","@"," ","C","o","i","n","s","/","m","i","n"]

/*: "get json error" :*/
fileprivate let str_markViewCornerName:String = "let size map trueget j"
fileprivate let str_nameToolData:[Character] = ["o","r"]

/*: "matchId" :*/
fileprivate let str_popName:[Character] = ["m","a","t","c","h"]
fileprivate let str_modeName:[Character] = ["I","d"]

/*: "errno" :*/
fileprivate let str_postTitle:[UInt8] = [0x2c,0x3b,0x3b,0x27,0x26]

private func differentialCost(container num: UInt8) -> UInt8 {
    return num ^ 73
}

/*: "VIPFee" :*/
fileprivate let str_spaceText:[Character] = ["V","I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let str_ofCustomData:String = "business modelMatc"
fileprivate let str_labName:[Character] = ["o","w"]

/*: "userTouch" :*/
fileprivate let str_layerTitle:[Character] = ["u","s","e","r","T","o","u","c"]
fileprivate let str_awakeName:String = "to"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let str_firstTitle:String = "model iconmf/fr"
fileprivate let str_liveTitle:String = "l/getVcell display"
fileprivate let str_clipData:[Character] = ["i","d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let str_insideName:[UInt8] = [0x49,0x5d,0x40,0x42,0x7b,0x56,0x5f,0x4a]

private func errorMoment(at num: UInt8) -> UInt8 {
    return num ^ 47
}

/*: "You've been barred from receiving calls" :*/
fileprivate let str_callData:[UInt8] = [0x70,0x46,0x5c,0xe,0x5f,0x4c,0x9,0x4b,0x4c,0x4c,0x47,0x9,0x4b,0x48,0x5b,0x5b,0x4c,0x4d,0x9,0x4f,0x5b,0x46,0x44,0x9,0x5b,0x4c,0x4a,0x4c,0x40,0x5f,0x40,0x47,0x4e,0x9,0x4a,0x48,0x45,0x45,0x5a]

/*: "source" :*/
fileprivate let str_makeText:[UInt8] = [0xd1,0xcd,0xd3,0xd0,0xc1,0xc3]

fileprivate func inputObject(message num: UInt8) -> UInt8 {
    let value = Int(num) + 162
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let str_timeCheckValue:[UInt8] = [0xda,0xdf,0xd6,0xcb]

fileprivate func anyFrame(data num: UInt8) -> UInt8 {
    let value = Int(num) + 154
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let str_clickData:[UInt8] = [0x45,0x39,0x34]

fileprivate func groupAdd(path num: UInt8) -> UInt8 {
    let value = Int(num) - 208
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fromFreeCall" :*/
fileprivate let str_totalName:[Character] = ["f","r","o","m","F","r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let str_renderShareContent:String = "requestCcall label data section"
fileprivate let str_sharedTitle:[Character] = ["a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let str_managerName:String = "oattention"
fileprivate let str_viewFillText:String = "Videostyle equal equal index let"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let str_taskContent:[UInt8] = [0x67,0x6e,0x69,0x6d,0x6f,0x63,0x6e,0x69,0x20,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x76,0x69,0x65,0x63,0x65,0x52,0x20,0x5d,0x6f,0x65,0x64,0x69,0x76,0x20,0x63,0x69,0x74,0x61,0x6d,0x6f,0x74,0x75,0x41,0x5b]

/*: "refuseCall" :*/
fileprivate let str_everyViewValue:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let str_dataValue:String = "clearl"

/*: "onCancelCall" :*/
fileprivate let str_cellButtonData:String = "onCancage gift private cell"
fileprivate let str_gallerySizeName:String = "elCalllog equal"

/*: "retryAfter" :*/
fileprivate let str_equalData:String = "label add hide gift coinretryA"
fileprivate let str_statusValue:String = "poper"

/*: "onRequestCall" :*/
fileprivate let str_frameTableName:String = "onRequbegin path"

/*: "failType" :*/
fileprivate let str_appValue:String = "clear"
fileprivate let str_freeName:String = "ailTypename to view too view"

/*: "callWaitingTime" :*/
fileprivate let str_showLocationText:String = "ccellll"
fileprivate let str_fileEqualData:[Character] = ["g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let str_moreData:String = "toUidinfo make let please"

/*: "Free 1 min" :*/
fileprivate let str_playerText:String = "Free var gift min data"
fileprivate let str_toData:[Character] = ["1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let str_endValue:String = "be key user let%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FailRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class FailRecognizerDelegate: ServiceRecognizerDelegate {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.iceHockeyPlayer()
            }
        }
        //: self.resetCamera()
        self.resetObject()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        userSubviews()
        //: setupSubViewsConstraint()
        overBind()
        //: bindInteraction()
        betweenSearch()
        //: videoManager.startPreview(isMatch: true)
        videoManager.answerDelayGetMoving(isMatch: true)
        //: setPlayer(isRandon: false)
        ballplayerRandon(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            stochasticLaunchVisualCommunication()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_countName)), "data": ["logId": logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        timerOnInstance()
        //: if TalkingSocketManager.shared.isTalking == false {
        if JewishCalendarMonthThen.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            JewishCalendarMonthThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            JewishCalendarMonthThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            JewishCalendarMonthThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            JewishCalendarMonthThen.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AddCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = AddCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = PastChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .appNameVisualProperty()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(stochasticLaunchVisualCommunication), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: consLet_errorDeviceHeightData + 1, height: consLet_sessionName + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(str_labelTitle.prefix(4)) + "to sta" + String(str_cornerValue.prefix(5)) + String(str_popText) + str_enableData.replacingOccurrences(of: "title", with: "g")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.nearTap(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.nearTap(name: (String(str_modelValue) + str_managerLabelData.lowercased() + String(str_whiteValue.suffix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(methodClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: LabelPositionReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = LabelPositionReactiveCompatible()
        //: btn.setImage(UIImage.nearTap(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.nearTap(name: (str_sizeData.replacingOccurrences(of: "m", with: "co") + "_views" + String(str_dataContent))), for: .normal)
        //: btn.setImage(UIImage.nearTap(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.nearTap(name: (str_sizeData.replacingOccurrences(of: "m", with: "co") + "_views" + String(str_dataContent))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.loadEnable(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.loadEnable(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(str_normalValue) + str_viewText.replacingOccurrences(of: "range", with: "t")).localizedArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nearTap(name: "icon_lounge")
        img.image = UIImage.nearTap(name: (String(str_feeData) + String(str_logData)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.nearTap(name: "icon_rank_coin")
        img.image = UIImage.nearTap(name: (String(str_frameValue) + str_genderHomeTitle.replacingOccurrences(of: "request", with: "n")))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.loadEnable(type: .Regular, fontSize: 15)
        lab.font = UIFont.loadEnable(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.nearTap(name: "vip_btn_top")
        imgV.image = UIImage.nearTap(name: (String(str_infoTitle.suffix(7)) + String(str_pathToTitle.prefix(4))))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: LabelPositionReactiveCompatible = {
        //: let btn = TalkingButton()
        let btn = LabelPositionReactiveCompatible()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(shirtButtonClickCostAntheral), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.nearTap(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.nearTap(name: (String(str_indexTitle.prefix(5)) + "earby" + String(str_byCellTitle.suffix(4)) + "tings" + String(str_pointFreeContent))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.nearTap(name: "icon_rank_coin")
        imgV.image = UIImage.nearTap(name: (String(str_frameValue) + str_genderHomeTitle.replacingOccurrences(of: "request", with: "n")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .beyondEnd()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(str_hostMenuData)).localizedArguments(vipFee)
        //: lab.font = UIFont.loadEnable(type: .Medium, fontSize: 15)
        lab.font = UIFont.loadEnable(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: GiftAlertView = //: return GiftAlertView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension FailRecognizerDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func ballplayerRandon(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = ResourceEffectTool.default.butterflyEffect(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(str_markViewCornerName.suffix(5)) + "son err" + String(str_nameToolData)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func errnoPull(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = ["matchId": self.matchId,
                      //: "cmd": cmd,
                      "cmd": cmd,
                      //: "errno": errno] as [String: Any]
                      "errno": errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        LogSendThen.hitOrMissImage(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.stochasticStatus()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json["matchId"].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func dayFee() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        LogSendThen.then { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json["VIPFee"].intValue
            //: self.refreshVideoCallPrice()
            self.iceHockeyPlayer()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension FailRecognizerDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func shirtButtonClickCostAntheral() {
        //: PostPushManager.share.func__pushToSubscribeAlert()
        PostPushManager.share.markParams()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func methodClick() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        pointBtn()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        consLet_contentData.postEqual(eventID: consLet_failName)

        //: if IndexStyleAppManager.share.appConfigMode.displayMatchV4PopUp, IndexStyleAppManager.share.loginUserMode.freeCallTimes > 0,
        if IndexStyleAppManager.share.appConfigMode.displayMatchV4PopUp, IndexStyleAppManager.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.tokenShow()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        userRead()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func stochasticLaunchVisualCommunication() {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard JewishCalendarMonthThen.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.noblesseExtra(showMsg: consLet_viewData)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        contentView()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(str_ofCustomData.suffix(4)) + "hing n" + String(str_labName)).localized
        //: setPlayer(isRandon: true)
        ballplayerRandon(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if IndexStyleAppManager.share.loginUserMode.randomVideoType == 2 {
        if IndexStyleAppManager.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            consLet_contentData.postEqual(eventID: consLet_tabRecordTotalimateName)
            //: req_getVideoInfo()
            makeInfo()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            consLet_contentData.postEqual(eventID: consLet_marginData)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            JewishCalendarMonthThen.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            JewishCalendarMonthThen.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            JewishCalendarMonthThen.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            JewishCalendarMonthThen.shared.errorDelegate = self
            //: restart_randomVideo()
            situationPath()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func situationPath(cmd: String = (String(str_layerTitle) + str_awakeName.replacingOccurrences(of: "to", with: "h")), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        timerOnInstance()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        errnoPull(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func stochasticStatus() {
        // 停止定时器
        //: destroyWaitingTimer()
        timerOnInstance()
        //: showCloseBtn()
        pointBtn()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(str_labelTitle.prefix(4)) + "to sta" + String(str_cornerValue.prefix(5)) + String(str_popText) + str_enableData.replacingOccurrences(of: "title", with: "g")).localized
        //: setPlayer(isRandon: false)
        ballplayerRandon(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isTalking == false {
        if JewishCalendarMonthThen.shared.isTalking == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            JewishCalendarMonthThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            JewishCalendarMonthThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            JewishCalendarMonthThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            JewishCalendarMonthThen.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        iceHockeyPlayer()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func timerOnInstance() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func makeInfo() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SafetyMarginReactiveCompatible()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(str_firstTitle.suffix(5)) + "eeCal" + String(str_liveTitle.prefix(6)) + String(str_clipData))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: str_insideName.map{errorMoment(at: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        consLet_listTitle.coloring(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.stochasticStatus()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: IndexStyleAppManager.share.loginUserMode.randomVideoType = 1
                    IndexStyleAppManager.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.stochasticLaunchVisualCommunication()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.noblesseExtra(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = AstatineMeasurable.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = WoodPlayerManager()
                //: player.setMute(bEnable: true)
                player.setQuantity(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.seedWithoutForefront(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                NameCallManager.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                KeyPermissionTool.searchAndMicrophoneCell { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.upThrough()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func upThrough() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        consLet_contentData.postEqual(eventID: consLet_bottomValue)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard NameCallManager.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.noblesseExtra(showMsg: String(bytes: str_callData.map{$0^41}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            NameCallManager.shared.fortView(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.stochasticStatus()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = ContextCallVc()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.userRead()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.stochasticStatus()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func contentView() {
        //: if IndexStyleAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if IndexStyleAppManager.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(IndexStyleAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(IndexStyleAppManager.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func pointBtn() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func resetObject() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.answerDelayGetMoving()
        }
    }
}

// MARK: - QualityMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension FailRecognizerDelegate: QualityMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func sizeBy(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = RoomResultModel.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = ["matchId": model.matchId,
                                       //: "source": model.source,
                                       "source": model.source,
                                       //: "type": model.type,
                                       "type": model.type,
                                       //: "uid": model.uid]
                                       "uid": model.uid]
        //: let index = EnableFreeCallType.randow
        let index = SumHiddenSubscriptType.randow
        //: if IndexStyleAppManager.share.loginUserMode.freeCallTimes > 0 && IndexStyleAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if IndexStyleAppManager.share.loginUserMode.freeCallTimes > 0 && IndexStyleAppManager.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(str_totalName)))
            //: TalkingSocketManager.shared.isFreeCall = true
            JewishCalendarMonthThen.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = ["cmd": (String(str_renderShareContent.prefix(8)) + String(str_sharedTitle)), "data": callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        JewishCalendarMonthThen.shared.objectInfo(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_countName)), "data": ["logId": self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.situationPath(cmd: (str_managerName.replacingOccurrences(of: "attention", with: "n") + String(str_viewFillText.prefix(5)) + "Match"))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func subjugate(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json["logId"].intValue
    }
}

// MARK: - LastStableCallerDelegate【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension FailRecognizerDelegate: LastStableCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func addIn(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func textTitle(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func read(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func appRecord(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            EveryLastBaseDelegate.userUrl(body: String(bytes: str_taskContent.reversed(), encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = RoomResultModel.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            toEachOne(toUid: String(uid))
        }
        //: stop_randomVideo()
        stochasticStatus()
        //: IndexStyleAppManager.share.start1v1TalkCall(info: data, situation: .Calling)
        IndexStyleAppManager.share.componentView(info: data, situation: .Calling)
    }
}

// MARK: - DataJumpPartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension FailRecognizerDelegate: DataJumpPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chatRe(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        situationPath(cmd: (String(str_everyViewValue) + str_dataValue.replacingOccurrences(of: "clear", with: "al")))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func contextOfUse(data _: [String: Any]) {}
}

// MARK: - ViewObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension FailRecognizerDelegate: ViewObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func realize(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(str_cellButtonData.prefix(6)) + String(str_gallerySizeName.prefix(6))) { // 取消通话异常
            //: stop_randomVideo()
            stochasticStatus()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (str_managerName.replacingOccurrences(of: "attention", with: "n") + String(str_viewFillText.prefix(5)) + "Match"), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            stochasticStatus()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json["matchId"].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.noblesseExtra(showMsg: msg)
            //: uploadFailTime(type: 2)
            upload(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (str_managerName.replacingOccurrences(of: "attention", with: "n") + String(str_viewFillText.prefix(5)) + "Match"), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(str_equalData.suffix(6)) + str_statusValue.replacingOccurrences(of: "pop", with: "ft"))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json["matchId"].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.situationPath(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(str_frameTableName.prefix(6)) + "estCall"), errorNo == ResourceBlockDefaultsSerializable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            stochasticStatus()
            //: guard IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue else { return }
            //: PostPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            PostPushManager.share.chromaticColourMakeEnd(clickEvent: consLet_sessionTitle, sufficient: false)
            //: uploadFailTime(type: 1)
            upload(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        situationPath(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension FailRecognizerDelegate {
    //: func uploadFailTime(type: Int) {
    func upload(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(str_appValue.replacingOccurrences(of: "clear", with: "f") + String(str_freeName.prefix(7)))] = type
        //: parm["callWaitingTime"] = time
        parm[(str_showLocationText.replacingOccurrences(of: "cell", with: "a") + "Waitin" + String(str_fileEqualData))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        consLet_contentData.selectEvent(eventID: consLet_senseValue, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func toEachOne(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(str_showLocationText.replacingOccurrences(of: "cell", with: "a") + "Waitin" + String(str_fileEqualData))] = time
        //: parm["toUid"] = toUid
        parm["toUid"] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        consLet_contentData.selectEvent(eventID: consLet_deviceClickText, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension FailRecognizerDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func iceHockeyPlayer() {
        //: guard IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !IndexStyleAppManager.share.loginUserMode.loungePlus
        vipIcon.isHidden = !IndexStyleAppManager.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = SumHiddenSubscriptType.randow
        //: if IndexStyleAppManager.share.loginUserMode.freeCallTimes > 0 && IndexStyleAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if IndexStyleAppManager.share.loginUserMode.freeCallTimes > 0, IndexStyleAppManager.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if IndexStyleAppManager.share.appUserConfigMode.callBtnStyle == 1 {
            if IndexStyleAppManager.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(str_playerText.prefix(5)) + String(str_toData)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = IndexStyleAppManager.share.loginUserMode.loungePlus ? vipFee:fee
            let price = IndexStyleAppManager.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = "%@/min".localizedArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.little(fontSize: 10)])
            let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.little(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = IndexStyleAppManager.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = IndexStyleAppManager.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = IndexStyleAppManager.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = IndexStyleAppManager.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !IndexStyleAppManager.share.loginUserMode.loungePlus
            priceStackView.isHidden = !IndexStyleAppManager.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func userSubviews() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func overBind() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(consLet_netViewValue + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func betweenSearch() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(iceHockeyPlayer),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: consLet_contentValue,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(dayFee),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: consLet_buildName,
                                               //: object: nil)
                                               object: nil)
    }
}
