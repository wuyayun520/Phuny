
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let str_indexCellErrorData:[UInt8] = [0x45,0x54,0x54,0x48,0x41,0x5b,0x4d,0x45,0x54,0x5b,0x54,0x45,0x5d,0x5b,0x57,0x51,0x47,0x47,0x41,0x41,0x40,0x5b,0x4a,0x4b,0x50,0x4d,0x42,0x4d,0x47,0x45,0x50,0x4d,0x4b,0x4a]

private func enableRead(recommend num: UInt8) -> UInt8 {
    return num ^ 4
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let str_areaName:[UInt8] = [0x4a,0x5b,0x5b,0x47,0x4e,0x54,0x42,0x4a,0x5b,0x54,0x58,0x5e,0x49,0x58,0x48,0x59,0x42,0x49,0x4e,0x54,0x58,0x5e,0x48,0x48,0x4e,0x4e,0x4f,0x54,0x45,0x44,0x5f,0x42,0x4d,0x42,0x48,0x4a,0x5f,0x42,0x44,0x45]

private func guardIndex(count num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let str_equalValue:String = "app return wrap valuemf/re"
fileprivate let str_viewSizeSourceContent:String = "user make nearge/cre"
fileprivate let str_hiddenValue:String = "in in viewateA"
fileprivate let str_leadingValue:String = "add var normalpplePay"

/*: "productId" :*/
fileprivate let str_tapQuantityeractionName:String = "productIdcount let let"

/*: "source" :*/
fileprivate let str_makeInputVarData:String = "labelource"

/*: "IAP充值创建订单失败: :*/
fileprivate let str_textValue:[Character] = ["I","A","P","充","值","\u{521b}","建"]
fileprivate let str_mainTitle:[Character] = ["订","\u{5355}","失","败",":"]

/*: , 参数: :*/
fileprivate let str_indexData:String = ", leading数:"

/*: ." :*/
fileprivate let str_starName:String = "."

/*: "orderNum" :*/
fileprivate let str_toText:String = "ORDE"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let str_viewValue:String = "mf/recvar view app"
fileprivate let str_meName:String = "text table error/app"
fileprivate let str_whiteName:String = "lePaextra for view date"

/*: "IAP充值校验失败: :*/
fileprivate let str_videoContent:[Character] = ["I","A","P","充","值","\u{6821}","\u{9a8c}"]
fileprivate let str_centerName:String = "失\u{8d25}:"

/*: "transactionId" :*/
fileprivate let str_cornerData:String = "TRANSAC"
fileprivate let str_attributeData:[Character] = ["t"]
fileprivate let str_contentValue:[Character] = ["i","o","n","I","d"]

/*: "reportMoney" :*/
fileprivate let str_backgroundData:String = "REPO"
fileprivate let str_originEqualValue:[Character] = ["e","y"]

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let str_toDeadlineContent:[Character] = ["m","f","/","A","u","t","o","S","u","b","/","A","p","p","l","e","C"]
fileprivate let str_addValue:String = "reaup"

/*: "IAP订阅创建订单失败: :*/
fileprivate let str_playGoldText:String = "IAP订阅创color equal"
fileprivate let str_indexPhotoFalseValue:[Character] = ["建"]
fileprivate let str_labText:String = "订\u{5355}失败:"

/*: "orderId" :*/
fileprivate let str_appValue:[UInt8] = [0x1,0x1c,0xa,0xb,0x1c,0x27,0xa]

private func imageRange(title num: UInt8) -> UInt8 {
    return num ^ 110
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let str_viewText:String = "mf/Atop extension"
fileprivate let str_sectionData:String = "send transaction giftub/Ap"
fileprivate let str_constraintName:String = "plePshow card table"
fileprivate let str_viewName:String = "aySuctable model white event pic"

/*: "IAP订阅校验失败: :*/
fileprivate let str_bottomValue:String = "IAP订make for self path"

/*: "App" :*/
fileprivate let str_instanceSoundValue:String = "item hidden likeApp"

/*: "OrderTransactionInfo_ :*/
fileprivate let str_titleName:[Character] = ["O","r","d","e","r","T","r","a","n","s","a","c","t","i","o"]
fileprivate let str_closeData:String = "nInfo_block log bottom"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let str_toCloseValue:[UInt8] = [0xe,0x33,0x25,0x24,0x33,0x15,0x33,0x20,0x2f,0x32,0x20,0x22,0x35,0x28,0x2e,0x2f,0x8,0x2f,0x27,0x2e,0x1e,0x12,0x34,0x23,0x32,0x22,0x33,0x28,0x23,0x24,0x1e]

/*: "verifyData" :*/
fileprivate let str_hiddenText:[UInt8] = [0x46,0x35,0x42,0x39,0x36,0x49,0x14,0x31,0x44,0x31]

fileprivate func dittyBag(origin num: UInt8) -> UInt8 {
    let value = Int(num) + 48
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let str_beyondText:String = "IAP\u{67e5}询"
fileprivate let str_tapData:String = "prpair"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let str_capacityModeName:[Character] = ["I","A","P","查","\u{8be2}","内","购","商","\u{54c1}","失","败",","," ","p","r","o","d","u","c"]
fileprivate let str_listenTitle:[Character] = ["t","s",".","c","o","u"]
fileprivate let str_withEditData:[Character] = ["n","t","为","0","."]

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let str_rowImageInName:String = "IAP查\u{8be2}"
fileprivate let str_textTitle:String = "失败, "
fileprivate let str_fileInsideValue:[Character] = ["E","r","r","o","r",":"]

/*: "IAP-purchased: :*/
fileprivate let str_indexValue:String = "begin at data self touchIAP-p"
fileprivate let str_onBringName:[Character] = ["a","s","e","d",":"]

/*: , 订单号: :*/
fileprivate let str_genderName:String = "user"
fileprivate let str_extraName:String = " 订单号:"

/*: , 商品Id: :*/
fileprivate let str_ofTitle:[Character] = [","," ","商","\u{54c1}","I","d",":"]

/*: , 苹果transId: :*/
fileprivate let str_methodData:String = "let selection face answer, 苹果t"

/*: "IAP-failed: :*/
fileprivate let str_itemData:String = "model"
fileprivate let str_makeData:[Character] = ["A","P","-","f","a","i","l","e","d",":"]

/*: "IAP-deferred: :*/
fileprivate let str_sizeValue:String = "IAP-defemodel any item every image"
fileprivate let str_textCaptureValue:[Character] = ["r","r","e","d",":"]

/*: "IAP未知交易类型: :*/
fileprivate let str_videoData:[Character] = ["I","A","P","未","\u{77e5}","交","易"]
fileprivate let str_freshValue:String = "类\u{578b}:"

/*: " 未知的交易类型" :*/
fileprivate let str_alongBottomData:String = " \u{672a}知\u{7684}"
fileprivate let str_sessionData:String = "交\u{6613}类\u{578b}"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let str_workDataContent:String = "IAP获取view color in action view"
fileprivate let str_managerName:String = "mode color row screen据失败, tr"
fileprivate let str_mBeginTitle:[Character] = ["a","n","s","a","c","t","i","o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let str_noTitle:String = "scale break, or"
fileprivate let str_cornerText:String = "d:make tap"

/*: , payType: :*/
fileprivate let str_rowEraseData:[Character] = [","," ","p","a","y"]
fileprivate let str_errorValue:String = "item label view equalType:"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SoundPressureLevelTransactionObserver.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import UIKit
import UIKit

//: import StoreKit
import StoreKit

//: import SwiftyJSON
import SwiftyJSON

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let consLet_explainData = NSNotification.Name(rawValue: String(bytes: str_indexCellErrorData.map{enableRead(recommend: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let consLet_userName = 9

/// 支付类型
//: enum ApplePayType {
enum InvisibleType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum ClearBlockKeyRepresentable: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double) -> Void
typealias IAPcompletionHandle = (ClearBlockKeyRepresentable, Double) -> Void

//: class AppleIAPManager: NSObject {
class SoundPressureLevelTransactionObserver: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: InvisibleType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = SoundPressureLevelTransactionObserver()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(modelApp),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func modelApp() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension SoundPressureLevelTransactionObserver {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func digitiserKey(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SafetyMarginReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(str_equalValue.suffix(5)) + "char" + String(str_viewSizeSourceContent.suffix(6)) + String(str_hiddenValue.suffix(4)) + String(str_leadingValue.suffix(7)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(str_tapQuantityeractionName.prefix(9)))] = productId
        //: dict["source"] = source
        dict["source"] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        consLet_listTitle.coloring(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: EqualLogTool.writeDoing(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                EqualLogTool.writeDoing(msg: (String(str_textValue) + String(str_mainTitle)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(str_toText.lowercased() + "rNum")].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func awakeParams(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SafetyMarginReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(str_viewValue.prefix(6)) + "harge" + String(str_meName.suffix(4)) + String(str_whiteName.prefix(4)) + "yNotify")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        consLet_listTitle.coloring(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: EqualLogTool.writeDoing(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    EqualLogTool.writeDoing(msg: (String(str_videoContent) + str_centerName) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.roomForDisappear(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.colorful()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: consLet_explainData, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(str_backgroundData.lowercased() + "rtMon" + String(str_originEqualValue))].doubleValue)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension SoundPressureLevelTransactionObserver {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func explosiveTraceDetection(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SafetyMarginReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(str_toDeadlineContent) + str_addValue.replacingOccurrences(of: "up", with: "t") + "eOrder")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(str_tapQuantityeractionName.prefix(9)))] = productId
        //: dict["source"] = source
        dict["source"] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        consLet_listTitle.coloring(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: EqualLogTool.writeDoing(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                EqualLogTool.writeDoing(msg: (String(str_playGoldText.prefix(6)) + String(str_indexPhotoFalseValue) + str_labText) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json["orderId"].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func executeParams(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = SafetyMarginReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(str_viewText.prefix(4)) + "utoS" + String(str_sectionData.suffix(5)) + String(str_constraintName.prefix(4)) + String(str_viewName.prefix(5)) + "cess")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        consLet_listTitle.coloring(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: EqualLogTool.writeDoing(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    EqualLogTool.writeDoing(msg: (String(str_bottomValue.prefix(4)) + "阅校验失\u{8d25}:") + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.roomForDisappear(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.showBy()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue)
            self.completionHandle?(.veritySucceed, result[(str_backgroundData.lowercased() + "rtMon" + String(str_originEqualValue))].doubleValue)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension SoundPressureLevelTransactionObserver {
    /// 初始化数据
    //: private func iap_initData() {
    private func onLocal() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = equalObject(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = equalObject(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func equalObject(payType: InvisibleType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = colorful()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = showBy()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func colorful() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = consLet_partyTitle.string(forKey: consLet_screenTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(str_titleName) + String(str_closeData.prefix(6))) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func showBy() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = consLet_partyTitle.string(forKey: consLet_screenTitle) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: str_toCloseValue.map{$0^65}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func lowerClass(_ transactionId: String, _ payType: InvisibleType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: str_hiddenText.map{dittyBag(origin: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: str_hiddenText.map{dittyBag(origin: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension SoundPressureLevelTransactionObserver {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func addUserTransactions() {
        //: iap_initData()
        onLocal()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            modelRetry(dict[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            modelRetry(dict[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func modelRetry(_ transactionId: String?, _ payType: InvisibleType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        roomForDisappear(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension SoundPressureLevelTransactionObserver {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func errorObserve(productId: String, payType: InvisibleType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        onLocal()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            digitiserKey(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.bringEqual(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            explosiveTraceDetection(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0)
                    self.completionHandle?(.createOrderFail, 0)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.bringEqual(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func bringEqual(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: EqualLogTool.writeDoing(msg: "IAP查询内购商品失败, productId:\(productId).")
            EqualLogTool.writeDoing(msg: (str_beyondText + "内\u{8d2d}商品失败, " + str_tapData.replacingOccurrences(of: "pair", with: "od") + "uctId:") + "\(productId).")
            //: self.completionHandle?(.notArrow, 0)
            self.completionHandle?(.notArrow, 0)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.executeStatus()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func executeStatus() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension SoundPressureLevelTransactionObserver: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: EqualLogTool.writeDoing(msg: "IAP查询内购商品失败, products.count为0.")
            EqualLogTool.writeDoing(msg: (String(str_capacityModeName) + String(str_listenTitle) + String(str_withEditData)))
            //: self.completionHandle?( .noProductId, 0)
            self.completionHandle?(.noProductId, 0)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: EqualLogTool.writeDoing(msg: "IAP查询内购商品失败, Error:\(error).")
        EqualLogTool.writeDoing(msg: (str_rowImageInName + "内购商品" + str_textTitle.capitalized + String(str_fileInsideValue)) + "\(error).")
        //: self.completionHandle?( .noProductId, 0)
        self.completionHandle?(.noProductId, 0)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension SoundPressureLevelTransactionObserver: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0)
                    self.completionHandle?(.renewSucceed, 0)
                    //: } else {
                } else { // 普通购买和订阅
                    //: EqualLogTool.writeDoing(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    EqualLogTool.writeDoing(msg: (String(str_indexValue.suffix(5)) + "urch" + String(str_onBringName)) + "\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier)" + (String(str_methodData.suffix(5)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    roomForDisappear(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: EqualLogTool.writeDoing(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                EqualLogTool.writeDoing(msg: (str_itemData.replacingOccurrences(of: "model", with: "I") + String(str_makeData)) + "\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier)" + (String(str_methodData.suffix(5)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0)
                self.completionHandle?(.failed, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0)
                self.completionHandle?(.restored, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: EqualLogTool.writeDoing(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                EqualLogTool.writeDoing(msg: (String(str_sizeValue.prefix(8)) + String(str_textCaptureValue)) + "\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier)" + (String(str_methodData.suffix(5)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0)
                self.completionHandle?(.deferred, 0)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0)
                self.completionHandle?(.unknow, 0)
                //: createOrderId = nil
                createOrderId = nil
                //: EqualLogTool.writeDoing(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                EqualLogTool.writeDoing(msg: (String(str_videoData) + str_freshValue) + "\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier)" + (String(str_methodData.suffix(5)) + "ransId:") + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError(" 未知的交易类型")
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func roomForDisappear(_ transactionId: String, _ payType: InvisibleType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = lowerClass(transactionId, payType) else {
            //: EqualLogTool.writeDoing(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            EqualLogTool.writeDoing(msg: (String(str_workDataContent.prefix(5)) + "\u{672c}地收据\u{6570}" + String(str_managerName.suffix(7)) + String(str_mBeginTitle)) + "\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] == transactionId || $0["orderId"] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue)): transactionId,
                                     //: "orderId": createOrderId!,
                                     "orderId": createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_hiddenText.map{dittyBag(origin: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.colorful()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] == transactionId || $0["orderId"] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue)): transactionId,
                                     //: "orderId": createOrderId!,
                                     "orderId": createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: str_hiddenText.map{dittyBag(origin: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.showBy()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > consLet_userName {
            //: self.completionHandle?(.verityFail, 0)
            self.completionHandle?(.verityFail, 0)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            awakeParams(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(str_cornerData.lowercased() + String(str_attributeData) + String(str_contentValue))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            executeParams(transactionId, params: paramsArr.first!)
        }
    }
}
