
//: Declare String Begin

/*: / :*/
fileprivate let str_sourceModeMoreData:[Character] = ["/"]

/*:  ( :*/
fileprivate let str_toMeValue:String = " ("

/*: ; build: :*/
fileprivate let str_pathData:[Character] = [";"]
fileprivate let str_makeValue:String = "equal situation name layer manager build:"

/*: ; iOS  :*/
fileprivate let str_iconData:String = "; iOS equal false then view"

/*: ;  :*/
fileprivate let str_styleData:String = "; "

/*: )" :*/
fileprivate let str_sectionText:[Character] = [")"]

/*: "audio" :*/
fileprivate let str_tableName:String = "ahiddenio"

/*: + :*/
fileprivate let str_countData:String = "+"

/*: .wav" :*/
fileprivate let str_sizeName:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let str_contentQuantityData:String = "audidata"
fileprivate let str_colorModelText:[Character] = ["/","w","a","v"]

/*: .jpg" :*/
fileprivate let str_layerData:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let str_shareValue:[Character] = ["i","m","a","g","e","/","j","p","e","g"]

/*: "://app." :*/
fileprivate let str_miniName:String = "://appcollection icon"
fileprivate let str_areaValue:String = "top"

/*: "\"data\":null" :*/
fileprivate let str_dataName:String = "\"data"

/*: "\"data\":{}" :*/
fileprivate let str_imageTitle:String = "\"data"
fileprivate let str_indexName:String = "\":{}"

/*: ":null" :*/
fileprivate let str_textContent:[Character] = [":","n","u","l","l"]

/*: ":\"\"" :*/
fileprivate let str_afterValue:String = ":\"\\"

/*: "Request failed, Try again later" :*/
fileprivate let str_shareHandleValue:[UInt8] = [0x8c,0x9f,0xab,0xaf,0x9f,0xad,0xae,0x5a,0xa0,0x9b,0xa3,0xa6,0x9f,0x9e,0x66,0x5a,0x8e,0xac,0xb3,0x5a,0x9b,0xa1,0x9b,0xa3,0xa8,0x5a,0xa6,0x9b,0xae,0x9f,0xac]

fileprivate func equalMode(look num: UInt8) -> UInt8 {
    let value = Int(num) - 58
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "json error" :*/
fileprivate let str_playCallName:[Character] = ["j","s","o","n"," "]
fileprivate let str_userData:[Character] = ["e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let str_equalContent:String = "messageat"
fileprivate let str_buttonDismissName:String = "title new playerphone&ver"
fileprivate let str_soundName:[Character] = ["s","i","o","n","="]

/*: &packageId= :*/
fileprivate let str_indexModelText:String = "date last image push for&pack"

/*: &bundleId= :*/
fileprivate let str_labValue:[Character] = ["&","b","u"]
fileprivate let str_eventValue:String = "ndleId=near right true if manager"

/*: &lang= :*/
fileprivate let str_labelData:[Character] = ["&","l","a","n","g","="]

/*: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| " :*/
fileprivate let str_fromData:[Character] = ["?","!","@","#","$","^"]
fileprivate let str_dataTitle:String = "&quote*+"
fileprivate let str_downContent:String = "\'\"`<>"
fileprivate let str_backgroundName:String = "}/\\| "

/*: "/route/slowindex" :*/
fileprivate let str_contentTitle:[Character] = ["/","r","o","u","t","e","/","s","l","o","w","i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let str_scriptValue:[Character] = ["/","r","o","u","t","e"]
fileprivate let str_viewData:String = "/indexkey path color if true"

/*: "?path= :*/
fileprivate let str_modelTitle:String = "error"
fileprivate let str_centerData:String = "path=to video"

/*: "&GJNonceStr= :*/
fileprivate let str_infoStateName:String = "var var reason awake text&GJNo"
fileprivate let str_lineData:String = "model let screennceStr="

/*: "& :*/
fileprivate let str_screenLabModelValue:String = "level"

/*: = :*/
fileprivate let str_makeData:String = "result"

/*: "&params= :*/
fileprivate let str_pathName:String = "time var string&params"
fileprivate let str_normalValue:[Character] = ["="]

/*: "/ :*/
fileprivate let str_infoText:String = "shared"

/*: "? :*/
fileprivate let str_sizeTitle:[Character] = ["?"]

/*: "token" :*/
fileprivate let str_ofValue:String = "tlistken"

/*: "请求成功 :*/
fileprivate let str_labName:[Character] = ["请","\u{6c42}","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let str_equalTitle:[Character] = ["A","A"]
fileprivate let str_nameContent:[Character] = ["A","A","A","A","A","A"]

/*: "AA" :*/
fileprivate let str_localData:String = "aa"

/*: "BB" :*/
fileprivate let str_processorTouchText:String = "bb"

/*: "下载完成 -  :*/
fileprivate let str_inputCenterManagerData:[Character] = ["下","载","完","成"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let str_effectName:String = "make self make model log/Doc"

/*: ".mp4" :*/
fileprivate let str_playerData:String = "location else.mp4"

/*: "移动地址: :*/
fileprivate let str_kitName:String = "移动地view:"

/*: "后台任务下载回来" :*/
fileprivate let str_textName:String = "后台任务\u{4e0b}载"
fileprivate let str_requestValue:String = "回来"

/*: "下载成功 : :*/
fileprivate let str_statusData:String = "image载成功 :"

/*: "Net Error" :*/
fileprivate let str_backData:[Character] = ["N","e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let str_noObserverValue:[Character] = ["下"]
fileprivate let str_interactionSendName:[Character] = ["载","失","败"," ",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  KeyItemSessionDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import UIKit
import UIKit

//: import Alamofire
import Alamofire

//: import CoreMedia
import CoreMedia

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let consLet_homeValue = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let consLet_lineData = "\(consLet_deviceData)/\(consLet_keyScreenTimeTitle) (\(consLet_recordData); build:\(consLet_likeContent); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: SpotErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let consLet_listTitle = KeyItemSessionDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class KeyItemSessionDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func coloring(model: SafetyMarginReactiveCompatible, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.fromObject() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.callData(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.railroad(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func callData(model: SafetyMarginReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.sendOf(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.requestPic(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    targetAcross(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SpotErrorResponse(errorCode: HiddenComparable.NetError.rawValue, errorMsg: consLet_momentValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        noblesseExtra(showMsg: consLet_momentValue)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == "audio" {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.statusJudge())+\(consLet_packageName).wav", mimeType: (str_contentQuantityData.replacingOccurrences(of: "data", with: "o") + String(str_colorModelText)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: (String(str_shareValue)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    targetAcross(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SpotErrorResponse(errorCode: HiddenComparable.NetError.rawValue, errorMsg: consLet_momentValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        noblesseExtra(showMsg: consLet_momentValue)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func railroad(model: SafetyMarginReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.sendOf(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.requestPic(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: consLet_senseData ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = consLet_homeValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    targetAcross(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SpotErrorResponse(errorCode: HiddenComparable.NetError.rawValue, errorMsg: consLet_momentValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        noblesseExtra(showMsg: consLet_momentValue)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = consLet_homeValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    targetAcross(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, SpotErrorResponse(errorCode: HiddenComparable.NetError.rawValue, errorMsg: consLet_momentValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        noblesseExtra(showMsg: consLet_momentValue)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func targetAcross(model: SafetyMarginReactiveCompatible, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            TalkingBenderTotalensityRequestThen.share.upwardsStart(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if consLet_senseData, model.requestServer.contains("://app.") {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.aes256Decrypt(key: TalkingBenderTotalensityRequestThen.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: (str_dataName + "\":null"), with: "\"data\":{}")
        //: if !Encryption_Request {
        if !consLet_senseData {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<BlockVideoBaseResponse>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == HiddenComparable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, SpotErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    noblesseExtra(showMsg: responseModel.msg ?? String(bytes: str_shareHandleValue.map{equalMode(look: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case HiddenComparable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: consLet_askData, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, SpotErrorResponse(errorCode: HiddenComparable.NetError.rawValue, errorMsg: (String(str_playCallName) + String(str_userData)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func sendOf(model: SafetyMarginReactiveCompatible) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (str_equalContent.replacingOccurrences(of: "message", with: "pl") + "form=i" + String(str_buttonDismissName.suffix(9)) + String(str_soundName)) + "\(consLet_environmentContent)" + (String(str_indexModelText.suffix(5)) + "ageId=") + "\(consLet_packageName)" + (String(str_labValue) + String(str_eventValue.prefix(7))) + "\(consLet_recordData)&lang=\(TalkingBenderTotalensityRequestThen.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if consLet_senseData, serverUrl.contains("://app.") {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if TalkingBenderTotalensityRequestThen.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(str_contentTitle)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(str_scriptValue) + String(str_viewData.prefix(6)))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append("?path=\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(str_infoStateName.suffix(5)) + String(str_lineData.suffix(7))) + "\(TalkingBenderTotalensityRequestThen.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.aes256Encrypt(key: TalkingBenderTotalensityRequestThen.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append("&params=\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func requestPic(model: SafetyMarginReactiveCompatible) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(consLet_lineData)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func cellDesign(model: SafetyMarginReactiveCompatible) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.sendOf(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: "请求成功\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(str_equalTitle) + String(str_nameContent))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: "下载完成 - \(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(str_effectName.suffix(4)) + "uments/") + NSDate.statusJudge() + ".mp4"
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: "移动地址:\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (str_textName + str_requestValue.capitalized))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = EveryLastBaseDelegate.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func listName(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: consLet_netTitle)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: "下载成功 :\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, SpotErrorResponse(errorCode: HiddenComparable.Normal.rawValue, errorMsg: (String(str_backData)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, SpotErrorResponse(errorCode: HiddenComparable.NetError.rawValue, errorMsg: (String(str_backData)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: "下载失败 :\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func roundSunnah(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: consLet_netTitle)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: "下载成功 :\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, SpotErrorResponse(errorCode: HiddenComparable.Normal.rawValue, errorMsg: (String(str_backData)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, SpotErrorResponse(errorCode: HiddenComparable.NetError.rawValue, errorMsg: (String(str_backData)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: "下载失败 :\(errMsg)")
            }
        }
    }
}
