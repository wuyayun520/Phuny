
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let str_shadowName:String = "else picSOCKET_A"
fileprivate let str_detailName:String = "DDR_KEYsource else pop user model"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let str_quoteName:[UInt8] = [0x1c,0x18,0xc,0x14,0xe,0x1d,0x28,0x1f,0x12,0xd,0xe,0x18,0xc,0x11,0xa,0x1d,0x28,0x1b,0xe,0x1a,0x1e,0xe,0x1c,0x1d,0xc,0xa,0x15,0x15,0x28,0x17,0x18,0x1d,0x12,0xf,0x12,0xc,0xa,0x1d,0x12,0x18,0x17]

fileprivate func backPhoto(state num: UInt8) -> UInt8 {
    let value = Int(num) - 201
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let str_labelModelData:[UInt8] = [0x7a,0x6d,0x6b,0x61,0x7e,0x6d,0x77,0x6e,0x64,0x67,0x69,0x7c,0x7b,0x6b,0x7a,0x6d,0x6d,0x66,0x77,0x66,0x67,0x7c,0x61,0x6e,0x61,0x6b,0x69,0x7c,0x61,0x67,0x66]

private func viewCreate(thumb num: UInt8) -> UInt8 {
    return num ^ 40
}

/*: "isCalling" :*/
fileprivate let str_userStorageValue:[Character] = ["i"]
fileprivate let str_frameName:[Character] = ["s","C","a","l","l","i","n","g"]

/*: "isTalking" :*/
fileprivate let str_exitValue:String = "isTalmini path row"

/*: "SOCKET 连接成功." :*/
fileprivate let str_userValue:[Character] = ["S","O","C","K","E","T"," ","\u{8fde}","接","成"]
fileprivate let str_positionData:String = "title."

/*: "SOCKET 连接失败, error: :*/
fileprivate let str_bottomTitle:String = "SOCKET system front model voice other"
fileprivate let str_makeGestureTitle:String = "连live失败"

/*: ." :*/
fileprivate let str_tapName:String = "."

/*: "SOCKET 失败 :*/
fileprivate let str_addName:[Character] = ["S","O","C","K","E","T"," ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let str_succeedName:String = "\u{6b21}, "
fileprivate let str_equalValue:String = "\u{5207}换过So"
fileprivate let str_pathValue:String = "重新\u{5c1d}\u{8bd5}，地\u{5740}\u{ff1a}"

/*: "SOCKET 已关闭, code: :*/
fileprivate let str_centerName:[Character] = ["S","O","C","K","E"]
fileprivate let str_giftValue:[Character] = ["T"," ","\u{5df2}","关","闭",","," ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let str_rangeMmValue:[Character] = [","]
fileprivate let str_stopValue:String = "pure if clear rea"

/*: "ws:// :*/
fileprivate let str_giftTaskText:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let str_sizeValue:[Character] = ["/","?","s","i","d","="]

/*: &uid= :*/
fileprivate let str_theValue:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let str_withContent:String = "self model app data player&plat"
fileprivate let str_replyMakeViewName:String = "phone&add tap port return"
fileprivate let str_buttonSubAddValue:String = "ion=lab head lab frame tap"

/*: &packageId= :*/
fileprivate let str_errorName:[Character] = ["&","p"]
fileprivate let str_currentText:[Character] = ["a","c","k","a","g","e","I","d","="]

/*: "1v1server. :*/
fileprivate let str_enableValue:[Character] = ["1","v","1","s","e","r"]
fileprivate let str_textValue:String = "ver.lab to make key data"

/*: .com:7766" :*/
fileprivate let str_keyValue:String = ".cocolor dimension"
fileprivate let str_destroyName:String = "m:7766var self"

/*: "43.129.185.194:7767" :*/
fileprivate let str_detailData:[Character] = ["4","3",".","1","2","9",".","1","8","5",".","1","9","4",":","7","7","6","7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let str_talkValue:[UInt8] = [0xf4,0xe8,0xe4,0xec,0xe2,0xf3,0x87,0x40,0x1a,0x36,0x40,0x1c,0x3b,0xd5,0xc2,0xc6,0xc4,0xcf,0xc6,0xc5,0xce,0xcb,0xce,0xd3,0xde,0xe4,0xcf,0xc6,0xc9,0xc0,0xc2,0xc3,0x40,0x3c,0x36,0x42,0x37,0xb,0x48,0x1b,0x2b,0x41,0x31,0xa,0x40,0x1a,0x36,0x42,0x22,0x14,0x4e,0x30,0xa,0x89]

/*: "cmd" :*/
fileprivate let str_showValue:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let str_countData:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let str_extraName:String = "callHequal action make color"
fileprivate let str_rawTitle:String = "playeat"

/*: "recievedDate" :*/
fileprivate let str_keyBottomValue:String = "manager"
fileprivate let str_valueText:String = "eciecall"

/*: "liveHeartBeat" :*/
fileprivate let str_toRecordRefuseName:String = "bar heightliveH"
fileprivate let str_appContent:[Character] = ["e","a"]
fileprivate let str_videoText:[Character] = ["r","t","B","e","a","t"]

/*: "🔗 关闭 socket" :*/
fileprivate let str_iconValue:[Character] = ["🔗"," ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let str_currentValue:String = "case app lab data selfTh"
fileprivate let str_stylePathData:String = "e usein to interaction for voice"
fileprivate let str_toName:String = "s outto app gift true skin"

/*: "SOCKET  :*/
fileprivate let str_beforeData:String = "push false if viewSOCKET "

/*: 重连. 重试次数： :*/
fileprivate let str_afterExistData:String = "title连. title"
fileprivate let str_pathData:[Character] = ["试","次","数","\u{ff1a}"]

/*: , 地址： :*/
fileprivate let str_loopName:[Character] = [","," ","地","址","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let str_responseText:String = "groupOCKE"
fileprivate let str_managerData:[Character] = ["T"," ","\u{8fde}","接","正","常"," ","不","需","要","重","新","连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let str_labelData:[Character] = ["S","O","C","K","E","T"," ","\u{6b63}"]
fileprivate let str_sessionName:String = "在连接 不"

/*: "&resume=1" :*/
fileprivate let str_regularData:[Character] = ["&","r","e","s","u","m","e"]
fileprivate let str_itemValue:[Character] = ["=","1"]

/*: "&lang= :*/
fileprivate let str_showCameraIfData:[Character] = ["&"]
fileprivate let str_fromContent:[Character] = ["l","a","n","g","="]

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let str_addTitle:[Character] = ["S","O","C","K","E","T"," ","\u{5f00}"]
fileprivate let str_makeName:[Character] = ["始","连","接",","," ","地","址","\u{ff1a}"]

/*: "onNetDisabled" :*/
fileprivate let str_keyName:String = "onNelabel state"
fileprivate let str_verticalName:String = "tDisabledtrue data size"

/*: "errno" :*/
fileprivate let str_targetValue:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let str_makeLeadingValue:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let str_skinLanguageData:String = "SOCKEbutterfly"
fileprivate let str_handleName:String = "gender outside wait rendermd: "
fileprivate let str_serverTitle:String = "ping.else user limit"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let str_dataEqualContent:[UInt8] = [0x9d,0x99,0x8d,0x95,0x8f,0x9e,0x6a,0xbe,0xbc,0xbe,0xad,0x2e,0x9,0xe7,0x30,0xfe,0x5,0xad,0xb7,0xae,0x84,0x6a,0xad,0xab,0xb6,0xb6,0x92,0xaf,0xab,0xbc,0xbe,0xac,0xaf,0xab,0xbe,0x78]

fileprivate func viewModel(free num: UInt8) -> UInt8 {
    let value = Int(num) - 74
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let str_fillName:[UInt8] = [0x18,0x4,0x8,0x0,0xe,0x1f,0x6b,0x27,0x22,0x3d,0x2e,0xaf,0xf4,0xd6,0xad,0xff,0xf0,0x28,0x26,0x2f,0x71,0x6b,0x27,0x22,0x3d,0x2e,0x3,0x2e,0x2a,0x39,0x3f,0x9,0x2e,0x2a,0x3f,0x65]

private func colorResume(background num: UInt8) -> UInt8 {
    return num ^ 75
}

/*: "onP" :*/
fileprivate let str_mainData:String = "view let stringonP"

/*: "msg" :*/
fileprivate let str_pathTitle:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let str_oneValue:[Character] = ["🔗"," ","\u{5904}","理","消","息","：","c","m","d","N","a"]
fileprivate let str_logMapData:String = "me:item false current name"

/*: , errNo: :*/
fileprivate let str_matchText:String = ", eruser model type corner"

/*: , msg: :*/
fileprivate let str_lineData:String = ", msg:line appear video user"

/*: , data: :*/
fileprivate let str_imageData:[Character] = [","," ","d","a","t","a"]
fileprivate let str_closeValue:String = "status"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let str_backgroundData:String = "textCK"
fileprivate let str_userData:String = "收异equal消息"
fileprivate let str_numberText:String = "if importame:"

/*: "onReceivedGift" :*/
fileprivate let str_languageWithValue:[Character] = ["o","n","R","e","c"]
fileprivate let str_actionValue:String = "eitime"
fileprivate let str_gestureMakeData:[Character] = ["e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let str_bottomWithData:[Character] = ["o","n","V","i","d","e","o","M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let str_roomSucceedTitle:[Character] = ["o","n","R","e","q","u","e","s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let str_userName:String = "load type make whiteonCan"
fileprivate let str_modelData:String = "show"

/*: "onRefuseCall" :*/
fileprivate let str_textPlayerName:[Character] = ["o","n","R","e","f","u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let str_scaleValue:String = "onAccvar shared make make select"

/*: "onStopCall" :*/
fileprivate let str_tapTitle:String = "like makeonSt"

/*: "freeCallTimes" :*/
fileprivate let str_toData:String = "self user viewfreeC"
fileprivate let str_layerName:String = "iviewes"

/*: "mfCoin" :*/
fileprivate let str_makeValue:String = "mfCoinpush content as let make"

/*: "onMissCoin" :*/
fileprivate let str_addBarrelData:String = "ON"
fileprivate let str_toolData:[Character] = ["M","i","s","s","C","o","i","n"]

/*: "acceptCall" :*/
fileprivate let str_viewValue:String = "atitletitlee"

/*: "stopCall" :*/
fileprivate let str_limitTitle:String = "button bottom likestopCal"
fileprivate let str_nameText:String = "now"

/*: "requestCall" :*/
fileprivate let str_customData:String = "REQUES"
fileprivate let str_giftCallValue:[Character] = ["t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let str_makeTitle:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let str_targetPathName:String = "typagee"

/*: "[Invite you to a voice call...]" :*/
fileprivate let str_finishName:[UInt8] = [0x1e,0xc,0x2b,0x33,0x2c,0x31,0x20,0x65,0x3c,0x2a,0x30,0x65,0x31,0x2a,0x65,0x24,0x65,0x33,0x2a,0x2c,0x26,0x20,0x65,0x26,0x24,0x29,0x29,0x6b,0x6b,0x6b,0x18]

private func deviceCustom(pic num: UInt8) -> UInt8 {
    return num ^ 69
}

/*: "uid" :*/
fileprivate let str_dataCellActualName:String = "colord"

/*: "nickname" :*/
fileprivate let str_acceptValue:String = "styleickstyleam"
fileprivate let str_upTitle:String = "E"

/*: "headPic" :*/
fileprivate let str_sizeData:[Character] = ["h"]
fileprivate let str_appText:String = "eadPicpackage style"

/*: "onCallResume" :*/
fileprivate let str_styleName:[Character] = ["o","n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let str_onlineValue:[Character] = ["c"]
fileprivate let str_collectionName:[Character] = ["a","n","c","e","l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let str_indexData:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let str_clickValue:[Character] = ["o","n","C","a","l","l","M","o","t","i","v","a","t","i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let str_extraTitle:String = "true male succeedonLi"
fileprivate let str_addEdgeValue:String = "ninvalue"

/*: "reason" :*/
fileprivate let str_tabData:String = "REASON"

/*: "time" :*/
fileprivate let str_pathText:String = "viewme"

/*: "onStopLive" :*/
fileprivate let str_succeedValue:String = "onStoor return sure key"
fileprivate let str_deviceValue:[Character] = ["p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let str_textData:String = "section share make viewonSen"
fileprivate let str_errorClearValue:[Character] = ["m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let str_groupSessionValue:[Character] = ["o","n","G"]
fileprivate let str_valueName:[Character] = ["a","m","e","F","s"]

/*: "broadStatus" :*/
fileprivate let str_nameViewData:[Character] = ["b","r","o","a"]
fileprivate let str_infoValue:[Character] = ["d","S","t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let str_selectedTitle:String = "true to let backonCa"
fileprivate let str_timeTitle:[Character] = ["l","l","C","h","a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let str_messageTextData:String = "manager equal frame uponNotif"
fileprivate let str_clickData:[Character] = ["i","c","a","t","i","o","n","P","o","p","u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let str_cornerValue:String = "center elseonUs"

/*: "freeCallWin" :*/
fileprivate let str_withLabelTitle:String = "frposition"
fileprivate let str_transformValue:[Character] = ["W","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  JewishCalendarMonthThen.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import UIKit
import UIKit

//: import SocketRocket
import SocketRocket

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let consLet_videoValue = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let consLet_nextValue = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let consLet_statusName = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let consLet_managerAdjustValue = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let consLet_useName = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let consLet_pointData = (String(str_shadowName.suffix(8)) + String(str_detailName.prefix(7)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let consLet_screenText = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let consLet_errorContent = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let consLet_environmentName = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let consLet_failData = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let consLet_scaleData = NSNotification.Name(rawValue: String(bytes: str_quoteName.map{backPhoto(state: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let consLet_transformName = NSNotification.Name(rawValue: String(bytes: str_labelModelData.map{viewCreate(thumb: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol ViewObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func realize(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol QualityMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func sizeBy(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func subjugate(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol LastStableCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func appRecord(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func textTitle(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func addIn(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func read(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol DataJumpPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func chatRe(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func contextOfUse(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func headAdd(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class JewishCalendarMonthThen: NSObject {
    //: enum SocketRetryType: String {
    enum VideoType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ServerLanguageManager.shared.direction == .rightToLeft ? 0 : consLet_errorDeviceHeightData,
        //: y: StatusBarHeight+190
        y: consLet_netViewValue + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ServerLanguageManager.shared.direction == .rightToLeft ? 0 : consLet_errorDeviceHeightData,
        //: y: StatusBarHeight+190
        y: consLet_netViewValue + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: ServerLanguageManager.shared.direction == .rightToLeft ? 0 : consLet_errorDeviceHeightData,
        //: y: StatusBarHeight+500
        y: consLet_netViewValue + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: ViewObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: QualityMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: LastStableCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: DataJumpPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isTalking = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = JewishCalendarMonthThen()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(reachabilityEqual(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(cellDoing),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(str_userStorageValue) + String(str_frameName)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (UtilizationKingfisherCompatible.shared as! UtilizationKingfisherCompatible).destroyTime()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isTalking")
            .observeWeakly(Bool.self, (String(str_exitValue.prefix(5)) + "king"))
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (UtilizationKingfisherCompatible.shared as! UtilizationKingfisherCompatible).destroyTime()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension JewishCalendarMonthThen: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: EqualLogTool.writeDoing(msg: "SOCKET 连接成功.")
        EqualLogTool.writeDoing(msg: (String(str_userValue) + str_positionData.replacingOccurrences(of: "title", with: "功")))
        //: sendPing()
        startMatch()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: consLet_detailDeviceHeightData,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: consLet_pointData)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        positionView()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: EqualLogTool.writeDoing(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        EqualLogTool.writeDoing(msg: (String(str_bottomTitle.prefix(7)) + str_makeGestureTitle.replacingOccurrences(of: "live", with: "接") + ", error:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = IndexStyleAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = IndexStyleAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == consLet_managerAdjustValue {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = prime(isRetry: true)
            //: EqualLogTool.writeDoing(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            EqualLogTool.writeDoing(msg: (String(str_addName)) + "\(consLet_managerAdjustValue)" + (str_succeedName + str_equalValue + "cket域名" + str_pathValue) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.reactionTime(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.subAdd(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: EqualLogTool.writeDoing(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        EqualLogTool.writeDoing(msg: (String(str_centerName) + String(str_giftValue)) + "\(code), reason:\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.afterRecord(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == consLet_failData {
            //: return
            return
        }
        //: if let status = IndexStyleAppManager.share.reachability?.connection, status != .unavailable {
        if let status = IndexStyleAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.reactionTime(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func prime(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = IndexStyleAppManager.share.appConfigMode.ws
        self.linkAddrArr = IndexStyleAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = willLimited()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: consLet_pointData)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = IndexStyleAppManager.share.loginUid
        let uid = IndexStyleAppManager.share.loginUid
        //: let sid = IndexStyleAppManager.share.loginSessionId
        let sid = IndexStyleAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)" + (String(str_withContent.suffix(5)) + "form=i" + String(str_replyMakeViewName.prefix(6)) + "vers" + String(str_buttonSubAddValue.prefix(4))) + "\(consLet_environmentContent)" + (String(str_errorName) + String(str_currentText)) + "\(consLet_packageName)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func willLimited() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return consLet_searchData ? [(String(str_enableValue) + String(str_textValue.prefix(4))) + "\(consLet_netTitle).com:7766"] : [(String(str_detailData))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension JewishCalendarMonthThen {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func reachabilityEqual(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: EqualLogTool.writeDoing(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            EqualLogTool.writeDoing(msg: String(bytes: str_talkValue.map{$0^167}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.afterRecord(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.reactionTime(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func cellDoing() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.reactionTime(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.barJuggle()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension JewishCalendarMonthThen {
    /// socket保活心跳包
    //: private func sendPing() {
    private func startMatch() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: consLet_videoValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.objectInfo(info: ["cmd": "ping"])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func effectError() {
        //: endTalkingPing()
        speed()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: consLet_nextValue, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isTalking || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isTalking || (self.userRead()?.isKind(of: MetronymicThen.self) == false && self.userRead()?.isKind(of: NameManagerViewDelegate.self) == false) && (self.currTalkingVC?.isKind(of: MetronymicThen.self) == false && self.currTalkingVC?.isKind(of: NameManagerViewDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.objectInfo(info: ["cmd": (String(str_extraName.prefix(5)) + "eart" + str_rawTitle.replacingOccurrences(of: "play", with: "b"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func speed() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func barJuggle() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(str_keyBottomValue.replacingOccurrences(of: "manager", with: "r") + str_valueText.replacingOccurrences(of: "call", with: "v") + "edDate")] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < consLet_tabData {
                //: IndexStyleAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                IndexStyleAppManager.share.equalColor(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension JewishCalendarMonthThen {
    /// 开启直播心跳包
    //: func startLivePing() {
    func domainName() {
        //: endLivePing()
        collideWithCurrent()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: consLet_statusName, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.objectInfo(info: ["cmd": (String(str_toRecordRefuseName.suffix(5)) + String(str_appContent) + String(str_videoText))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func collideWithCurrent() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension JewishCalendarMonthThen {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func positionView() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = BeginThen()
        //: let keyWindow = OverdoReactiveCompatible.getWindow()
        let keyWindow = OverdoReactiveCompatible.outMax()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isTalking
        self.containerView?.isHidden = !self.isTalking
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func gildTheLily() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard DecisionMakerReactiveCompatible.barIn().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard EqualSendReactiveCompatible.clout().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension JewishCalendarMonthThen {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func afterRecord(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(str_iconValue)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: consLet_failData, reason: (String(str_currentValue.suffix(2)) + String(str_stylePathData.prefix(5)) + "r log" + String(str_toName.prefix(5))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        speed()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            gildTheLily()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func timeSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = JewishCalendarMonthThen.shared.prime(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.reactionTime(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func reactionTime(timeInterval: TimeInterval, type: VideoType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: EqualLogTool.writeDoing(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            EqualLogTool.writeDoing(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.modelStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func modelStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: EqualLogTool.writeDoing(msg: "SOCKET 连接正常 不需要重新连接.")
                EqualLogTool.writeDoing(msg: (str_responseText.replacingOccurrences(of: "group", with: "S") + String(str_managerData)))
                //: } else {
            } else {
                //: EqualLogTool.writeDoing(msg: "SOCKET 正在连接 不需要重新连接.")
                EqualLogTool.writeDoing(msg: (String(str_labelData) + str_sessionName.capitalized + "需要\u{91cd}新连接."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isTalking {
        if self.isTalking {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending("&resume=1")
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending("&lang=\(TalkingBenderTotalensityRequestThen.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        afterRecord(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = consLet_useName
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: EqualLogTool.writeDoing(msg: "SOCKET 开始连接, 地址：\(newStr).")
        EqualLogTool.writeDoing(msg: (String(str_addTitle) + String(str_makeName)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func objectInfo(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if IndexStyleAppManager.share.reachability?.connection == nil || IndexStyleAppManager.share.reachability?.connection == .unavailable {
        if IndexStyleAppManager.share.reachability?.connection == nil || IndexStyleAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = ["cmd": (String(str_keyName.prefix(4)) + String(str_verticalName.prefix(9))),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       "errno": "\(consLet_environmentName)",
                                       //: "data": [:]]
                                       "data": [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            subAdd(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info!["cmd"] as? String == "ping" {
                //: EqualLogTool.writeDoing(msg: "SOCKET 连接保活cmd: ping.")
                EqualLogTool.writeDoing(msg: (str_skinLanguageData.replacingOccurrences(of: "butterfly", with: "T") + " 连接保活c" + String(str_handleName.suffix(4)) + String(str_serverTitle.prefix(5))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info!["cmd"] as? String == (String(str_extraName.prefix(5)) + "eart" + str_rawTitle.replacingOccurrences(of: "play", with: "b")) {
                //: EqualLogTool.writeDoing(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                EqualLogTool.writeDoing(msg: String(bytes: str_dataEqualContent.map{viewModel(free: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info!["cmd"] as? String == (String(str_toRecordRefuseName.suffix(5)) + String(str_appContent) + String(str_videoText)) {
                //: EqualLogTool.writeDoing(msg: "SOCKET live保活cmd: liveHeartBeat.")
                EqualLogTool.writeDoing(msg: String(bytes: str_fillName.map{colorResume(background: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info!["cmd"] as? String == "ping" {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = ["cmd": "onP",
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           "errno": "\(consLet_screenText)",
                                           //: "data": [:]]
                                           "data": [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                subAdd(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.reactionTime(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func subAdd(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json["cmd"].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json["errno"].intValue
        //: let msg = json["msg"].stringValue
        let msg = json["msg"].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json["data"].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(str_oneValue) + String(str_logMapData.prefix(3))) + "\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        same(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: EqualLogTool.writeDoing(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            EqualLogTool.writeDoing(msg: (str_backgroundData.replacingOccurrences(of: "text", with: "SO") + "ET \u{63a5}" + str_userData.replacingOccurrences(of: "equal", with: "常") + "：cmdN" + String(str_numberText.suffix(4))) + "\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: IndexStyleAppManager.share.loginUserMode.freeCallTimes = 0
                IndexStyleAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !IndexStyleAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !IndexStyleAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                QuantityWindowManager.shared.sectionIntervalimate()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.realize(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(str_languageWithValue) + str_actionValue.replacingOccurrences(of: "time", with: "v") + String(str_gestureMakeData)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: consLet_needErrorScreenData, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(str_bottomWithData)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.sizeBy(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(str_roomSucceedTitle)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.subjugate(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(str_userName.suffix(5)) + "celCal" + str_modelData.replacingOccurrences(of: "show", with: "l")) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(str_textPlayerName)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(str_scaleValue.prefix(5)) + "eptCall") { // 接收方发送acceptCall回执
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            effectError()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.contextOfUse(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(str_tapTitle.suffix(4)) + "opCall") { // 接收方发送stopCall回执
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            speed()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(str_toData.suffix(5)) + "allT" + str_layerName.replacingOccurrences(of: "view", with: "m"))] as? Int {
                //: IndexStyleAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                IndexStyleAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: IndexStyleAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            IndexStyleAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: consLet_roomData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (UtilizationKingfisherCompatible.shared as! UtilizationKingfisherCompatible).anArrayOffline()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (str_addBarrelData.lowercased() + String(str_toolData)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.read(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (str_viewValue.replacingOccurrences(of: "title", with: "c") + "ptCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            effectError()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.appRecord(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(str_limitTitle.suffix(7)) + str_nameText.replacingOccurrences(of: "now", with: "l")) { // 接收方/拨打方 挂断了通话
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            speed()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.textTitle(data: data)
            // 更新余额
            //: IndexStyleAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            IndexStyleAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            contact(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: consLet_roomData, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (UtilizationKingfisherCompatible.shared as! UtilizationKingfisherCompatible).anArrayOffline()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (str_customData.lowercased() + String(str_giftCallValue)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: consLet_scaleData, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: IndexStyleAppManager.share.recieve1v1TalkRequest(info: data)
                IndexStyleAppManager.share.equalColor(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(str_keyBottomValue.replacingOccurrences(of: "manager", with: "r") + str_valueText.replacingOccurrences(of: "call", with: "v") + "edDate")] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: str_makeTitle.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json["data"]["type"].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: str_finishName.map{deviceCustom(pic: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                EveryLastBaseDelegate.userUrl(uid: String(json["data"]["uid"].intValue),
                                          //: title: data["nickname"] as? String,
                                          title: data[(str_acceptValue.replacingOccurrences(of: "style", with: "n") + str_upTitle.lowercased())] as? String,
                                          //: body: body,
                                          body: body,
                                          //: imageUrl: data["headPic"] as? String)
                                          imageUrl: data["headPic"] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                EveryLastBaseDelegate.shared.cellBgm()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(str_styleName)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isTalking = true
            self.isTalking = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            effectError()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(str_styleName))] = "1"
            //: IndexStyleAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            IndexStyleAppManager.share.courtInFormatting(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (UtilizationKingfisherCompatible.shared as! UtilizationKingfisherCompatible).destroyTime()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(str_onlineValue) + String(str_collectionName)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            speed()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.addIn(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                EveryLastBaseDelegate.gestureAppear(identifier: String(json["data"]["uid"].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                EveryLastBaseDelegate.shared.country()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(str_indexData)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isTalking = false
            self.isTalking = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            speed()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.chatRe(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(str_clickValue)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if IndexStyleAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.headAdd?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(str_extraTitle.suffix(4)) + "veWar" + str_addEdgeValue.replacingOccurrences(of: "value", with: "g")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            DecisionMakerReactiveCompatible.needTime(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(str_succeedValue.prefix(5)) + String(str_deviceValue)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DecisionMakerReactiveCompatible.barIn().aboveLimit()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(str_textData.suffix(5)) + "dProgra" + String(str_errorClearValue)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.scaleMsg(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(str_groupSessionValue) + String(str_valueName)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(str_nameViewData) + String(str_infoValue))] as? Int ?? -1
            //: if IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: consLet_transformName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: consLet_needErrorScreenData, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: consLet_transformName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(str_selectedTitle.suffix(4)) + String(str_timeTitle)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            LabelThen.beyondMini().fillUp(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(str_messageTextData.suffix(7)) + String(str_clickData)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            QuantityWindowManager.shared.imageDic(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(str_cornerValue.suffix(4)) + "erBlock") { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DecisionMakerReactiveCompatible.barIn().aboveLimit()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            EqualSendReactiveCompatible.clout().sinceShared()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: consLet_askData, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension JewishCalendarMonthThen {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func same(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (str_customData.lowercased() + String(str_giftCallValue)) || cmdName == (String(str_roomSucceedTitle)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if DecisionMakerReactiveCompatible.barIn().isLive == true {
                //: NEED_RESTORE_LIVE = true
                consLet_messageData = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                DecisionMakerReactiveCompatible.upCompletion(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                DecisionMakerReactiveCompatible.barIn().aboveLimit()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard consLet_messageData == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(str_onlineValue) + String(str_collectionName)) || cmdName == (String(str_userName.suffix(5)) + "celCal" + str_modelData.replacingOccurrences(of: "show", with: "l")) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(str_indexData)) || cmdName == (String(str_textPlayerName)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(str_limitTitle.suffix(7)) + str_nameText.replacingOccurrences(of: "now", with: "l")) || cmdName == (String(str_tapTitle.suffix(4)) + "opCall")
        {
            //: NEED_RESTORE_LIVE = false
            consLet_messageData = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = GiftAlertView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                DecisionMakerReactiveCompatible.barIn().equalLive()
            }
            //: alert.show()
            alert.tokenShow()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func contact(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(str_toData.suffix(5)) + "allT" + str_layerName.replacingOccurrences(of: "view", with: "m"))] as? Int {
            //: IndexStyleAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            IndexStyleAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(str_withLabelTitle.replacingOccurrences(of: "position", with: "ee") + "Call" + String(str_transformValue))] as? Bool
        //: if freeCallWin == true && IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ForceUpwardsTruncatedReactiveCompatible()
            //: view.show()
            view.totalGesture()
            //: view.setShowData(data: data)
            view.pigeonholeToRawData(data: data)
        }
    }
}
