
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_toTitle:[UInt8] = [0x1a,0x1d,0x1a,0x7,0x5b,0x10,0x1c,0x17,0x16,0x1,0x49,0x5a,0x53,0x1b,0x12,0x0,0x53,0x1d,0x1c,0x7,0x53,0x11,0x16,0x16,0x1d,0x53,0x1a,0x1e,0x3,0x1f,0x16,0x1e,0x16,0x1d,0x7,0x16,0x17]

private func criminalRecord(user num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "uid" :*/
fileprivate let str_cellText:[UInt8] = [0x7f,0x63,0x6e]

/*: "headPic" :*/
fileprivate let str_shareContent:String = "headPicmodel translate text view"

/*: "nickname" :*/
fileprivate let str_wrapMessageData:[Character] = ["n"]
fileprivate let str_withBarName:[Character] = ["i","c","k","n","a","m","e"]

/*: "birthday" :*/
fileprivate let str_fromText:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let str_sizeData:[Character] = ["v","i"]
fileprivate let str_ofValue:[Character] = ["d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let str_locationData:String = "lolittle"
fileprivate let str_toData:[Character] = ["a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let str_aName:[UInt8] = [0xa0,0xae,0xa7]

private func dataColor(view num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "refuseCall" :*/
fileprivate let str_dataTopName:String = "pushefus"
fileprivate let str_leadingName:[Character] = ["e","C","a","l","l"]

/*: "data" :*/
fileprivate let str_enableComponentValue:[UInt8] = [0x3c,0x39,0x2c,0x39]

/*: "Call interruption" :*/
fileprivate let str_valueName:String = "Callinput cloud make"
fileprivate let str_leadingData:String = "ERRUPTION"

/*: "stopCall" :*/
fileprivate let str_ofText:String = "stopCalappear self view type info"
fileprivate let str_equalViewCustomValue:String = "device"

/*: "logId" :*/
fileprivate let str_colorTitle:[UInt8] = [0x81,0x84,0x7c,0x5e,0x79]

fileprivate func tableOfTitles(begin num: UInt8) -> UInt8 {
    let value = Int(num) + 235
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_barWantContent:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let str_borderTagEmptyData:[Character] = ["N","o"," ","a","n","s","w","e"]
fileprivate let str_modelData:String = "app"

/*: "duration" :*/
fileprivate let str_observeText:String = "durviewion"

/*: "She has hung up" :*/
fileprivate let str_exampleData:String = "She haif equal path hidden text"
fileprivate let str_labName:[Character] = [" ","u","p"]

/*: "cancelSex" :*/
fileprivate let str_textLabelData:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let str_frameMakeContent:String = "equal content varHe ha"
fileprivate let str_currentData:[Character] = ["n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let str_objectValue:[Character] = ["e","n","d","T","y","p"]
fileprivate let str_toolData:[Character] = ["e"]

/*: "stopSex" :*/
fileprivate let str_downContent:String = "stopSexapp guard play bag lab"

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_labTitle:[UInt8] = [0x64,0x65,0x64,0x6e,0x65,0x20,0x73,0x61,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x2c,0x74,0x69,0x64,0x65,0x72,0x63,0x20,0x74,0x6e,0x65,0x69,0x63,0x69,0x66,0x66,0x75,0x73,0x6e,0x49]

/*: "The other hung up the phone" :*/
fileprivate let str_calculationStateInValue:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"]
fileprivate let str_keyName:[Character] = [" ","u","p"," ","t","h"]
fileprivate let str_valueText:String = "room mode height officiale phone"

/*: "type" :*/
fileprivate let str_drawingText:String = "tlabpe"

/*: "jumpUrl" :*/
fileprivate let str_imageModelText:String = "collection letjumpU"
fileprivate let str_centerContent:String = "normall"

/*: "Cancel" :*/
fileprivate let str_nameContent:String = "c"
fileprivate let str_makeCosTitle:String = "anccelll"

/*: "Purchase" :*/
fileprivate let str_resultPointLetName:[Character] = ["P","u","r","c","h","a","s","e"]

/*: "&type=4" :*/
fileprivate let str_playerData:[Character] = ["&"]
fileprivate let str_sourceTitle:[Character] = ["t","y","p","e","=","4"]

/*: "addBean" :*/
fileprivate let str_roomValue:String = "addBeacorner in mini model"
fileprivate let str_actionTitle:[Character] = ["n"]

/*: "refuseSex" :*/
fileprivate let str_keyData:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let str_collectionText:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let str_leadingRecordingName:String = "ACCEPT"
fileprivate let str_textNetManagerData:String = "bottom male frameCall"

/*: "Calls are cancelled" :*/
fileprivate let str_userText:String = "Calls let date view let var"
fileprivate let str_intervalValue:[Character] = ["c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let str_atData:String = "female imageThe ca"
fileprivate let str_inviteTableName:[Character] = [" ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NameManagerViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let consLet_barName = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class NameManagerViewDelegate: ServiceRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: PastChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: AllThen? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: StartMiniReactiveCompatible? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        JewishCalendarMonthThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        JewishCalendarMonthThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        JewishCalendarMonthThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_toTitle.map{criminalRecord(user: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! NameModelNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.grandmaster()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            untilOfAuth()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.sizeCamera()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        album()
        //: setupSubViewsConstraint()
        releaseTask()
        //: initNotificationObserver()
        resume()
        //: req_callGetUserInfo()
        dubBy()
        //: timeoutRules()
        statusRules()
        //: resumeTalk()
        sharedTalk()
        //: setUpBGMPlayer()
        beginDismiss()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if IndexStyleAppManager.share.appConfigMode.disableShootScreen {
            if IndexStyleAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = LabelReactiveCompatible(frame: CGRect(x: 0, y: 0, width: consLet_errorDeviceHeightData, height: consLet_sessionName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        JewishCalendarMonthThen.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.noModel()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: ResourceTotaleractionView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ResourceTotaleractionView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: IndexChatManager = {
        //: let m = TalkingVoiceChatManager()
        let m = IndexChatManager()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension NameManagerViewDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func dubBy() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AddCloudDelegate.rawData(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json["uid"].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json["headPic"].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(str_wrapMessageData) + String(str_withBarName))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(str_fromText))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(str_sizeData) + String(str_ofValue))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_locationData.replacingOccurrences(of: "little", with: "c") + String(str_toData))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.clickInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension NameManagerViewDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func untilOfAuth() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        KeyPermissionTool.awakeEqual(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.noblesseExtra(showMsg: consLet_busySoundData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_dataTopName.replacingOccurrences(of: "push", with: "r") + String(str_leadingName)), "data": ["uid": self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.nameAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.appIn()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = AllThen.modelType(type: .Response)
            //: self.startBGM()
            self.grandmaster()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func noModel() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.appIn()
        //: TalkingSocketManager.shared.callerDelegate = nil
        JewishCalendarMonthThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        JewishCalendarMonthThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        JewishCalendarMonthThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func nameAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.appIn()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func colorQuantityercommunicate() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.isColor(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func sharedTalk() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            colorQuantityercommunicate()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension NameManagerViewDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func resume() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(field(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(iconPlay(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: consLet_detailDeviceHeightData,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        consLet_barName.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func field(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            nameAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func iconPlay(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard JewishCalendarMonthThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        JewishCalendarMonthThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        JewishCalendarMonthThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        JewishCalendarMonthThen.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.accept(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension NameManagerViewDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.noblesseExtra(showMsg: (String(str_valueName.prefix(4)) + " int" + str_leadingData.lowercased()).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_ofText.prefix(7)) + str_equalViewCustomValue.replacingOccurrences(of: "device", with: "l")), "data": ["logId": self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_barWantContent)), "data": ["logId": self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_dataTopName.replacingOccurrences(of: "push", with: "r") + String(str_leadingName)), "data": ["uid": self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.replacement()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                JewishCalendarMonthThen.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            nameAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.ballup(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension NameManagerViewDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func beginDismiss() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? BackgroundColumnConvertible.Call : BackgroundColumnConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = AllThen.modelType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func grandmaster() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.convert()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func appIn() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.anyPush()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension NameManagerViewDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func statusRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? consLet_restoreName : self.chatModel?.stickTogetherFromInstance()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            pathAction()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.pathAction()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func pathAction() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !JewishCalendarMonthThen.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.noblesseExtra(showMsg: (String(str_borderTagEmptyData) + str_modelData.replacingOccurrences(of: "app", with: "r")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = ["cmd": (String(str_barWantContent)), "data": ["logId": self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = ["cmd": (str_dataTopName.replacingOccurrences(of: "push", with: "r") + String(str_leadingName)), "data": ["uid": self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        JewishCalendarMonthThen.shared.objectInfo(info: socketDict)
        //: popCurrentViewController()
        nameAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension NameManagerViewDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func upwardVoice(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json["logId"].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_observeText.replacingOccurrences(of: "view", with: "at"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.modelImage(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.remoteIn()
            //: createTalkRoom()
            colorQuantityercommunicate()
            //: self.stopBGM()
            self.appIn()
        }
    }
}

// MARK: - LastStableCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension NameManagerViewDelegate: LastStableCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func addIn(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_exampleData.prefix(6)) + "s hung" + String(str_labName)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_textLabelData))].stringValue == ColorExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_frameMakeContent.suffix(5)) + "s hu" + String(str_currentData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.noblesseExtra(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.replacement()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        nameAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func appRecord(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json["logId"].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.appIn()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.remoteIn()
        //: self.createTalkRoom()
        self.colorQuantityercommunicate()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func textTitle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json["endType"].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_exampleData.prefix(6)) + "s hung" + String(str_labName)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json["stopSex"].stringValue == ColorExpressionConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_frameMakeContent.suffix(5)) + "s hu" + String(str_currentData)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_labTitle.reversed(), encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_calculationStateInValue) + String(str_keyName) + String(str_valueText.suffix(7))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.noblesseExtra(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.replacement()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.pushSlow()
        //: self.popCurrentViewController()
        self.nameAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: consLet_roomData, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func read(msg: String, data: [String: Any]) {
        //: guard IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data["type"] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data["jumpUrl"] as? String else { return }
            //: let httpsUrl = IndexStyleAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = IndexStyleAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = FailConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: PostPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            PostPushManager.share.gildTheLilyCount(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            EqualAlertShow.showConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: "Cancel".localized,
                                        //: rightBtnTitle: "Purchase".localized) {
                                        rightBtnTitle: (String(str_resultPointLetName)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                EqualAlertShow.postBegin()
                //: } rightBlock: {
            } rightBlock: {
                //: PostPushManager.share.func__pushToRechargeAlert(appendParams: "&type=4")
                PostPushManager.share.everyReason(appendParams: "&type=4")
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func headAdd(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json["addBean"].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.atPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.animaConstraint(point: addBean)
        }
    }
}

// MARK: - DataJumpPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension NameManagerViewDelegate: DataJumpPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chatRe(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_exampleData.prefix(6)) + "s hung" + String(str_labName)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_keyData))].stringValue == ColorExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_frameMakeContent.suffix(5)) + "s hu" + String(str_currentData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.noblesseExtra(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.replacement()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        nameAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func contextOfUse(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json["logId"].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.appIn()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.remoteIn()
        //: self.createTalkRoom()
        self.colorQuantityercommunicate()
    }
}

// MARK: - ViewObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension NameManagerViewDelegate: ViewObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func realize(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_dataTopName.replacingOccurrences(of: "push", with: "r") + String(str_leadingName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.noblesseExtra(showMsg: (String(str_borderTagEmptyData) + str_modelData.replacingOccurrences(of: "app", with: "r")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.replacement()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                JewishCalendarMonthThen.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.nameAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_collectionText)) && errorNo == 101 { // 接听通话时，余额不足
            //: PostPushManager.share.func__pushToRechargeAlert(appendParams: "&type=4")
            PostPushManager.share.everyReason(appendParams: "&type=4")

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_collectionText)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.noblesseExtra(showMsg: msg)
            //: self.popCurrentViewController()
            self.nameAnimated()
        }
    }
}

// MARK: - ShowObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension NameManagerViewDelegate: ShowObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func tableOfContentsNo() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_leadingRecordingName.lowercased() + String(str_textNetManagerData.suffix(4))), "data": ["uid": self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func viewCall() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.noblesseExtra(showMsg: (String(str_userText.prefix(6)) + "are " + String(str_intervalValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_barWantContent)), "data": ["logId": self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.noblesseExtra(showMsg: (String(str_userText.prefix(6)) + "are " + String(str_intervalValue)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_dataTopName.replacingOccurrences(of: "push", with: "r") + String(str_leadingName)), "data": ["uid": self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.noblesseExtra(showMsg: (String(str_atData.suffix(6)) + "ll has" + String(str_inviteTableName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_ofText.prefix(7)) + str_equalViewCustomValue.replacingOccurrences(of: "device", with: "l")), "data": ["logId": self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.pushSlow()
        //: self.popCurrentViewController()
        self.nameAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func controlVideo(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.itemObserverBar(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func imageText(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.ensconceMomentCollection(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func whenPlay() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        JewishCalendarMonthThen.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = StartMiniReactiveCompatible.scope()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.userRead()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.doingTime()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.elflike(talkingTime)
        //: popCurrentViewController()
        nameAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func pathQuantityeraction(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.elflike(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func switchside(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.pointOfDepartureArr(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension NameManagerViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func album() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func releaseTask() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
