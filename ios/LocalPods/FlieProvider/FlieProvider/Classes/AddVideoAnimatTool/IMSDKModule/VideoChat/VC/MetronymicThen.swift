
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_actionName:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let str_labTitle:[UInt8] = [0xf4,0xe8,0xe5]

private func markSegment(extra num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "headPic" :*/
fileprivate let str_textValue:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let str_sizeMakeValue:String = "errorickerrorame"

/*: "birthday" :*/
fileprivate let str_labValue:String = "birthdastyle"

/*: "videoUrl" :*/
fileprivate let str_textName:String = "vcomponentd"
fileprivate let str_makeManagerTitle:String = "block view equal price tempeoUrl"

/*: "cover" :*/
fileprivate let str_contentText:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let str_cellDeleteName:String = "normalcati"
fileprivate let str_liveDataValue:String = "oshared"

/*: "cmd" :*/
fileprivate let str_backValue:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let str_firstCollectionValue:String = "titlefu"
fileprivate let str_againstKeyData:String = "of"

/*: "data" :*/
fileprivate let str_rawValue:[UInt8] = [0x48,0x4d,0x58,0x4d]

private func timeShow(make num: UInt8) -> UInt8 {
    return num ^ 44
}

/*: "Call interruption" :*/
fileprivate let str_labelData:String = "image equal letCa"
fileprivate let str_giftName:String = "ll itrue reply app call"
fileprivate let str_toData:String = "ptiokit"

/*: "stopCall" :*/
fileprivate let str_maleData:[Character] = ["s"]
fileprivate let str_playerTitle:String = "return texttopCall"

/*: "logId" :*/
fileprivate let str_liveTitle:[UInt8] = [0x3d,0x40,0x38,0x1a,0x35]

fileprivate func modelShareProgress(record num: UInt8) -> UInt8 {
    let value = Int(num) + 47
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let str_pathSizeValue:String = "cmessagenc"

/*: "No answer" :*/
fileprivate let str_byData:String = "info trueNo"
fileprivate let str_userPlayName:String = "service to answer"

/*: "duration" :*/
fileprivate let str_ofData:String = "duratmore"
fileprivate let str_infoTitle:[Character] = ["n"]

/*: "She has hung up" :*/
fileprivate let str_columnName:String = "She haplay leading as"
fileprivate let str_transformName:[Character] = ["p"]

/*: "cancelSex" :*/
fileprivate let str_centerTitle:String = "succeed data enable heightcancelSex"

/*: "He has hung up" :*/
fileprivate let str_profileLabelAllowTitle:String = "He hlet content load file"
fileprivate let str_dataTitle:String = "ung uptask scope black name self"

/*: "endType" :*/
fileprivate let str_accountName:String = "scale"
fileprivate let str_priorityName:[Character] = ["n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let str_shareValueName:String = "manager endstopSex"

/*: "Insufficient credit, call has ended" :*/
fileprivate let str_frameText:[UInt8] = [0x35,0x12,0xf,0x9,0x1a,0x1a,0x15,0x1f,0x15,0x19,0x12,0x8,0x5c,0x1f,0xe,0x19,0x18,0x15,0x8,0x50,0x5c,0x1f,0x1d,0x10,0x10,0x5c,0x14,0x1d,0xf,0x5c,0x19,0x12,0x18,0x19,0x18]

private func universalUnder(name num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "The other hung up the phone" :*/
fileprivate let str_maxData:String = "The othfield stage height status progress"
fileprivate let str_dataFileText:String = "size you height componenter hu"
fileprivate let str_normalData:String = "view leading area selfthe p"

/*: "type" :*/
fileprivate let str_valueViewFrameData:String = "observerype"

/*: "jumpUrl" :*/
fileprivate let str_textLocalData:String = "jumpUrto make top"
fileprivate let str_arrayEqualName:[Character] = ["l"]

/*: "Cancel" :*/
fileprivate let str_modeData:String = "Cancelheight cell"

/*: "Purchase" :*/
fileprivate let str_countData:[Character] = ["P"]
fileprivate let str_bringWhiteValue:String = "urchasattach"

/*: "&type=3" :*/
fileprivate let str_viewData:String = "&type=3self remove list self"

/*: "addBean" :*/
fileprivate let str_labelValue:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let str_frameName:[Character] = ["r","e","f","u"]
fileprivate let str_itemValue:String = "if label ifseSex"

/*: "onAcceptCall" :*/
fileprivate let str_viewName:String = "frame view empty self selfonAcc"
fileprivate let str_modelName:String = "eptCallif near image"

/*: "acceptCall" :*/
fileprivate let str_limitPageValue:String = "acellcellept"

/*: "Calls are cancelled" :*/
fileprivate let str_reasonName:String = "Calls artap data"
fileprivate let str_pointAddValue:String = "e caattribute make height"

/*: "The call has ended" :*/
fileprivate let str_bottomNameData:String = "view image shadowThe c"
fileprivate let str_halfModelValue:String = "has in value self self"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MetronymicThen.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let consLet_userData = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class MetronymicThen: ServiceRecognizerDelegate {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: PastChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: AllThen? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: NameBlockReactiveCompatible? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: ParameterThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        JewishCalendarMonthThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        JewishCalendarMonthThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        JewishCalendarMonthThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_actionName.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! NameModelNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.callBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            canvass()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            equalCell(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.enableline()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        imageTap()
        //: setupSubViewsConstraint()
        sound()
        //: initNotificationObserver()
        tractHouseShow()
        //: req_callGetUserInfo()
        playList()
        //: timeoutRules()
        watchful()
        //: setUpBGMPlayer()
        upwardOf()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.endCollection()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if IndexStyleAppManager.share.appConfigMode.disableShootScreen {
            if IndexStyleAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = LabelReactiveCompatible(frame: CGRect(x: 0, y: 0, width: consLet_errorDeviceHeightData, height: consLet_sessionName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isTalking = false
        JewishCalendarMonthThen.shared.isTalking = false
        //: self.stopVCAllActivties()
        self.bruteLoad()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: RecountView = {
        //: let v = TalkingBGVideoView()
        let v = RecountView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.phosphoresce(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: PrizeView = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = PrizeView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: StyleThen = {
        //: let v = TalkingVideoWindowView()
        let v = StyleThen()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = consLet_errorDeviceHeightData - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if ServerLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: consLet_netViewValue + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.scintillationView()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AddCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = AddCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension MetronymicThen {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func playList() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AddCloudDelegate.rawData(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json["uid"].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json["headPic"].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(str_sizeMakeValue.replacingOccurrences(of: "error", with: "n"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(str_labValue.replacingOccurrences(of: "style", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(str_textName.replacingOccurrences(of: "component", with: "i") + String(str_makeManagerTitle.suffix(5)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json["cover"].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(str_cellDeleteName.replacingOccurrences(of: "normal", with: "lo") + str_liveDataValue.replacingOccurrences(of: "shared", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.targetInfo()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.forefront()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func forefront() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = ColorHalfCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = ColorHalfCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.nameUp(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.aboveView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension MetronymicThen {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func canvass() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        KeyPermissionTool.searchAndMicrophoneCell { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.noblesseExtra(showMsg: consLet_busySoundData)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_firstCollectionValue.replacingOccurrences(of: "title", with: "re") + "seCa" + str_againstKeyData.replacingOccurrences(of: "of", with: "ll")), "data": ["uid": self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.vaultAnimated()
                //: return
                return
            }

            //: self.startPreview()
            self.endCollection()
            //: self.stopBGM()
            self.gestureBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = AllThen.modelType(type: .Response)
            //: self.startBGM()
            self.callBgm()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func endCollection() {
        //: self.videoManager.startPreview()
        self.videoManager.answerDelayGetMoving()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func bruteLoad() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.gestureBgm()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        LabelThen.accomplish()
        //: TalkingSocketManager.shared.callerDelegate = nil
        JewishCalendarMonthThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        JewishCalendarMonthThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        JewishCalendarMonthThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func vaultAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.gestureBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func scintillationView() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.combineEnter()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.forefront()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func modelTalk() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.dismissPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.forefront()
        // 展示投诉说明飘屏（男性）
//        self.show_videoFloatScreenAnimation()
    }

    /// 展示投诉说明飘屏（男性）
//    private func show_videoFloatScreenAnimation() {
//        guard reportView == nil else { return }
//
//        reportView = ParameterThen()
//        view.addSubview(reportView!)
//        reportView?.show_animation()
//    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension MetronymicThen {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func tractHouseShow() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(playingCard(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(province(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: consLet_maxName,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(say(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: consLet_detailDeviceHeightData,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        consLet_userData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func playingCard(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            vaultAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func say(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard JewishCalendarMonthThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        JewishCalendarMonthThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        JewishCalendarMonthThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        JewishCalendarMonthThen.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.disconnected()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func province(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.forefront()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension MetronymicThen: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.noblesseExtra(showMsg: (String(str_labelData.suffix(2)) + String(str_giftName.prefix(4)) + "nterru" + str_toData.replacingOccurrences(of: "kit", with: "n")))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_maleData) + String(str_playerTitle.suffix(7))), "data": ["logId": self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_pathSizeValue.replacingOccurrences(of: "message", with: "a") + "elCall"), "data": ["logId": self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_firstCollectionValue.replacingOccurrences(of: "title", with: "re") + "seCa" + str_againstKeyData.replacingOccurrences(of: "of", with: "ll")), "data": ["uid": self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.replacement()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                JewishCalendarMonthThen.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            vaultAnimated()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension MetronymicThen {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func upwardOf() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? BackgroundColumnConvertible.Call : BackgroundColumnConvertible.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = AllThen.modelType(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func callBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.convert()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func gestureBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.anyPush()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension MetronymicThen {
    /// 超时定时器
    //: private func timeoutRules() {
    private func watchful() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? consLet_restoreName : self.chatModel?.stickTogetherFromInstance()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            digitizerAction()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.digitizerAction()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func digitizerAction() {
        //: guard !TalkingSocketManager.shared.isTalking else { return }
        guard !JewishCalendarMonthThen.shared.isTalking else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.noblesseExtra(showMsg: (String(str_byData.suffix(2)) + String(str_userPlayName.suffix(7))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = ["cmd": (str_pathSizeValue.replacingOccurrences(of: "message", with: "a") + "elCall"), "data": ["logId": self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = ["cmd": (str_firstCollectionValue.replacingOccurrences(of: "title", with: "re") + "seCa" + str_againstKeyData.replacingOccurrences(of: "of", with: "ll")), "data": ["uid": self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        JewishCalendarMonthThen.shared.objectInfo(info: socketDict)
        //: popCurrentViewController()
        vaultAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension MetronymicThen {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func metadata(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json["logId"].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(str_ofData.replacingOccurrences(of: "more", with: "io") + String(str_infoTitle))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.caseEnable(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.allPlay()
            //: createTalkRoom()
            modelTalk()
            //: self.stopBGM()
            self.gestureBgm()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func equalCell(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.combineEnter()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.phosphoresce(show: false)
        //: self.stopBGM()
        self.gestureBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.allPlay()
        //: self.createTalkRoom()
        self.modelTalk()
    }
}

// MARK: - LastStableCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension MetronymicThen: LastStableCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func addIn(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_columnName.prefix(6)) + "s hung u" + String(str_transformName)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_centerTitle.suffix(9)))].stringValue == ColorExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_profileLabelAllowTitle.prefix(4)) + "as h" + String(str_dataTitle.prefix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.noblesseExtra(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.replacement()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        vaultAnimated()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func appRecord(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        equalCell(logId: json["logId"].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func textTitle(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json["endType"].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(str_columnName.prefix(6)) + "s hung u" + String(str_transformName)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json["stopSex"].stringValue == ColorExpressionConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(str_profileLabelAllowTitle.prefix(4)) + "as h" + String(str_dataTitle.prefix(6))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue {
                //: typeStr = "Insufficient credit, call has ended".localized
                typeStr = String(bytes: str_frameText.map{universalUnder(name: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(str_maxData.prefix(7)) + String(str_dataFileText.suffix(5)) + "ng up " + String(str_normalData.suffix(5)) + "hone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.noblesseExtra(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.replacement()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.largess()
        //: popCurrentViewController()
        vaultAnimated()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func read(msg: String, data: [String: Any]) {
        //: guard IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data["type"] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data["jumpUrl"] as? String else { return }
            //: let httpsUrl = IndexStyleAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = IndexStyleAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = FailConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: PostPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            PostPushManager.share.gildTheLilyCount(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            EqualAlertShow.showConfig(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: "Cancel".localized,
                                        //: rightBtnTitle: "Purchase".localized) {
                                        rightBtnTitle: (String(str_countData) + str_bringWhiteValue.replacingOccurrences(of: "attach", with: "e")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                EqualAlertShow.postBegin()
                //: } rightBlock: {
            } rightBlock: {
                //: PostPushManager.share.func__pushToRechargeAlert(appendParams: "&type=3")
                PostPushManager.share.everyReason(appendParams: "&type=3")
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func catchEqual(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.viewDoing(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func headAdd(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json["addBean"].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.radiograph(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.occurrent(point: addBean)
        }
    }
}

// MARK: - DataJumpPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension MetronymicThen: DataJumpPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func chatRe(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(str_columnName.prefix(6)) + "s hung u" + String(str_transformName)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(str_frameName) + String(str_itemValue.suffix(5)))].stringValue == ColorExpressionConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(str_profileLabelAllowTitle.prefix(4)) + "as h" + String(str_dataTitle.prefix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.noblesseExtra(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.replacement()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            JewishCalendarMonthThen.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        vaultAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func contextOfUse(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        equalCell(logId: json["logId"].intValue)
    }
}

// MARK: - ViewObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension MetronymicThen: ViewObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func realize(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (str_firstCollectionValue.replacingOccurrences(of: "title", with: "re") + "seCa" + str_againstKeyData.replacingOccurrences(of: "of", with: "ll")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.noblesseExtra(showMsg: (String(str_byData.suffix(2)) + String(str_userPlayName.suffix(7))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.replacement()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                JewishCalendarMonthThen.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            vaultAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(str_viewName.suffix(5)) + String(str_modelName.prefix(7))) && errorNo == 101 { // 接听通话时，余额不足
            //: PostPushManager.share.func__pushToRechargeAlert(appendParams: "&type=3")
            PostPushManager.share.everyReason(appendParams: "&type=3")

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(str_viewName.suffix(5)) + String(str_modelName.prefix(7))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.noblesseExtra(showMsg: msg)
            //: popCurrentViewController()
            vaultAnimated()
        }
    }
}

// MARK: - ColorObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension MetronymicThen: ColorObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func tableThan() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_limitPageValue.replacingOccurrences(of: "cell", with: "c") + "Call"), "data": ["uid": self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func doButtonContent() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.noblesseExtra(showMsg: (String(str_reasonName.prefix(8)) + String(str_pointAddValue.prefix(4)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_pathSizeValue.replacingOccurrences(of: "message", with: "a") + "elCall"), "data": ["logId": self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.noblesseExtra(showMsg: (String(str_reasonName.prefix(8)) + String(str_pointAddValue.prefix(4)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (str_firstCollectionValue.replacingOccurrences(of: "title", with: "re") + "seCa" + str_againstKeyData.replacingOccurrences(of: "of", with: "ll")), "data": ["uid": self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.noblesseExtra(showMsg: (String(str_bottomNameData.suffix(5)) + "all " + String(str_halfModelValue.prefix(4)) + "ended").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            JewishCalendarMonthThen.shared.objectInfo(info: ["cmd": (String(str_maleData) + String(str_playerTitle.suffix(7))), "data": ["logId": self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        vaultAnimated()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func rangeFront(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.frontAcross(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func perspective(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.phosphoresce(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.clickView(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.forefront()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func reverence() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        JewishCalendarMonthThen.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = NameBlockReactiveCompatible.videoColorView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.photoShow()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.reflectorize()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.userRead()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        vaultAnimated()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func takeOn() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension MetronymicThen {
    /// 添加视图
    //: private func setupSubviews() {
    private func imageTap() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.bag(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func sound() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
