
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let str_modelValue:[UInt8] = [0x2e,0x33,0x2e,0x39,0xed,0x28,0x34,0x29,0x2a,0x37,0xff,0xee,0xe5,0x2d,0x26,0x38,0xe5,0x33,0x34,0x39,0xe5,0x27,0x2a,0x2a,0x33,0xe5,0x2e,0x32,0x35,0x31,0x2a,0x32,0x2a,0x33,0x39,0x2a,0x29]

fileprivate func pageSizeCenter(frame num: UInt8) -> UInt8 {
    let value = Int(num) + 59
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "F5F5F5" :*/
fileprivate let str_firstMapContent:String = "labellabellabel"

/*: "TalkingChatTextMsgCellID" :*/
fileprivate let str_viewValue:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","e","x","t","M","s","g","C","e","l","l"]
fileprivate let str_stopValue:[Character] = ["I","D"]

/*: "TalkingChatImageMsgCellID" :*/
fileprivate let str_layerData:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","I","m","a","g","e","M","s","g","C","e"]
fileprivate let str_onValue:String = "off model type selfllID"

/*: "TalkingChatVideoMsgCellID" :*/
fileprivate let str_listName:String = "Taview equal make"
fileprivate let str_topFitTextName:[Character] = ["l","k","i","n","g","C","h","a","t","V","i","d","e","o","M","s","g","C","e","l","l","I","D"]

/*: "TalkingChatGiftMsgCellID" :*/
fileprivate let str_managerTitle:String = "model model image self managerTalki"
fileprivate let str_backgroundValue:[Character] = ["G","i","f"]
fileprivate let str_sendName:[Character] = ["t","M","s","g","C","e","l","l","I","D"]

/*: "HeaderReactiveCompatible" :*/
fileprivate let str_interactionData:String = "Talkitoward guard center if"
fileprivate let str_userImageName:[Character] = ["n","g","C","h","a","t","A","s","k","F"]
fileprivate let str_cornerData:[Character] = ["o","r","G","i","f","t","M","s","g","C","e","l","l"]

/*: "TalkingChatTipsMsgCellID" :*/
fileprivate let str_hiddenName:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","T","i","p","s","M","s","g","C","e"]
fileprivate let str_sourceData:String = "llIDarray true menu push"

/*: "TalkingChatMatchMsgCellID" :*/
fileprivate let str_screenName:String = "kit path none itemTalkin"
fileprivate let str_callData:String = "gChatMview back height visual"
fileprivate let str_removeData:[Character] = ["g","C","e","l","l","I","D"]

/*: "TalkingChatAudioMsgCellID" :*/
fileprivate let str_statusCellTitle:[Character] = ["T","a","l","k","i","n","g","C","h","a","t","A","u","d","i","o","M","s","g","C","e"]
fileprivate let str_priorityText:[Character] = ["l","l","I","D"]

/*: "TalkingChatSystemNotifJumpMsgCellID" :*/
fileprivate let str_insideValue:[UInt8] = [0x6a,0x77,0x82,0x81,0x7f,0x84,0x7d,0x59,0x7e,0x77,0x8a,0x69,0x8f,0x89,0x8a,0x7b,0x83,0x64,0x85,0x8a,0x7f,0x7c,0x60,0x8b,0x83,0x86,0x63,0x89,0x7d,0x59,0x7b,0x82,0x82,0x5f,0x5a]

fileprivate func sexDelete(layer num: UInt8) -> UInt8 {
    let value = Int(num) + 234
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TSystemMessageCell_ReuseId" :*/
fileprivate let str_requestData:String = "TSysframe if processor collection size"
fileprivate let str_frameValue:String = "temMfalse type"
fileprivate let str_colorSectionActionName:String = "geCelreturn mic self make add"

/*: "TalkingChatMapMsgCellID" :*/
fileprivate let str_askName:String = "var make score table callTalki"
fileprivate let str_pathMakeText:String = "path to gifttMapMs"
fileprivate let str_imageName:[Character] = ["g","C","e","l","l","I","D"]

/*: "#128CFF" :*/
fileprivate let str_titleData:String = "ad bottom pic if#128CF"
fileprivate let str_tableText:String = "color"

/*: "Feedback" :*/
fileprivate let str_tabCurrentCustomText:String = "name path textFeedb"
fileprivate let str_homeName:String = "agiftk"

/*: "#8370FF" :*/
fileprivate let str_viewData:[Character] = ["#","8","3","7","0","F","F"]

/*: "Quick Greeting" :*/
fileprivate let str_buttonValue:String = "data detailQuick Gre"
fileprivate let str_textTitle:String = "eticolor"

/*: "Beauty Photos" :*/
fileprivate let str_eventData:[Character] = ["B","e","a"]
fileprivate let str_selectedData:String = "tip case self change ifuty P"

/*: "url" :*/
fileprivate let str_statusSharedText:String = "forcel"

/*: "unlockGift" :*/
fileprivate let str_renderValue:String = "true fromunlockG"
fileprivate let str_equalValue:String = "userft"

/*: "%d" :*/
fileprivate let str_listValue:String = "%delse add"

/*: "isBurn" :*/
fileprivate let str_viewText:[UInt8] = [0xa8,0xb2,0x83,0xb4,0xb3,0xaf]

/*: "giftId" :*/
fileprivate let str_emptyData:String = "giftIdtool equal background let"

/*: "rYMsgType" :*/
fileprivate let str_colorData:String = "name title view stringrYM"
fileprivate let str_resultFirstData:[Character] = ["s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let str_intimateToText:[Character] = ["G","J",":","C","a","l","l","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let str_dataValue:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "extra" :*/
fileprivate let str_atData:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let str_resourceContent:[Character] = ["m","s","g","I","n","f","o"]

/*: "toUid" :*/
fileprivate let str_momentData:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let str_videoValue:String = "wantGiftany to icon type"

/*: "jumpKey" :*/
fileprivate let str_enableData:[Character] = ["j","u","m","p","K","e"]
fileprivate let str_tailTitle:String = "session"

/*: "truePersonAuth" :*/
fileprivate let str_buttonData:String = "linerue"
fileprivate let str_infoName:String = "timage"

/*: "logType" :*/
fileprivate let str_sharedData:[Character] = ["l","o","g","T","y"]
fileprivate let str_networkName:String = "ptime"

/*: "msgType" :*/
fileprivate let str_onlineData:[UInt8] = [0x3d,0x23,0x37,0x4,0x29,0x20,0x35]

/*: "txt" :*/
fileprivate let str_finishData:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let str_showName:String = "typimage"

/*: "QuickMsg" :*/
fileprivate let str_progressValue:String = "Quickinteraction hidden in on zone"
fileprivate let str_nameValue:String = "data self equal inMsg"

/*: "params" :*/
fileprivate let str_popCollectName:[UInt8] = [0x3d,0x2c,0x3f,0x2c,0x20,0x3e]

private func blackAndWhite(line num: UInt8) -> UInt8 {
    return num ^ 77
}

/*: "content" :*/
fileprivate let str_countValue:[UInt8] = [0x6e,0x62,0x63,0x79,0x68,0x63,0x79]

/*: "id" :*/
fileprivate let str_textName:String = "effectd"

/*: "txtId" :*/
fileprivate let str_callEqualData:[Character] = ["t","x","t","I","d"]

/*: "message" :*/
fileprivate let str_skinTitle:[UInt8] = [0x60,0x68,0x7e,0x7e,0x6c,0x6a,0x68]

private func easyLay(model num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "TxtMsg" :*/
fileprivate let str_cancelTitle:String = "user modelTxtMsg"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let str_pathTitle:[UInt8] = [0x8e,0xb7,0xb1,0xbb,0xbd,0xf8,0xb0,0xbd,0xb4,0xb4,0xb7,0xf8,0xbc,0xb7,0xaf,0xb6,0xb4,0xb7,0xb9,0xbc,0xf8,0xbe,0xb9,0xb1,0xb4,0xbd,0xbc,0xf6,0xf8,0x88,0xaa,0xb7,0xbb,0xbd,0xbc,0xad,0xaa,0xbd]

private func valueColor(fatal num: UInt8) -> UInt8 {
    return num ^ 216
}

/*: "length" :*/
fileprivate let str_searchValue:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let str_lineVideoName:String = "audtoo"

/*: "audioId" :*/
fileprivate let str_styleName:String = "to self pathaudioId"

/*: "audioLength" :*/
fileprivate let str_tapAssetTitle:[UInt8] = [0x32,0x46,0x35,0x3a,0x40,0x1d,0x36,0x3f,0x38,0x45,0x39]

fileprivate func modelReaction(lab num: UInt8) -> UInt8 {
    let value = Int(num) + 47
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let str_pushTitle:[Character] = ["a","u","d","i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let str_imageLengthValue:[UInt8] = [0xe2,0xf4,0xff,0xf5,0xf4,0xe3,0xd8,0xf5]

/*: "AudioMsg" :*/
fileprivate let str_cornerName:String = "name array viewAudioM"
fileprivate let str_borderValue:String = "SG"

/*: "Can't send blank message" :*/
fileprivate let str_sharedSizeText:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m"]
fileprivate let str_equalData:String = "essaerrore"

/*: "quoteMsgId" :*/
fileprivate let str_modelName:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let str_nearTitle:String = "kit app equal varquoteM"
fileprivate let str_contentName:String = "FO"

/*: "contentType" :*/
fileprivate let str_connectionData:[UInt8] = [0xc2,0xce,0xcf,0xd5,0xc4,0xcf,0xd5,0xf5,0xd8,0xd1,0xc4]

private func blackPathEdit(sign num: UInt8) -> UInt8 {
    return num ^ 161
}

/*: "ImgMsg" :*/
fileprivate let str_moreData:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let str_playerData:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let str_reactionData:[UInt8] = [0x1c,0x18,0x14,0x12,0x10,0x20,0x7,0x1c]

private func viewUp(allow num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "customSend" :*/
fileprivate let str_deviceTitle:[UInt8] = [0x45,0x53,0x55,0x52,0x49,0x4b,0x75,0x43,0x48,0x42]

private func viewGift(table num: UInt8) -> UInt8 {
    return num ^ 38
}

/*: "pic" :*/
fileprivate let str_kindData:String = "pinputc"

/*: "VideoMsg" :*/
fileprivate let str_toName:[Character] = ["V","i","d"]
fileprivate let str_topHalfName:String = "true guard data height hiddeneoMsg"

/*: "video" :*/
fileprivate let str_imageTitle:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let str_cellName:[UInt8] = [0x16,0x1a,0x3,0x10,0x7,0x3c,0x18,0x12]

private func openLineLast(manager num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "videoUrl" :*/
fileprivate let str_addCornerContent:[UInt8] = [0x54,0x4b,0x46,0x47,0x4d,0x77,0x50,0x4e]

private func appRecord(make num: UInt8) -> UInt8 {
    return num ^ 34
}

/*: "duration" :*/
fileprivate let str_userChildScriptText:[UInt8] = [0x29,0x38,0x3f,0x2c,0x39,0x24,0x22,0x23]

private func dailyColor(view num: UInt8) -> UInt8 {
    return num ^ 77
}

/*: "len" :*/
fileprivate let str_contentValue:[UInt8] = [0xe6,0xef,0xe4]

private func viewPublic(succeed num: UInt8) -> UInt8 {
    return num ^ 138
}

/*: "map" :*/
fileprivate let str_responseBlockName:String = "MAP"

/*: "latlon" :*/
fileprivate let str_addData:[UInt8] = [0xb7,0xba,0xaf,0xb7,0xb4,0xb5]

private func pathMake(button num: UInt8) -> UInt8 {
    return num ^ 219
}

/*: "messageType" :*/
fileprivate let str_toText:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "MapMsg" :*/
fileprivate let str_showCellData:String = "height repeatMapMs"
fileprivate let str_sharedHiddenContent:[Character] = ["g"]

/*: "Documents/User/voice/" :*/
fileprivate let str_backName:String = "head message image var clearDocume"
fileprivate let str_labValue:String = "more"
fileprivate let str_displayValue:[Character] = ["/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let str_userValue:String = "instance session makeDocume"
fileprivate let str_bottomTableText:String = "nts/user make string view"
fileprivate let str_upData:String = "to cell moreecord/"

/*: "msgIncome" :*/
fileprivate let str_pointValue:[Character] = ["m","s","g","I","n","c","o","m"]
fileprivate let str_leadingData:String = "E"

/*: "mfBean" :*/
fileprivate let str_gestureName:[UInt8] = [0x3,0xfc,0xd8,0xfb,0xf7,0x4]

fileprivate func travelKit(define num: UInt8) -> UInt8 {
    let value = Int(num) + 106
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "WantGift" :*/
fileprivate let str_lineName:String = "Wantwindow since let"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let str_originTitle:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t","T","i","p","s","T","i"]
fileprivate let str_fromValue:[Character] = ["t","l","e","_"]

/*: "msgId" :*/
fileprivate let str_sizeValue:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let str_fileText:[Character] = ["t","o","t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let str_appName:[Character] = ["a","t","t","a","c","h","S","y","s","M"]
fileprivate let str_cellValue:String = "sfilter"

/*: "intimatePhoto" :*/
fileprivate let str_awakeContent:String = "intervaltim"
fileprivate let str_hideTitle:[Character] = ["h","o","t","o"]

/*: "&type= :*/
fileprivate let str_succeedValueText:[Character] = ["&","t","y","p","e","="]

/*: _ :*/
fileprivate let str_segmentValue:String = "_"

/*: "totalIntimate" :*/
fileprivate let str_giftTitle:[Character] = ["t","o","t","a"]
fileprivate let str_genderInfoValue:[Character] = ["l","I","n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let str_groupData:[UInt8] = [0x72,0x6e,0x63]

private func dataConverter(location num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "intimate" :*/
fileprivate let str_targetName:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let str_endValue:[UInt8] = [0x52,0x70,0x7f,0x36,0x65,0x31,0x64,0x62,0x74,0x31,0x65,0x79,0x74,0x31,0x77,0x64,0x7f,0x72,0x65,0x78,0x7e,0x7f,0x31,0x75,0x64,0x74,0x31,0x65,0x7e,0x31,0x68,0x7e,0x64,0x63,0x31,0x7d,0x70,0x72,0x7a,0x31,0x7e,0x77,0x31,0x78,0x7f,0x65,0x78,0x7c,0x70,0x72,0x68,0x31,0x7d,0x74,0x67,0x74,0x7d,0x3f]

private func relicClose(lab num: UInt8) -> UInt8 {
    return num ^ 17
}

/*: "giftNum" :*/
fileprivate let str_languageContent:String = "origin onlinegiftN"
fileprivate let str_subModelValue:[Character] = ["u","m"]

/*: "pkgItemsetId" :*/
fileprivate let str_imageValue:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let str_voiceActionText:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let str_failureName:String = "wantGiftcolor view"
fileprivate let str_modelTitle:String = "party label to position centerId"

/*: "WantGiftMsg" :*/
fileprivate let str_giftValue:[Character] = ["W","a","n","t","G","i","f"]
fileprivate let str_frameContent:[Character] = ["t","M","s","g"]

/*: "gift" :*/
fileprivate let str_saveAttentionValue:String = "gnormalt"

/*: "View info" :*/
fileprivate let str_tableValue:[Character] = ["V"]
fileprivate let str_clickValue:[Character] = ["i","e","w"," ","i","n","f","o"]

/*: "Notes" :*/
fileprivate let str_selectedValue:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let str_appTagValue:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let str_playExploreTitle:String = "no value icon labView"
fileprivate let str_kitManagerName:String = "mode mini data true rece"
fileprivate let str_hiddenTitleName:[Character] = ["i","p","t","s","?"]

/*: "He" :*/
fileprivate let str_youTitle:String = "Hemedium center status add"

/*: "She" :*/
fileprivate let str_appData:String = "Sheself in"

/*: "%@ is using too low a version" :*/
fileprivate let str_callColorValue:String = "input target medium%@ is"
fileprivate let str_centerViewData:String = "each make conversation stringng to"
fileprivate let str_transformData:String = "to in extension view if a ve"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let str_addColorForValue:String = "for task gameShoo"
fileprivate let str_buttonName:String = "deo atmodel to"
fileprivate let str_photoValue:String = " leasspeed data"
fileprivate let str_shText:String = "emakends"

/*: "Upload failed" :*/
fileprivate let str_awakeExtraValue:[Character] = ["U","p","l","o","a"]
fileprivate let str_liveContent:String = "d faprivate data m for back"

/*: "播放状态: :*/
fileprivate let str_labContent:String = "播放add:"

/*: "c2c_" :*/
fileprivate let str_labelTitle:String = "else data blockc2c_"

/*: "msg" :*/
fileprivate let str_roomValue:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let str_topText:String = "normalps"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let str_emptyValue:[UInt8] = [0xa0,0x96,0x8c,0xd9,0x9a,0x98,0x97,0xd9,0x8c,0x8a,0x9c,0xd9,0x8d,0x91,0x9c,0xd9,0x8f,0x90,0x9d,0x9c,0x96,0xd9,0x9a,0x91,0x98,0x8d,0xd9,0x9f,0x9c,0x98,0x8d,0x8c,0x8b,0x9c,0xd9,0x8d,0x96,0xd9,0x9e,0x90,0x8f,0x9c,0xd9,0xdc,0xb9,0xd9,0x98,0xd9,0x9a,0x98,0x95,0x95]

private func givenText(error num: UInt8) -> UInt8 {
    return num ^ 249
}

/*: "cellData" :*/
fileprivate let str_addArrayName:String = "range typecellData"

/*: "System notification" :*/
fileprivate let str_bottomRoomData:String = "else all fieldSyst"
fileprivate let str_indexValue:[Character] = ["o","t","i","f","i","c","a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CountensityViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import UIKit
import UIKit

//: import AVFAudio
import AVFAudio

//: import Photos
import Photos

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum ModelDeviceFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class CountensityViewController: ServiceRecognizerDelegate {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = UserHandyJSON()
    private var userWrap = UserHandyJSON() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = ModelDeviceFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: UserHandyJSON = UserHandyJSON()) {
    init(chatID: String, isFrom: ModelDeviceFromEnum = .Normal, userWrap: UserHandyJSON = UserHandyJSON()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        CountensityViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        WaitressThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == OverdoReactiveCompatible.getXiaoMiID() {
        if targetId == OverdoReactiveCompatible.winImage() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == OverdoReactiveCompatible.getCustomerServiceID() {
        } else if targetId == OverdoReactiveCompatible.use() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: str_modelValue.map{pageSizeCenter(frame: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if IndexStyleAppManager.share.appConfigMode.disableShootScreen {
            if IndexStyleAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = LabelReactiveCompatible(frame: CGRect(x: 0, y: 0, width: consLet_errorDeviceHeightData, height: consLet_sessionName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        page()
        //: createUI()
        addEvent()
        //: createUIConstraints()
        asLevel()
        //: bindInteraction()
        upSumeraction()
        //: addTapGestureRecognizer()
        currentView()
        //: func__requestChatInfo()
        priceInfo()
        //: func__getFillDraftText()
        draft()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        PrizeObjectProtocol.shared.addWithDelegate()
        //: func__addVoiceNotification()
        funcNotification()
        //: addReadPeceiptNotification()
        peceiptEqual()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            valueTo()
            //: func__TipsNotification()
            gildTheLilyNotification()
            //: if IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue {
                //: func__setReplyTipsView()
                methodAdd()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        moveAdd(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        moveAdd(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            generate()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TUIMessageController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TUIMessageController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: "TalkingChatTextMsgCellID")
        msgVC.tableView.register(CommenceMsgCell.self, forCellReuseIdentifier: (String(str_viewValue) + String(str_stopValue)))
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: "TalkingChatImageMsgCellID")
        msgVC.tableView.register(VideoViewCell.self, forCellReuseIdentifier: (String(str_layerData) + String(str_onValue.suffix(4))))
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: "TalkingChatVideoMsgCellID")
        msgVC.tableView.register(ContextValueThen.self, forCellReuseIdentifier: (String(str_listName.prefix(2)) + String(str_topFitTextName)))
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatGiftMsgCellID")
        msgVC.tableView.register(DeviceFileReactiveCompatible.self, forCellReuseIdentifier: (String(str_managerTitle.suffix(5)) + "ngChat" + String(str_backgroundValue) + String(str_sendName)))
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: "TalkingChatAskForGiftMsgCell")
        msgVC.tableView.register(HeaderReactiveCompatible.self, forCellReuseIdentifier: (String(str_interactionData.prefix(5)) + String(str_userImageName) + String(str_cornerData)))
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: "TalkingChatTipsMsgCellID")
        msgVC.tableView.register(PrizeMsgCell.self, forCellReuseIdentifier: (String(str_hiddenName) + String(str_sourceData.prefix(4))))
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: "TalkingChatMatchMsgCellID")
        msgVC.tableView.register(MatchReactiveCompatible.self, forCellReuseIdentifier: (String(str_screenName.suffix(6)) + String(str_callData.prefix(6)) + "atchMs" + String(str_removeData)))
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: "TalkingChatAudioMsgCellID")
        msgVC.tableView.register(StableRoomThen.self, forCellReuseIdentifier: (String(str_statusCellTitle) + String(str_priorityText)))
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
        msgVC.tableView.register(SkiJumpThen.self, forCellReuseIdentifier: String(bytes: str_insideValue.map{sexDelete(layer: $0)}, encoding: .utf8)!)
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: "TSystemMessageCell_ReuseId")
        msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: (String(str_requestData.prefix(4)) + String(str_frameValue.prefix(4)) + "essa" + String(str_colorSectionActionName.prefix(5)) + "l_ReuseId"))

        //: msgVC.tableView.register(TalkingChatMapMsgCell.self, forCellReuseIdentifier: "TalkingChatMapMsgCellID")
        msgVC.tableView.register(ExpressViewCell.self, forCellReuseIdentifier: (String(str_askName.suffix(5)) + "ngCha" + String(str_pathMakeText.suffix(6)) + String(str_imageName)))

        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: StochasticProcessMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = StochasticProcessMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: OnlyReactiveCompatible = {
        //: var type: InputViewType = .PrivateChat
        var type: QualityViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = OnlyReactiveCompatible(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.loadEnable(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.loadEnable(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(str_tabCurrentCustomText.suffix(5)) + str_homeName.replacingOccurrences(of: "gift", with: "c")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(disoblige), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: CountAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = CountAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: HeadingReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = HeadingReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.loadEnable(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.loadEnable(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(str_buttonValue.suffix(9)) + str_textTitle.replacingOccurrences(of: "color", with: "ng")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.labelSumervalMedium().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(eventClick), for: .touchUpInside)
        //: btn.isHidden = !IndexStyleAppManager.share.appUserConfigMode.showDirectGreet || IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue || IndexStyleAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !IndexStyleAppManager.share.appUserConfigMode.showDirectGreet || IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue || IndexStyleAppManager.share.appStatus == FailHashable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: AtFileThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = AtFileThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: TitleSightThen = {
        //: let v = TalkingSelectTranslationView()
        let v = TitleSightThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.empty(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: LightReactiveCompatible = {
        //: let v = TalkingTranslationResultView()
        let v = LightReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.divert(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.loadEnable(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.loadEnable(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(str_eventData) + String(str_selectedData.suffix(5)) + "hotos").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.labelSumervalMedium().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(attractiveness), for: .touchUpInside)
        //: btn.isHidden = IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue || IndexStyleAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue || IndexStyleAppManager.share.appStatus == FailHashable.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: DeviceViewDelegate = {
        //: let v = TalkingLiveRoomGamesView()
        let v = DeviceViewDelegate()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: EventToReactiveCompatible = {
        //: let view = TalkingPrivateBannerView.init()
        let view = EventToReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: TextReactiveCompatible = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = TextReactiveCompatible()
        //: v.isHidden = (IndexStyleAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (IndexStyleAppManager.share.appStatus == FailHashable.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: MakeReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = MakeReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.upAwake()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension CountensityViewController: ResourceReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func push(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.example(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension CountensityViewController: TMessageControllerDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func eventClick() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        WaitressThen.todayRequest(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.noblesseExtra(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.dataDict(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func attractiveness() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        WaitressThen.fireside(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = CeremonyViewDelegate()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().cling(urlStr: dict?["url"] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(str_renderValue.suffix(7)) + str_equalValue.replacingOccurrences(of: "user", with: "i"))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ExpressDataJsonModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.confinement(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
    func messageController(_: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> TUIMessageCellData! {
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = TUISocialChatManager.handleTXReceiveMessageModel(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: ViewCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ViewCellData.self), self.userWrap.uid.count > 0 {
            //: cellData.msgModel.userInfoModel = self.userWrap
            cellData.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(str_colorData.suffix(3)) + String(str_resultFirstData))].stringValue == (String(str_intimateToText)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(str_dataValue))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                // 更正消息展示方向
                //: let msgModel = cellData.msgModel
                let msgModel = cellData.msgModel
                //: cellData = BackgroundLabelCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = BackgroundLabelCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData.msgModel = msgModel
                cellData.msgModel = msgModel
            }
        }
        //: if cellData.isKind(of: ClearCellData.self) == true || cellData.isKind(of: TalkingManagerCollectionCellData.self) == true || cellData.isKind(of: BackgroundLabelCellData.self) == true || cellData.isKind(of: TalkingCellData.self) == true || cellData.isKind(of: HiddenCellData.self) == true {
        if cellData.isKind(of: ClearCellData.self) == true || cellData.isKind(of: TalkingManagerCollectionCellData.self) == true || cellData.isKind(of: BackgroundLabelCellData.self) == true || cellData.isKind(of: TalkingCellData.self) == true || cellData.isKind(of: HiddenCellData.self) == true {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
        //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
        WaitressThen.direction(cellData: cellData, msgView: self.msgView)

        // 女性记录最新回复时间, 刷新表格
        //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
        WaitressThen.roomFlush(cellData.msgIncome, data.timestamp, msgView)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
    func messageController(_: TUIMessageController!, onShowMessageData data: TUIMessageCellData!) -> TUIMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = asButton(data: data)

        //: if data.isKind(of: BackgroundLabelCellData.self) {
        if data.isKind(of: BackgroundLabelCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTextMsgCellID")
            let cell = CommenceMsgCell(style: .default, reuseIdentifier: (String(str_viewValue) + String(str_stopValue)))
            //: let cellData = data as! BackgroundLabelCellData
            let cellData = data as! BackgroundLabelCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.key() {
                //: if cellData.msgModel.quoteMsgInfo.uid == IndexStyleAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == IndexStyleAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = IndexStyleAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = IndexStyleAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingManagerCollectionCellData.self) {
        if data.isKind(of: TalkingManagerCollectionCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: "TalkingChatImageMsgCellID")
            let cell = VideoViewCell(style: .default, reuseIdentifier: (String(str_layerData) + String(str_onValue.suffix(4))))
            //: let cellData = data as! TalkingManagerCollectionCellData
            let cellData = data as! TalkingManagerCollectionCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingCellData.self) {
        if data.isKind(of: TalkingCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: "TalkingChatVideoMsgCellID")
            let cell = ContextValueThen(style: .default, reuseIdentifier: (String(str_listName.prefix(2)) + String(str_topFitTextName)))
            //: let cellData = data as! TalkingCellData
            let cellData = data as! TalkingCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: CellData.self) {
        if data.isKind(of: CellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatGiftMsgCellID")
            let cell = DeviceFileReactiveCompatible(style: .default, reuseIdentifier: (String(str_managerTitle.suffix(5)) + "ngChat" + String(str_backgroundValue) + String(str_sendName)))
            //: let cellData = data as! CellData
            let cellData = data as! CellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: GrantCellData.self) {
        if data.isKind(of: GrantCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAskForGiftMsgCell")
            let cell = HeaderReactiveCompatible(style: .default, reuseIdentifier: (String(str_interactionData.prefix(5)) + String(str_userImageName) + String(str_cornerData)))
            //: let cellData = data as! GrantCellData
            let cellData = data as! GrantCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = AwardModelType()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.moveDivert(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(str_videoValue.prefix(8))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: ClearCellData.self) {
        if data.isKind(of: ClearCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: "TalkingChatAudioMsgCellID")
            let cell = StableRoomThen(style: .default, reuseIdentifier: (String(str_statusCellTitle) + String(str_priorityText)))
            //: let cellData = data as! ClearCellData
            let cellData = data as! ClearCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ViewCellData.self) {
        if data.isKind(of: ViewCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMatchMsgCellID")
            let cell = MatchReactiveCompatible(style: .default, reuseIdentifier: (String(str_screenName.suffix(6)) + String(str_callData.prefix(6)) + "atchMs" + String(str_removeData)))
            //: let cellData = data as! ViewCellData
            let cellData = data as! ViewCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: AddCellData.self) {
        if data.isKind(of: AddCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: "TalkingChatTipsMsgCellID")
            let cell = PrizeMsgCell(style: .default, reuseIdentifier: (String(str_hiddenName) + String(str_sourceData.prefix(4))))
            //: let cellData = data as! AddCellData
            let cellData = data as! AddCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: StepSequenceThen.self) {
        if data.isKind(of: StepSequenceThen.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: "TalkingChatSystemNotifJumpMsgCellID")
            let cell = SkiJumpThen(style: .default, reuseIdentifier: String(bytes: str_insideValue.map{sexDelete(layer: $0)}, encoding: .utf8)!)
            //: let cellData = data as! StepSequenceThen
            let cellData = data as! StepSequenceThen
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: HiddenCellData.self) {
        if data.isKind(of: HiddenCellData.self) {
            //: let cell = TalkingChatMapMsgCell.init(style: .default, reuseIdentifier: "TalkingChatMapMsgCellID")
            let cell = ExpressViewCell(style: .default, reuseIdentifier: (String(str_askName.suffix(5)) + "ngCha" + String(str_pathMakeText.suffix(6)) + String(str_imageName)))
            //: let cellData = data as! HiddenCellData
            let cellData = data as! HiddenCellData
            //: cell.fill(with: cellData)
            cell.fill(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageAvatar cell: TUIMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != OverdoReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != OverdoReactiveCompatible.use() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: PostPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            PostPushManager.share.miniskirt(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onSelectMessageContent cell: TUIMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: VideoViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! TalkingManagerCollectionCellData
            let imageCellData = cell.messageData as! TalkingManagerCollectionCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                //: let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                let needAlert = imageCellData.direction == .MsgDirectionIncoming && imageCellData.msgModel.msgInfo.intimatePhoto.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: true, needAlert: needAlert)
                let num = WaitressThen.titleAlert(isImg: true, needAlert: needAlert)
                /// 需要解锁的照片
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                let vc = ManagerPreviewVc(imageCellData: imageCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 1)
                        self.lessShared(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: consLet_postValue, type: 1)
                        //: return
                        return
                    }
                    //: IndexStyleAppManager.share.loginUserMode.updateResidualNum(type: 1, num: max(0, num - 1))
                    IndexStyleAppManager.share.loginUserMode.hitVariant(type: 1, num: max(0, num - 1))
//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                uphold()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(consLet_callTitle)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = MakeModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = TextTransformable()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [MakeModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = DevicePhotosVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                uphold()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ContextValueThen.self) { // 视频
            //: let videoCellData = cell.messageData as! TalkingCellData
            let videoCellData = cell.messageData as! TalkingCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = JewishCalendarMonthViewController(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                uphold()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                //: let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                let needAlert = videoCellData.direction == .MsgDirectionIncoming && videoCellData.msgModel.msgInfo.video.lockStatus == 1
                //: let num = TalkingPrivateChatManager.checkCanPreview(isImg: false, needAlert: needAlert)
                let num = WaitressThen.titleAlert(isImg: false, needAlert: needAlert)
                //: if num == 0 && needAlert {
                if num == 0 && needAlert {
                    //: return
                    return
                }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                let vc = KeyObserverPreviewVc(videoCellData: videoCellData, toUid: self.targetId, preNum: num)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, _, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton, type: 2)
                        self.lessShared(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: consLet_postValue, type: 2)
                        //: return
                        return
                    }
                    //: IndexStyleAppManager.share.loginUserMode.updateResidualNum(type: 2, num: max(0, num - 1))
                    IndexStyleAppManager.share.loginUserMode.hitVariant(type: 2, num: max(0, num - 1))

//                    self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
//                    let dataDict = result as! Dictionary<String, Any>
//                    self.unlockPhotoOrVideoTool(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                uphold()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: StableRoomThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! StableRoomThen
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! ClearCellData, cellView: audioCell)
            modelWithAll(cellData: audioCell.cellData as! ClearCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: CommenceMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! CommenceMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic["extra"] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains("jumpKey") {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (str_buttonData.replacingOccurrences(of: "line", with: "t") + "PersonAu" + str_infoName.replacingOccurrences(of: "image", with: "h")) {
                    //: if IndexStyleAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if IndexStyleAppManager.share.loginUserMode.isTPAuth == QualityNameLiteral.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if IndexStyleAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if IndexStyleAppManager.share.loginUserMode.isTPAuth == QualityNameLiteral.unknown.rawValue {
                        //: PostPushManager.share.func__pushUserVerifyController(toast: nil)
                        PostPushManager.share.loadToast(toast: nil)
                        //: } else if IndexStyleAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if IndexStyleAppManager.share.loginUserMode.isTPAuth == QualityNameLiteral.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = StyleViewController()
                        //: PostPushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        PostPushManager.share.overDoinglineVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: PostPushManager.share.func__pushUserVerifyController(toast: nil)
                        PostPushManager.share.loadToast(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AllTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PostPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PostPushManager.share.titleModel(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(str_colorData.suffix(3)) + String(str_resultFirstData))] as? String == (String(str_intimateToText)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson["msgInfo"]["logType"].intValue == 1 {
                    //: checkAndCallVoice()
                    conversationCell()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    targetMake()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: PrizeMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! PrizeMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = TUIMsgChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic["extra"] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains("jumpKey") {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<AllTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: PostPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    PostPushManager.share.titleModel(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: MatchReactiveCompatible.self) {
            //: PostPushManager.share.func__pushToUserDetailVC(uid: targetId)
            PostPushManager.share.miniskirt(uid: targetId)
            //: }else if cell.isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: ExpressViewCell.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! ExpressViewCell
            //: let mapCellData = mapCell.cellData as! HiddenCellData
            let mapCellData = mapCell.cellData as! HiddenCellData
            //: self.func__showLocationView(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: (mapCellData.direction == .MsgDirectionOutgoing))
            self.far(latlon: mapCellData.msgModel.msgInfo.latlon, lookSelf: mapCellData.direction == .MsgDirectionOutgoing)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
    func messageController(_: TUIMessageController!, onRetryMessageContent cell: TUIMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: MakeCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: VideoViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! VideoViewCell
            //: cellData = imageCell.cellData as! TalkingManagerCollectionCellData
            cellData = imageCell.cellData as! TalkingManagerCollectionCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: ContextValueThen.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! ContextValueThen
            //: cellData = videoCell.cellData as! TalkingCellData
            cellData = videoCell.cellData as! TalkingCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: StableRoomThen.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! StableRoomThen
            //: cellData = audioCell.cellData as! ClearCellData
            cellData = audioCell.cellData as! ClearCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: CommenceMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! CommenceMsgCell
            //: cellData = textCell.cellData as! BackgroundLabelCellData
            cellData = textCell.cellData as! BackgroundLabelCellData
            //: } else if cell .isKind(of: TalkingChatMapMsgCell.self) {
        } else if cell.isKind(of: ExpressViewCell.self) {
            //: let mapCell = cell as! TalkingChatMapMsgCell
            let mapCell = cell as! ExpressViewCell
            //: cellData = mapCell.cellData as! HiddenCellData
            cellData = mapCell.cellData as! HiddenCellData
        }

        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            let reCellData: TUIMessageCellData? = TUISocialChatManager.handleTXMessageReSendModel(with: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: TalkingCellData.self) {
                if reCellData!.isKind(of: TalkingCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    profile(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    fieldMake(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onReplyMessage cellData: TUIMessageCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = WaitressThen.alongCurrentId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
    func messageController(_: TUIMessageController!, onClickReplyMessage cellData: TUIMessageCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        WaitressThen.handleNameReference(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: TUIMessageCellData) -> TUIMessageCellData {
    private func asButton(data: TUIMessageCellData) -> TUIMessageCellData {
        //: let isSelf = data.msgModel.user.uid == Int(IndexStyleAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(IndexStyleAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if IndexStyleAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if IndexStyleAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = IndexStyleAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = IndexStyleAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = IndexStyleAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = IndexStyleAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func upMsg() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = ["msgType": "txt"]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = ["type": (String(str_progressValue.prefix(5)) + String(str_nameValue.suffix(3)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = ["msgInfo": msgInfo, "params": paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
        self.fieldMake(msgData: cellData, reSend: false)
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func dataDict(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?["content"]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = ["msgInfo": msgInfo, "params": paramsDic]
            //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.fieldMake(msgData: cellData, reSend: false)
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            GiftExpressReactiveCompatible.shared.sinceLabel(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.noblesseExtra(showMsg: String(bytes: str_pathTitle.map{valueColor(fatal: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        WaitressThen.month(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?["length"]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = ["msgType": "audio",
                               //: "audioId": greetId,
                               "audioId": greetId,
                               //: "audioLength": length,
                               String(bytes: str_tapAssetTitle.map{modelReaction(lab: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(str_pushTitle)): model.unZipFilePath,
                               //: "senderId": IndexStyleAppManager.share.loginUserMode.userID]
                               String(bytes: str_imageLengthValue.map{$0^145}, encoding: .utf8)!: IndexStyleAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = ["audioId": greetId,
                                 //: "audioLength": length,
                                 String(bytes: str_tapAssetTitle.map{modelReaction(lab: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(str_pushTitle)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 "type": (String(str_cornerName.suffix(6)) + str_borderValue.lowercased())]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = ["msgInfo": msgInfo, "params": paramsDic]
                //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.fieldMake(msgData: cellData, reSend: false)
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func divert(msgStr: String, quoteModel: DeviceJsonModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            noblesseExtra(showMsg: (String(str_sharedSizeText) + str_equalData.replacingOccurrences(of: "error", with: "g")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = ["content": temStr, "msgType": "txt"]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = ["message": temStr, "type": "TxtMsg"]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = ["msgInfo": msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(str_modelName))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(str_nearTitle.suffix(6)) + "sgIn" + str_contentName.lowercased())] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic["params"] = paramsDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
        cellData.msgModel.quoteMsgInfo = quoteModel ?? DeviceJsonModel()

        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        fieldMake(msgData: cellData, reSend: false)
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func confinement(img: UIImage, sendModel: ExpressDataJsonModel) {
        //: let imagePath = TalkingManagerCollectionCellData.getChatImagePath(image: img)
        let imagePath = TalkingManagerCollectionCellData.appearImageMake(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: str_connectionData.map{blackPathEdit(sign: $0)}, encoding: .utf8)!: "ImgMsg", "msgType": "img", String(bytes: str_reactionData.map{viewUp(allow: $0)}, encoding: .utf8)!: imagePath, String(bytes: str_deviceTitle.map{viewGift(table: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = ["pic": imagePath,
                                        //: "type": "ImgMsg",
                                        "type": "ImgMsg",
                                        //: "isBurn": sendModel.isBurn,
                                        "isBurn": sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        "giftId": sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = ["msgInfo": msgInfo, "params": paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        fieldMake(msgData: cellData, reSend: false)
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: TUIMessageCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func profile(msgData: TUIMessageCellData?, sendModel: ExpressDataJsonModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: str_connectionData.map{blackPathEdit(sign: $0)}, encoding: .utf8)!: (String(str_toName) + String(str_topHalfName.suffix(5))), "msgType": "video", String(bytes: str_deviceTitle.map{viewGift(table: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = ["type": (String(str_toName) + String(str_topHalfName.suffix(5))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: str_cellName.map{openLineLast(manager: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: str_addCornerContent.map{appRecord(make: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: str_userChildScriptText.map{dailyColor(view: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        "isBurn": sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        "giftId": sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = ["msgInfo": msgInfo, "params": paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            let uploadCellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dict, isSender: true, isTip: false)
            //: self.msgView.sendCustomMessage(uploadCellData)
            self.msgView.sendCustomMessage(uploadCellData)
            //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
            self.videoDelete(msgData: uploadCellData, sendModel: sendModel)
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.videoDelete(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        fieldMake(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func duringMagnitude(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = ["msgType": "audio",
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: str_tapAssetTitle.map{modelReaction(lab: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(str_pushTitle)): voicePath,
                       //: "senderId": IndexStyleAppManager.share.loginUserMode.userID]
                       String(bytes: str_imageLengthValue.map{$0^145}, encoding: .utf8)!: IndexStyleAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(str_pushTitle)): voicePath,
                         //: "type": "AudioMsg",
                         "type": (String(str_cornerName.suffix(6)) + str_borderValue.lowercased()),
                         //: "len": "\(voiceTime)"]
                         "len": "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = ["msgInfo": msgInfo, "params": paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        fieldMake(msgData: cellData, reSend: false)
    }

    //: func func__sendLocationMsg(latlon: String) {
    func thePic(latlon: String) {
        //: let msgInfo = ["msgType": "map",
        let msgInfo = ["msgType": "map",
                       //: "latlon": latlon,
                       "latlon": latlon,
                       //: "messageType": 28,
                       (String(str_toText)): 28,
                       //: "senderId": IndexStyleAppManager.share.loginUserMode.userID] as [String : Any]
                       String(bytes: str_imageLengthValue.map{$0^145}, encoding: .utf8)!: IndexStyleAppManager.share.loginUserMode.userID] as [String: Any]

        //: let paramsDic = ["latlon": latlon,
        let paramsDic = ["latlon": latlon,
                         //: "type": "MapMsg"] as [String : Any]
                         "type": "MapMsg"] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = ["msgInfo": msgInfo, "params": paramsDic]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        fieldMake(msgData: cellData, reSend: false)
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: TUIMessageCellData, reSend: Bool) {
    private func fieldMake(msgData: TUIMessageCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            noblesseExtra(showMsg: consLet_momentValue)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params["toUid"] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: TalkingCellData.self) == false && msgData.isKind(of: GrantCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: TalkingCellData.self) == false, msgData.isKind(of: GrantCellData.self) == false, params["type"] as? String != (String(str_progressValue.prefix(5)) + String(str_nameValue.suffix(3))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = TUIMsgChatManager.getMessageInsertTime()
        let recordTime = TUIMsgChatManager.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains("pic") { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params["pic"] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params["pic"] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params["type"] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(str_cornerName.suffix(6)) + str_borderValue.lowercased()) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(str_pushTitle))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(str_pushTitle))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(str_pushTitle)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(str_backName.suffix(6)) + "nts/Us" + str_labValue.replacingOccurrences(of: "more", with: "er") + String(str_displayValue))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = consLet_limitPointText + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(str_userValue.suffix(6)) + String(str_bottomTableText.prefix(4)) + "User/r" + String(str_upData.suffix(6)))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = consLet_errData + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params["audioId"] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params["audio"] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        ChatMakeReactiveCompatible.privete(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(str_progressValue.prefix(5)) + String(str_nameValue.suffix(3))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.lessShared(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: consLet_inputName)
                //: if params.keys.contains("txtId") {
                if params.keys.contains("txtId") {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    WaitressThen.month(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict["msgInfo"] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome = NSNumber(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(str_pointValue) + str_leadingData.lowercased())) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(str_pointValue) + str_leadingData.lowercased())] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if DecisionMakerReactiveCompatible.barIn().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: consLet_callValue, object: nil, userInfo: ["mfBean": mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.less(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(str_toText))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(str_progressValue.prefix(5)) + String(str_nameValue.suffix(3))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo["msgType"] = (String(str_lineName.prefix(4)) + "Gift")
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    consLet_contentData.postEqual(eventID: consLet_iconTitle)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(str_progressValue.prefix(5)) + String(str_nameValue.suffix(3))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo["msgType"] = "txt"
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = ["msgInfo": msgInfo]
                //: updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                updateMsgData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
                //: self.msgView.sendCustomMessage(updateMsgData)
                self.msgView.sendCustomMessage(updateMsgData)
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(IndexStyleAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(IndexStyleAppManager.share.loginUserMode.userID)" + (String(str_originTitle) + String(str_fromValue)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue, type != (String(str_progressValue.prefix(5)) + String(str_nameValue.suffix(3))) {
                //: self.removeReplyTipsView()
                self.tapSend()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            WaitressThen.roomFlush(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(str_fileText))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(str_fileText))] as! NSNumber
                //: IndexStyleAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                IndexStyleAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(str_appName) + str_cellValue.replacingOccurrences(of: "filter", with: "g"))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(str_appName) + str_cellValue.replacingOccurrences(of: "filter", with: "g"))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.tipBy(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((str_awakeContent.replacingOccurrences(of: "interval", with: "in") + "ateP" + String(str_hideTitle))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(str_awakeContent.replacingOccurrences(of: "interval", with: "in") + "ateP" + String(str_hideTitle))] as! [String: Any]
                //: let model = OverrideModel(dic: intimatePhotoDict)
                let model = OverrideModel(dic: intimatePhotoDict)
                //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                updateMsgData.msgModel.msgInfo.intimatePhoto = model
                //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo["video"] as! [String: Any]
                //: let model = ChiaroscuroModel(dic: intimateVideoDict)
                let model = ChiaroscuroModel(dic: intimateVideoDict)
                //: updateMsgData.msgModel.msgInfo.video = model
                updateMsgData.msgModel.msgInfo.video = model
                //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        fullPhaseOfTheMoon()

        //: updateSession()
        displace()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func displace() {
        //: AddInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        AddInfoManager.singleOut(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String, type: Int = 0) {
    private func lessShared(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent _: String, type: Int = 0) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard IndexStyleAppManager.share.loginUserMode.status != 1 else {
            guard IndexStyleAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    noblesseExtra(showMsg: errorStr)
                }
                //: return
                return
            }
            //: PostPushManager.share.func__pushToRechargeAlert(appendParams: "&type=\(type)")
            PostPushManager.share.everyReason(appendParams: "&type=\(type)")
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.viewDrop()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: PostPushManager.share.func__pushToSubscribeAlert()
            PostPushManager.share.markParams()

            //: } else if errorCode == 60003, !IndexStyleAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !IndexStyleAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            QuantityWindowManager.shared.sectionIntervalimate()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                noblesseExtra(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func utilizer(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict["msgInfo"] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.less(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func equalInstance() {
        //: if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.footing(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(IndexStyleAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(IndexStyleAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (consLet_partyTitle.array(forKey: consLet_streamTitle)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.blockClear()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                consLet_partyTitle.set(intimateCacheData, forKey: consLet_streamTitle)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func insert(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic["extra"].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?["msgInfo"]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(str_giftTitle) + String(str_genderInfoValue))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.nameEqual(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func less(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(str_giftTitle) + String(str_genderInfoValue))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(str_giftTitle) + String(str_genderInfoValue))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.nameEqual(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func nameEqual(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = AddInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = AddInfoManager.displayIn(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.footing(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.backstopModelApproximate(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.constraintNumberimate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.constraintNumberimate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: AddInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        AddInfoManager.map(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = ["uid": self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: str_targetName.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: consLet_intervalTitle,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension CountensityViewController: VideoThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func present() {
        //: guard IndexStyleAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.commemorative(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func upCite(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func brunswick(actionType: ToAGreaterExtentAdditiveArithmetic) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    premium()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    demonstrateCountimateErrorOf()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                premium()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if IndexStyleAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if IndexStyleAppManager.share.loginUserMode.loungePlus {
                if IndexStyleAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.singingVoiceCall(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                demonstrateCountimateErrorOf()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    common()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    demonstrateCountimateErrorOf()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                common()
            }

        //: case .Location:
        case .Location:
            //: TalkingPermissionTool.func__openLocationServiceWithBlock(true) { [weak self] isOpen in
            KeyPermissionTool.sommelier(true) { [weak self] isOpen in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if isOpen {
                if isOpen {
                    //: self.func__showLocationView(latlon: nil, lookSelf: false)
                    self.far(latlon: nil, lookSelf: false)
                }
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.demonstrateLoad(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func demonstrateCountimateErrorOf() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.noblesseExtra(showMsg: String(bytes: str_endValue.map{relicClose(lab: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func listener(giftModel _: AwardModelType, giftNum _: String, model _: TotalensityMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func fiscalFacilitateNum(giftModel: AwardModelType, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        moveDivert(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: GrantCellData?, fromStr: String?) {
    func moveDivert(giftModel: AwardModelType, giftNum: String, cellData: GrantCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            noblesseExtra(showMsg: consLet_momentValue)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params["toUid"] = self.targetId
        //: params["giftId"] = giftModel.gid
        params["giftId"] = giftModel.gid
        //: params["giftNum"] = giftNum
        params["giftNum"] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(str_imageValue))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params["from"] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ChatMakeReactiveCompatible.lagniappeEqual(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.lessShared(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: consLet_pathName)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.fastening(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.cellData(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.displace()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(str_giftTitle) + String(str_genderInfoValue))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(str_giftTitle) + String(str_genderInfoValue))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.nameEqual(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                consLet_contentData.postEqual(eventID: consLet_netData)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func giftAdd(giftModel: AwardModelType, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        consLet_contentData.postEqual(eventID: consLet_closeData)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            noblesseExtra(showMsg: consLet_momentValue)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= IndexStyleAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            demonstrateCountimateErrorOf()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(str_failureName.prefix(8)) + String(str_modelTitle.suffix(2)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params["type"] = (String(str_giftValue) + String(str_frameContent))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = ["msgType": (String(str_lineName.prefix(4)) + "Gift")]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = ["msgInfo": msgInfo, "params": params]
        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: dic, isSender: true, isTip: false)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        fieldMake(msgData: cellData, reSend: false)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func arrayView(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(consLet_barContent - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func duringSearched(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func radiogramRecord(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.radiogramRecord(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                thanControlAnimation(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func stonewall() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if PrizeObjectProtocol.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            PrizeObjectProtocol.shared.requestPosition()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func cellData(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic["msgType"] = "gift"
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo["msgInfo"] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = TUISocialChatManager.handleTXMessageSendModel(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(str_pointValue) + str_leadingData.lowercased())) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(str_pointValue) + str_leadingData.lowercased())] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        filterBy(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(str_fileText))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(str_fileText))] as! NSNumber
            //: IndexStyleAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            IndexStyleAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: IndexStyleAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.lifeStyle(needReload: false, mf_coin: IndexStyleAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func valueTo() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.actionFromPing(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func filterBy(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.behindDismiss(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func dowery(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.giftArr(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func modelGet() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func conversationCell() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.ticktockAway { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.displace()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func targetMake() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.selectDoingComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.displace()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func disoblige() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = TitleResourceReactiveCompatible()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        uphold()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func balloon() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ValueLabelReactiveCompatible(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.sizeTitle(cellTitleList: [(String(str_tableValue) + String(str_clickValue)).localized, "Notes".localized, "Report".localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .appNameVisualProperty()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: PostPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                PostPushManager.share.miniskirt(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                upAwake()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = ValueDataSource(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.thirdView(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func upAwake() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = MerelyThen()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.exhibitInAnalogDigitalConverter(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.graduatedTable(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func appClick() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            thanControlAnimation(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            uphold()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func hailNotification(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = TitleHandyJSON.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.spreadHead(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func peceiptEqual() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(match(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: consLet_versionTitle,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(componentTo),
                                               //: name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION,
                                               name: consLet_timeKeyValue,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func match(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard IndexStyleAppManager.share.loginUserMode.loungePlus == true else { return }
        guard IndexStyleAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func componentTo() {
        //: IndexStyleAppManager.share.loginUserMode.loungePlus = true
        IndexStyleAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func clickBy() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? TUIMessageCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if WaitressThen.addData(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.wantRequestPop(tipStr: (String(str_playExploreTitle.suffix(4)) + " read" + String(str_kitManagerName.suffix(5)) + String(str_hiddenTitleName)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension CountensityViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func premium() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        KeyPermissionTool.albumTheme(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = MightHaveBeenThen.sendLocal(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.thanEqual(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func addPath() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        KeyPermissionTool.modelBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.thanEqual(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func thanEqual(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ExpressDataJsonModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.confinement(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = DevicePhotosVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ExpressDataJsonModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.confinement(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        uphold()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func common() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard WaitressThen.videoUserVersionShortRangeQueryGift(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == ColorExpressionConvertible.male.rawValue ? "He".localized : "She".localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.noblesseExtra(showMsg: (String(str_callColorValue.suffix(5)) + " usi" + String(str_centerViewData.suffix(5)) + "o low" + String(str_transformData.suffix(5)) + "rsion").localizedArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.visualCommunication()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func visualCommunication() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = MightHaveBeenThen.sendLocal(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            MightHaveBeenThen.makeCompletion(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.coverSection(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.noblesseExtra(showMsg: (String(str_addColorForValue.suffix(4)) + "t vi" + String(str_buttonName.prefix(6)) + String(str_photoValue.prefix(5)) + "t 5 s" + str_shText.replacingOccurrences(of: "make", with: "co")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func coverSection(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: LabelValueEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = HiddenWhiteReactiveCompatible(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ExpressDataJsonModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.profile(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        DeviceFailThen.theAdd()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        uphold()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: TUIMessageCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func videoDelete(msgData: TUIMessageCellData, sendModel: ExpressDataJsonModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        EqualAutomaticallyVideoManager.shared.black(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.noblesseExtra(showMsg: (String(str_awakeExtraValue) + String(str_liveContent.prefix(4)) + "iled").localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.profile(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    //: func func__addVoiceNotification() {
    func funcNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(bombUp),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(roomBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(outTarget(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(quits(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: consLet_errContent,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(insert(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: consLet_limitData,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func moveAdd(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            PrizeObjectProtocol.shared.addWithDelegate()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            PrizeObjectProtocol.shared.pull()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func bombUp() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        moveAdd(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func roomBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        moveAdd(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func outTarget(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            moveAdd(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            moveAdd(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: ClearCellData, cellView: TalkingChatAudioMsgCell) {
    func modelWithAll(cellData: ClearCellData, cellView: StableRoomThen) {
        //: guard TalkingSocketManager.shared.isTalking == false else {
        guard JewishCalendarMonthThen.shared.isTalking == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.noblesseExtra(showMsg: consLet_viewData)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? TUIMessageCellData)!.isKind(of: ClearCellData.self) == false {
            if (indexModel as? TUIMessageCellData)!.isKind(of: ClearCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! ClearCellData
            let audioModel = indexModel as! ClearCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: cellData.voiceModel.db_voiceCacheWrap = AddInfoManager.cache_getAudioDB(message: message)
        cellData.voiceModel.db_voiceCacheWrap = AddInfoManager.mapStream(message: message)
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        PrizeObjectProtocol.shared.requestPosition()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if PrizeObjectProtocol.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: "播放状态:\(PrizeObjectProtocol.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if PrizeObjectProtocol.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                PrizeObjectProtocol.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        PrizeObjectProtocol.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        PrizeObjectProtocol.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                let subModel: TUIMessageCellData = self.msgView.uiMsgs.object(at: index) as! TUIMessageCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: ClearCellData.self) {
                if subModel.isKind(of: ClearCellData.self) {
                    //: let audioModel = subModel as! ClearCellData
                    let audioModel = subModel as! ClearCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: voiceModel.db_voiceCacheWrap = AddInfoManager.cache_getAudioDB(message: subMessage)
                    voiceModel.db_voiceCacheWrap = AddInfoManager.mapStream(message: subMessage)
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: QualityMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    QualityMsgTable.overEndMsg(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        PrizeObjectProtocol.shared.playDoing(msgArr: waitArray)
    }
}

// MARK: - 地图相关

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 发送地图
    //: private func func__showLocationView(latlon: String?, lookSelf: Bool) {
    private func far(latlon: String?, lookSelf: Bool) {
        //: let location = TalkingMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        let location = EventSwitchlyMapView(frame: CGRect.zero, isSelf: lookSelf, latlon: latlon)
        //: location.show()
        location.roundShow()
        //: location.sendBlcok = { [weak self] latlon in
        location.sendBlcok = { [weak self] latlon in
            //: self?.func__sendLocationMsg(latlon: latlon)
            self?.thePic(latlon: latlon)
        }
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func priceInfo() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.touchTime()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.dataDict(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? TUIMessageCellData
                let indexModel = model as? TUIMessageCellData
                //: if indexModel?.isKind(of: ClearCellData.self) == true || indexModel?.isKind(of: TalkingManagerCollectionCellData.self) == true || indexModel?.isKind(of: BackgroundLabelCellData.self) == true || indexModel?.isKind(of: TalkingCellData.self) == true {
                if indexModel?.isKind(of: ClearCellData.self) == true || indexModel?.isKind(of: TalkingManagerCollectionCellData.self) == true || indexModel?.isKind(of: BackgroundLabelCellData.self) == true || indexModel?.isKind(of: TalkingCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.fullPhaseOfTheMoon()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.videoAcross()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.clickBy()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func touchTime() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.underPause(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.enveloping(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.equalInstance()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == IndexStyleAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == IndexStyleAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == IndexStyleAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == IndexStyleAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.graduatedTable(userWrap)
    }

    //: func func__getFillDraftText() {
    func draft() {
        //: let userID = "c2c_"+targetId
        let userID = "c2c_" + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.present()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func generate() {
        //: let userID = "c2c_"+targetId
        let userID = "c2c_" + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    //: func func__TipsNotification() {
    func gildTheLilyNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(designate(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: consLet_attributeContent,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func designate(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: AddCellData  = userinfo["msg"] as! AddCellData
        let celldata: AddCellData = userinfo["msg"] as! AddCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func wantRequestPop(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = ["content": tipStr,
                                      //: "msgType": "tips"]
                                      "msgType": "tips"]
        //: var message: TUIMessageCellData?
        var message: TUIMessageCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = TUISocialChatManager.insertTXMessageModel(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
            message = TUISocialChatManager.handleTXMessageSendModel(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func videoAcross() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        WaitressThen.tiptoe { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.wantRequestPop(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func tipBy(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.wantRequestPop(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func fullPhaseOfTheMoon() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        WaitressThen.sharedWithHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.wantRequestPop(tipStr: String(bytes: str_emptyValue.map{givenText(error: $0)}, encoding: .utf8)!.localizedArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func quits(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: TUIMessageCellData = dict?["cellData"] as! TUIMessageCellData
        let cellData: TUIMessageCellData = dict?[(String(str_addArrayName.suffix(8)))] as! TUIMessageCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension CountensityViewController: PrizeReactiveCompatible {
    //: func func__setReplyTipsView() {
    func methodAdd() {
        //: let key =  "\(IndexStyleAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(IndexStyleAppManager.share.loginUserMode.userID)" + (String(str_originTitle) + String(str_fromValue)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && IndexStyleAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && IndexStyleAppManager.share.appStatus == FailHashable.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.behindWith()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            push(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            tapSend()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func targetStr(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        divert(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        tapSend()
    }

    //: func removeReplyTipsView() {
    func tapSend() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            push(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func page() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: consLet_saltValue, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: consLet_sessionName - consLet_barContent, width: consLet_errorDeviceHeightData, height: consLet_sessionName)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: consLet_errorDeviceHeightData,
                                     //: height: ScreenHeight)
                                     height: consLet_sessionName)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func thanControlAnimation(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.userRead() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if EqualSendReactiveCompatible.clout().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: consLet_domainName, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: PresentationCalorieChartDataSource.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: consLet_sessionName - consLet_barContent,
                                     //: width: ScreenWidth,
                                     width: consLet_errorDeviceHeightData,
                                     //: height: ScreenHeight)
                                     height: consLet_sessionName)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if EqualSendReactiveCompatible.clout().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: consLet_domainName, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension CountensityViewController {
    /// 创建UI
    //: private func createUI() {
    private func addEvent() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(str_bottomRoomData.suffix(4)) + "em n" + String(str_indexValue)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func asLevel() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? consLet_barContent : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.toColorRadii(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? consLet_messageText : consLet_tabValue)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + consLet_halfTitle)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? consLet_messageText : consLet_tabValue)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + consLet_halfTitle)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + consLet_halfTitle)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func upSumeraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.appClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.balloon()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.brunswick(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                consLet_contentData.postEqual(eventID: consLet_freeData, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
