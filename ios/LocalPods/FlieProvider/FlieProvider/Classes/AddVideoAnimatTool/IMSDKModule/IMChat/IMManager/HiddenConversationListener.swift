
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let str_userContent:String = "IM退出\u{767b}"
fileprivate let str_addName:String = "ode:for to model length name"

/*: , desc: :*/
fileprivate let str_equalData:String = ",label"
fileprivate let str_tapData:String = "desc:start gift view height"

/*: ." :*/
fileprivate let str_messageData:String = "."

/*: "IM登录失败，code: :*/
fileprivate let str_indicatorValue:[Character] = ["I","M","\u{767b}","录","\u{5931}","败","，"]
fileprivate let str_toPleaseName:String = "self view to stylecode:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let str_colorValue:[Character] = ["I","M","获","\u{53d6}","未","读"]
fileprivate let str_borderText:String = "消息总数\u{5931}败"

/*: "crushBadge" :*/
fileprivate let str_labName:[Character] = ["c","r","u","s","h","B","a","d","g","e"]

/*: "badNumber" :*/
fileprivate let str_componentValue:[UInt8] = [0x30,0x33,0x36,0x1c,0x27,0x3f,0x30,0x37,0x20]

private func modelDoing(area num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "plaza" :*/
fileprivate let str_actualTitle:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let str_fileData:[Character] = ["r","o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let str_addCellRoomData:String = "namem"

/*: "onlyOneRoomId" :*/
fileprivate let str_totalTitle:String = "onlyOnarea moment"

/*: "LIVE-CHAT" :*/
fileprivate let str_colorText:String = "return selfLIVE-CHAT"

/*: "PARTY-CHAT" :*/
fileprivate let str_equalATitle:String = "PARTY-any var case near"
fileprivate let str_objectValue:String = "chat"

/*: "extra" :*/
fileprivate let str_labelEqualData:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let str_indicatorName:[Character] = ["m"]
fileprivate let str_allName:String = "model user super import registersgType"

/*: "newPkgGift" :*/
fileprivate let str_toContent:[Character] = ["n","e","w","P","k","g","G"]
fileprivate let str_logText:[Character] = ["i","f","t"]

/*: "MF:ChatMentionMsg" :*/
fileprivate let str_topValue:String = "frame trueMF:Ch"
fileprivate let str_roomName:String = "nMsgfor list manager compute don"

/*: "uid" :*/
fileprivate let str_loadValue:[UInt8] = [0x92,0x8e,0x83]

/*: "onlineStatus" :*/
fileprivate let str_labData:[UInt8] = [0xe6,0xe7,0xe5,0xe0,0xe7,0xec,0xda,0xfd,0xe8,0xfd,0xfc,0xfa]

private func addClickName(status num: UInt8) -> UInt8 {
    return num ^ 137
}

/*: "isNewUser" :*/
fileprivate let str_centerContent:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "c2c_ :*/
fileprivate let str_pushTheContent:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let str_normalData:[Character] = ["I","M","获","取","会","话","\u{5931}","败","，","c"]
fileprivate let str_modelName:String = "onvId:max table room mode in"

/*: , code: :*/
fileprivate let str_whiteValue:String = "near"
fileprivate let str_styleContent:[Character] = [" ","c","o","d","e",":"]

/*: "IM插入消息失败，targetId: :*/
fileprivate let str_bottomValue:String = "at clickIM插入消"
fileprivate let str_modeValue:String = "argetIdmake to file"
fileprivate let str_miniData:String = ":"

/*: "changeDirection" :*/
fileprivate let str_pathValue:[Character] = ["c","h","a","n","g"]
fileprivate let str_norTitle:String = "eDirapp language let label user"

/*: "rYMsgType" :*/
fileprivate let str_positionData:[Character] = ["r","Y","M","s","g","T","y","p"]
fileprivate let str_layerCellTopName:String = "view"

/*: "GJ:CallCustom" :*/
fileprivate let str_mediumText:String = "GJ:Calfor equal"
fileprivate let str_labBubbleVarText:[Character] = ["m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let str_withTitle:String = "path start self viewIM获取会话"
fileprivate let str_nameContent:String = "，code:pop tap title view temp"

/*: "msgInfo" :*/
fileprivate let str_editTitle:String = "make package pathmsgInfo"

/*: "needFold" :*/
fileprivate let str_viewData:String = "ninfod"

/*: "group_ :*/
fileprivate let str_contentData:String = "group_if to"

/*: "IM删除会话失败，convID: :*/
fileprivate let str_tapTitle:[Character] = ["I","M","删","\u{9664}","会"]
fileprivate let str_colorName:[Character] = ["话","失","败","，","c","o","n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let str_phoneModeValue:[Character] = ["I","M","置","\u{9876}","会","话","失","败"]
fileprivate let str_maleHeadText:[Character] = ["，","c","o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let str_normalText:[Character] = ["S","D","K"," ","正","在","连","\u{63a5}","到"]
fileprivate let str_nameValue:[Character] = ["服","务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let str_userData:[Character] = ["S","D","K"," ","\u{5df2}","经","成"]
fileprivate let str_linePointValue:[Character] = ["功","连","接","到","服","务","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let str_premiumData:String = "IM\u{8fde}接服"
fileprivate let str_viewValue:String = "background data view，code:"

/*: , error: :*/
fileprivate let str_phoneValue:String = "label gift ban, erro"
fileprivate let str_logCurrentText:String = "r:cell color to else"

/*: "IM账号被挤下线。" :*/
fileprivate let str_transformTitle:[Character] = ["I","M","账","号","被","挤","下","线","\u{3002}"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let str_messageTitle:[UInt8] = [0x34,0x2,0x18,0x1f,0x4d,0xc,0xe,0xe,0x2,0x18,0x3,0x19,0x4d,0x4,0x1e,0x4d,0x1,0x2,0xa,0xa,0x8,0x9,0x4d,0x4,0x3,0x4d,0x2,0x3,0x4d,0xc,0x3,0x2,0x19,0x5,0x8,0x1f,0x4d,0x9,0x8,0x1b,0x4,0xe,0x8,0x4d,0xc,0x3,0x9,0x4d,0x14,0x2,0x18,0x4d,0xc,0x1f,0x8,0x4d,0xb,0x2,0x1f,0xe,0x8,0x9,0x4d,0x19,0x2,0x4d,0xa,0x2,0x4d,0x2,0xb,0xb,0x1,0x4,0x3,0x8,0x4c,0x4d,0x24,0xb,0x4d,0x4,0x19,0x4d,0x4,0x1e,0x4d,0x3,0x2,0x19,0x4d,0x9,0x2,0x3,0x8,0x4d,0xf,0x14,0x4d,0x0,0x8,0x41,0x4d,0x19,0x5,0x8,0x4d,0x1d,0xc,0x1e,0x1e,0x1a,0x2,0x1f,0x9,0x4d,0x0,0xc,0x14,0x4d,0x5,0xc,0x1b,0x8,0x4d,0xf,0x8,0x8,0x3,0x4d,0x1,0x8,0xc,0x6,0x8,0x9,0x41,0x4d,0xc,0x3,0x9,0x4d,0x4,0x19,0x4d,0x4,0x1e,0x4d,0x1f,0x8,0xe,0x2,0x0,0x0,0x8,0x3,0x9,0x8,0x9,0x4d,0x19,0x2,0x4d,0xe,0x5,0xc,0x3,0xa,0x8,0x4d,0x19,0x5,0x8,0x4d,0x1d,0xc,0x1e,0x1e,0x1a,0x2,0x1f,0x9,0x43]

private func giftBottomVisible(path num: UInt8) -> UInt8 {
    return num ^ 109
}

/*: "Offline Notification" :*/
fileprivate let str_miniValue:String = "Offlintrue stream round"
fileprivate let str_profileText:String = "tofic"
fileprivate let str_atName:[Character] = ["a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let str_progressData:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let str_drunkMeTitle:String = "empty class hidden device resultRe Login"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HiddenConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol HiddenObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func subControl(data: [ModelAllThen])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func notBegin(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func message(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum ServerDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class HiddenConversationListener: NSObject {
@objcMembers public class HiddenConversationListener: NSObject {
    //: public static let shared = HiddenConversationListener()
    public static let shared = HiddenConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [ModelAllThen] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [ModelAllThen] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [ModelAllThen] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [ModelAllThen] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [ModelAllThen] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: ModelAllThen]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.modelDisplayMake()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func physicsLaboratoryPause() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(consLet_pageLicenseTitle, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func modelDisplayMake() {
        //: func__removeAllDelegate()
        telecasting()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.anySelect()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func anySelect() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = ModelAllThen.tablet(chatType: .system)
//        let serviceModel = ModelAllThen.getPlaceholderModel(chatType: .service)
        //: self.officialList = [systemModel/|*, serviceModel*|/]
        self.officialList = [systemModel /* , serviceModel */ ]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func destroy() -> [ModelAllThen] {
        // 审核模式，在消息列表移除官方客服
        //: if IndexStyleAppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if IndexStyleAppManager.share.appStatus == FailHashable.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == OverdoReactiveCompatible.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == OverdoReactiveCompatible.use() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension HiddenConversationListener {
extension HiddenConversationListener {
    //: func func__removeAllDelegate() {
    func telecasting() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func inventoryItem(_ delegate: HiddenObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func addDelegate(_ delegate: HiddenObjectProtocol) {
        //: self.func__invokeDelegate {
        self.invoke {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func invoke(_ invocation: (HiddenObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! HiddenObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension HiddenConversationListener {
extension HiddenConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func currentExecute() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: EqualLogTool.writeDoing(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            EqualLogTool.writeDoing(msg: (str_userContent + "\u{5f55}失败，c" + String(str_addName.prefix(4))) + "\(code), desc:\(String(describing: desc)).")
        }
        //: self.initialization()
        self.modelDisplayMake()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func instanceThen(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = IndexStyleAppManager.share.appUserConfigMode.uSig
        let uSigStr = IndexStyleAppManager.share.appUserConfigMode.uSig
        //: let uidStr = IndexStyleAppManager.share.loginUserMode.userID
        let uidStr = IndexStyleAppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.logBy()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: EqualLogTool.writeDoing(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            EqualLogTool.writeDoing(msg: (String(str_indicatorValue) + String(str_toPleaseName.suffix(5))) + "\(code), desc:\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func quick() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || IndexStyleAppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || IndexStyleAppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.noblesseExtra(showMsg: consLet_momentValue)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension HiddenConversationListener {
extension HiddenConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func logBy() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: EqualLogTool.writeDoing(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            EqualLogTool.writeDoing(msg: (String(str_colorValue) + str_borderText + "，code:") + "\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func tradeNameRoom() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        ChatMakeReactiveCompatible.equalWithTo { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(str_labName))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: consLet_keyValue, object: self, userInfo: [String(bytes: str_componentValue.map{modelDoing(area: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func modeAll(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        ChatMakeReactiveCompatible.primaryColor { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data["plaza"] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza["roomNum"] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = ModelAllThen.tablet(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza["num"] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(str_totalTitle.prefix(6)) + "eRoomId")] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension HiddenConversationListener: V2TIMAdvancedMsgListener {
extension HiddenConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(str_colorText.suffix(9)))) || msg.groupID.contains((String(str_equalATitle.prefix(6)) + str_objectValue.uppercased()))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            VersionThen.clickLoad().danmuClick(msg: msg)
            //: return
            return
        }
        //: if msg.sender == IndexStyleAppManager.share.loginUserMode.userID { return }
        if msg.sender == IndexStyleAppManager.share.loginUserMode.userID { return }

        //: let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = TUIMsgChatManager.parseTXMessageData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic["extra"] as! [AnyHashable: Any]
        //: let msgModel = LockModel(dic: extraDic)
        let msgModel = LockModel(dic: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: consLet_limitData, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic["msgType"] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(str_toContent) + String(str_logText)) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: consLet_showValue, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if TUIMsgChatManager.checkVoiceMessage(message: msg) {
            if TUIMsgChatManager.checkVoiceMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                //: QualityMsgTable.db_inserVoiceMsgToDB(dic)
                QualityMsgTable.equidistantModel(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.detailDic(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            AddVideoAnimatTool.shared.toLine(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel.msgInfo.messageType == .MessageTypeGift {
        if msgModel.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            AddVideoAnimatTool.shared.aidAcross(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(str_topValue.suffix(5)) + "atMentio" + String(str_roomName.prefix(4))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            GrantThen.share.gestureEqual(msg: msg, extraInfo: extraDic)
        }

        //: if !msgModel.gift.unlockMsgId.isEmptyString {
        if !msgModel.gift.unlockMsgId.isEmptyString { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: WaitressThen.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel.gift.unlockMsgId)
            array.append(msgModel.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: WaitressThen.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != OverdoReactiveCompatible.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != OverdoReactiveCompatible.winImage() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.invoke { $0.message?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            VersionReactiveCompatible.shared.animal(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = ["uid": msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: str_labData.map{addClickName(status: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel.msgInfo.isNewUser] as [String: Any]
                                String(bytes: str_centerContent.reversed(), encoding: .utf8)!: msgModel.msgInfo.isNewUser] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: consLet_appData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: AddInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            AddInfoManager.nameSh(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            WaitressThen.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: consLet_versionTitle, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
    @objc public func priveteSendAt(targetId: String, msgCellData: TUIMessageCellData, isPrivete: Bool) {
        //: let imMsg = msgCellData.innerMessage
        let imMsg = msgCellData.innerMessage

        //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        self.section(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = "c2c_\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.appList(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.targetPhotoGift(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: EqualLogTool.writeDoing(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                EqualLogTool.writeDoing(msg: (String(str_normalData) + String(str_modelName.prefix(6))) + "\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func section(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = IndexStyleAppManager.share.loginUserMode.userID
        let senderStr = IndexStyleAppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                EqualLogTool.writeDoing(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func detailDic(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(str_pathValue) + String(str_norTitle.prefix(4)) + "ection")].boolValue
        if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && PostPushManager.share.overDoinglineVc()!.isKind(of: CountensityViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: consLet_screenContent) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: consLet_screenContent)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension HiddenConversationListener: V2TIMConversationListener {
extension HiddenConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.appList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.targetPhotoGift(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.appList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.targetPhotoGift(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: IndexStyleAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        IndexStyleAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.invoke {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.notBegin?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func targetPhotoGift(conVersationList: [ModelAllThen]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if AddInfoManager.shared().needReqUidsSet != nil {
            if AddInfoManager.pastGenerate().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: AddInfoManager.shared().needReqUidsSet!.formUnion(set)
                AddInfoManager.pastGenerate().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.invoke {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.subControl?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func carryEnableMenu(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [ModelAllThen] = []

        // V2TIMConversation->ModelAllThen
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = ModelAllThen(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.backgroundShow(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [ModelAllThen] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = VersionInfoTable.infoSize(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.insertFromRawStuffTop(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension HiddenConversationListener {
extension HiddenConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func completion(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.carryEnableMenu(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.targetPhotoGift(conVersationList: arr as? [ModelAllThen] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: EqualLogTool.writeDoing(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            EqualLogTool.writeDoing(msg: (String(str_withTitle.suffix(6)) + "列表失败" + String(str_nameContent.prefix(6))) + "\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func recuperate(targetId: String) -> ModelAllThen? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func appList(convList: [V2TIMConversation]) -> [ModelAllThen] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [ModelAllThen] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = ModelAllThen(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.backgroundShow(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.insertFromRawStuffTop(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func insertFromRawStuffTop(convModel: ModelAllThen, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.anySelect()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: ServerDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - IndexStyleAppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - IndexStyleAppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = AddInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = AddInfoManager.displayIn(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            let dict = TUIMsgChatManager.parseTXMessageData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json["extra"]["msgInfo"][(str_viewData.replacingOccurrences(of: "info", with: "ee") + "Fold")].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.toType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.toType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func toType(convList: [ModelAllThen], dataType: ServerDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func upType(targetId: String, dataType: ServerDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: AddInfoManager.db_removeRecord(with: targetId)
            AddInfoManager.subShowWith(with: targetId)
            //: QualityMsgTable.db_deleteVoiceMsg(IndexStyleAppManager.share.loginUserMode.userID, toUid: targetId)
            QualityMsgTable.withUid(IndexStyleAppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.logBy()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: EqualLogTool.writeDoing(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            EqualLogTool.writeDoing(msg: (String(str_tapTitle) + String(str_colorName)) + "\(convID), code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func inputTop(convModel: ModelAllThen, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: EqualLogTool.writeDoing(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            EqualLogTool.writeDoing(msg: (String(str_phoneModeValue) + String(str_maleHeadText)) + "\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.toType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.toType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension HiddenConversationListener: V2TIMSDKListener {
extension HiddenConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(str_normalText) + String(str_nameValue)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(str_userData) + String(str_linePointValue)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: EqualLogTool.writeDoing(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        EqualLogTool.writeDoing(msg: (str_premiumData + "务器失败" + String(str_viewValue.suffix(6))) + "\(code), error:\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: EqualLogTool.writeDoing(msg: "IM账号被挤下线。")
        EqualLogTool.writeDoing(msg: (String(str_transformTitle)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        JewishCalendarMonthThen.shared.afterRecord(userLoginOut: true)
        //: HiddenConversationListener.shared.isConnection = false
        HiddenConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (UtilizationKingfisherCompatible.shared as! UtilizationKingfisherCompatible).giftDown()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: str_messageTitle.map{giftBottomVisible(path: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = ResourceAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if TalkingBenderTotalensityRequestThen.share.interfaceLang == NameTransformable.ar.rawValue || TalkingBenderTotalensityRequestThen.share.interfaceLang == NameTransformable.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            TalkingBenderTotalensityRequestThen.share.interfaceLang == NameTransformable.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.little(fontSize: 13)
            config.rightFont = UIFont.little(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        EqualAlertShow.showConfig()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        EqualAlertShow.showConfig(title: (String(str_miniValue.prefix(6)) + "e No" + str_profileText.replacingOccurrences(of: "to", with: "ti") + String(str_atName)).localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DecisionMakerReactiveCompatible.barIn().aboveLimit()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            EqualSendReactiveCompatible.clout().sinceShared()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            AdministratorReactiveCompatible.information { t in
                //: ProgressHUD.dismiss()
                DeviceFailThen.theAdd()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: consLet_askData, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            AdministratorReactiveCompatible.bit { t in
                //: if t {
                if t {
                    //: HiddenConversationListener.shared.func__LogingIn { _ in
                    HiddenConversationListener.shared.instanceThen { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if DecisionMakerReactiveCompatible.barIn().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(DecisionMakerReactiveCompatible.barIn().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    JewishCalendarMonthThen.shared.timeSocket()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
