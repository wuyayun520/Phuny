
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let str_noContent:[UInt8] = [0xde,0xcf,0xdc,0xda,0xd7,0xd1,0xdd,0xda,0xcf,0xda,0xdb,0xdd,0xd1,0xcd,0xc6,0xcf,0xc0,0xc9,0xcb,0xd1,0xc0,0xc1,0xda,0xc7,0xc8,0xc7,0xcd,0xcf,0xda,0xc7,0xc1,0xc0]

private func spaceSkip(make num: UInt8) -> UInt8 {
    return num ^ 142
}

/*: "p- :*/
fileprivate let str_remarkData:[Character] = ["p","-"]

/*: - :*/
fileprivate let str_translateData:String = "-"

/*: "redirectUrl" :*/
fileprivate let str_miniData:[Character] = ["r","e","d","i","r","e","c","t","U"]
fileprivate let str_iconKeyData:[Character] = ["r","l"]

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let str_fileData:String = "VOICnormal"
fileprivate let str_randomValue:String = " \u{8fdb}房模型"
fileprivate let str_theName:String = "\u{89e3}析失败。"

/*: "Party room exception" :*/
fileprivate let str_pointName:String = "let head table view cardParty"
fileprivate let str_equalRateValue:String = "rare let extensionm excep"
fileprivate let str_roomObjectTitle:[Character] = ["t","i","o","n"]

/*: _ :*/
fileprivate let str_distanceCellData:String = "_"

/*: "male" :*/
fileprivate let str_voiceTempContent:[UInt8] = [0xb2,0xbe,0xb3,0xba]

private func deleteVideo(color num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "female" :*/
fileprivate let str_bottomName:[Character] = ["f","e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let str_modelData:String = "VOIdataRO"
fileprivate let str_mainSizeData:String = "repeat model equalOM: "
fileprivate let str_nowObjectTitle:String = "失\u{8d25}。grou"
fileprivate let str_matchContent:String = "view name status makepId: "

/*: ." :*/
fileprivate let str_rowData:String = "view"

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let str_itemValue:String = "VnameIC"
fileprivate let str_momentData:String = "M: 切房模top hidden task make type"

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let str_sexValue:[Character] = ["V","O","I","C","E"]
fileprivate let str_styleText:String = "ROOM: self talk return item add"
fileprivate let str_addData:String = "\u{8d25}。gro"
fileprivate let str_modeText:[Character] = ["u","p","I","d",":"," "]

/*: "errCode" :*/
fileprivate let str_playAppValue:[UInt8] = [0x65,0x64,0x6f,0x43,0x72,0x72,0x65]

/*: "custom_kickout_message" :*/
fileprivate let str_equalText:[Character] = ["c","u","s","t","o","m","_","k","i","c","k","o","u","t","_","m","e","s"]
fileprivate let str_toName:String = "SAGE"

/*: "closeRoom" :*/
fileprivate let str_atValue:String = "clab"
fileprivate let str_makeRoomValue:[Character] = ["o","s","e","R","o","o","m"]

/*: "adminCloseRoom" :*/
fileprivate let str_addText:[Character] = ["a","d","m","i","n","C"]
fileprivate let str_centerName:String = "managerse"

/*: "The room is closed, please go to another room" :*/
fileprivate let str_labPrivacyData:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x72,0x65,0x68,0x74,0x6f,0x6e,0x61,0x20,0x6f,0x74,0x20,0x6f,0x67,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x73,0x6f,0x6c,0x63,0x20,0x73,0x69,0x20,0x6d,0x6f,0x6f,0x72,0x20,0x65,0x68,0x54]

/*: "OK" :*/
fileprivate let str_lengthValue:String = "Oequal"

/*: "You were kicked out of the room" :*/
fileprivate let str_atLastValue:[UInt8] = [0xf5,0xc3,0xd9,0x8c,0xdb,0xc9,0xde,0xc9,0x8c,0xc7,0xc5,0xcf,0xc7,0xc9,0xc8,0x8c,0xc3,0xd9,0xd8,0x8c,0xc3,0xca,0x8c,0xd8,0xc4,0xc9,0x8c,0xde,0xc3,0xc3,0xc1]

private func addDoing(name num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let str_windowViewData:[Character] = ["V","O","I","C","E","R","O","O","M","："]
fileprivate let str_emptyText:String = "cell title进房失败，r"
fileprivate let str_toData:[Character] = ["o","o","m","I","D",":"," "]

/*: , error:  :*/
fileprivate let str_whiteValue:String = "info model model result make, er"

/*: , reason:  :*/
fileprivate let str_infoValue:[Character] = [","," ","r","e","a","s","o","n",":"]
fileprivate let str_clickTitleValue:String = " "

/*: , extendedData:  :*/
fileprivate let str_withText:String = ", exteself view transition"
fileprivate let str_remindStoreValue:String = "nlittleelittle"
fileprivate let str_changeTitle:String = "Data: make layer"

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let str_cellData:String = "VOImanager"
fileprivate let str_centerPickData:[Character] = ["当","前","用","户","推","流","失","败"]
fileprivate let str_arrayCollectTitle:[Character] = ["，","r","o","o","m","I","D",":"," "]

/*: , errorCode:  :*/
fileprivate let str_labelName:[Character] = [","," ","e","r","r","o","r","C","o","d","e",":"," "]

/*: , streamID:  :*/
fileprivate let str_sizeValue:String = ", strcost extension input play"

/*: "An error occurred, please try again" :*/
fileprivate let str_layerValue:[UInt8] = [0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x72,0x72,0x75,0x63,0x63,0x6f,0x20,0x72,0x6f,0x72,0x72,0x65,0x20,0x6e,0x41]

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let str_buttonBorderName:String = "VOfrom"
fileprivate let str_backgroundText:String = "流失\u{8d25}，r"

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let str_slideData:String = "VOviewC"
fileprivate let str_changeEqualTitle:String = ": \u{4fe1}令解析失"
fileprivate let str_firstData:[Character] = ["败","，","c","o","m","m","a","n","d",":"," "]

/*: , roomID:  :*/
fileprivate let str_contentText:[Character] = [","," ","r","o","o","m"]
fileprivate let str_replacementValue:[Character] = ["I","D",":"," "]

/*: "data" :*/
fileprivate let str_byName:[Character] = ["d","a","t","a"]

/*: "position" :*/
fileprivate let str_matchData:String = "ptitle"
fileprivate let str_voiceName:String = "sifinishion"

/*: "xxxx--  :*/
fileprivate let str_pathValue:[Character] = ["x","x","x","x","-","-"," "]

/*: "opType" :*/
fileprivate let str_collectionValue:String = "item up string arrayopType"

/*: "party:changeMike" :*/
fileprivate let str_indexCenterData:[Character] = ["p","a","r","t","y",":","c","h","a","n","g","e","M"]
fileprivate let str_cameraName:String = "iinpute"

/*: "type" :*/
fileprivate let str_appFrameName:String = "allype"

/*: "toPosition" :*/
fileprivate let str_bindValue:String = "share else status data maketoPosit"
fileprivate let str_upValue:[Character] = ["i","o","n"]

/*: "party:adminChangeMike" :*/
fileprivate let str_toFreeText:[Character] = ["p","a","r","t","y",":","a","d","m","i","n","C","h","a","n","g","e"]
fileprivate let str_errorName:String = "Mikeimage height fill make if"

/*: "You were kicked off the seat" :*/
fileprivate let str_styleTitle:String = "You was self"
fileprivate let str_sessionText:String = "icked pair history"
fileprivate let str_justValue:String = "lab ifthe "
fileprivate let str_loadName:String = "contexteat"

/*: "party:changeCoin" :*/
fileprivate let str_enterValue:String = "party:top table equal count add"
fileprivate let str_halfName:String = "info image raw ineCoin"

/*: "party:closeStream" :*/
fileprivate let str_nativeData:[Character] = ["p","a","r","t","y",":","c","l","o","s","e","S","t","r","e","a","m"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualSendReactiveCompatible.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let consLet_noTitle = NSNotification.Name(rawValue: String(bytes: str_noContent.map{spaceSkip(make: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let consLet_errorTitle = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum VideoRoomRole: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class EqualSendReactiveCompatible: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = KeyModelType()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: consLet_noTitle, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: EqualSendReactiveCompatible?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: LogViewDelegate?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: ServerTransformable]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = canPosition()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func clout() -> EqualSendReactiveCompatible {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = EqualSendReactiveCompatible()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func fiftyPercent() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if EqualSendReactiveCompatible._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            EqualSendReactiveCompatible._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: QuantityHiddenMiniView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = QuantityHiddenMiniView.pickingsView()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.tin()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension EqualSendReactiveCompatible {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func starKey(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        HeritageReqTool.appSave(roomId: partyModel.roomId,
                                        //: uid: Int(IndexStyleAppManager.share.loginUid) ?? 0,
                                        uid: Int(IndexStyleAppManager.share.loginUid) ?? 0,
                                        //: type: type,
                                        type: type,
                                        //: position: position,
                                        position: position,
                                        //: toPos: toPosition)
                                        toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.voiceId()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = ServerTransformable.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.polePosition(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.voiceId()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func roomSuccess(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        HeritageReqTool.multi(roomId: partyModel.roomId,
                                      //: uid: uid,
                                      uid: uid,
                                      //: type: type,
                                      type: type,
                                      //: position: position)
                                      position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func voiceId() -> String {
        //: return "p-\(IndexStyleAppManager.share.loginUid)-\(partyModel.roomId)"
        return "p-\(IndexStyleAppManager.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension EqualSendReactiveCompatible {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func colorModel(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        KeyPermissionTool.awakeEqual(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.noblesseExtra(showMsg: consLet_busySoundData)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            HeritageReqTool.gestureOn(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: PostPushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            PostPushManager.share.gildTheLilyCount(urlStr: result[(String(str_miniData) + String(str_iconKeyData))] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.noblesseExtra(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isTalking == false else {
                guard JewishCalendarMonthThen.shared.isTalking == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.noblesseExtra(showMsg: consLet_viewData)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = KeyModelType.deserialize(from: result as? Dictionary) else {
                    //: EqualLogTool.writeDoing(msg: "VOICEROOM: 进房模型解析失败。")
                    EqualLogTool.writeDoing(msg: (str_fileData.replacingOccurrences(of: "normal", with: "ER") + "OOM:" + str_randomValue + str_theName))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.noblesseExtra(showMsg: (String(str_pointName.suffix(5)) + " roo" + String(str_equalRateValue.suffix(7)) + String(str_roomObjectTitle)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = LogViewDelegate()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.uphold()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.addTo()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.colloquiumLivingRoom()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                consLet_contentData.postEqual(eventID: "\(consLet_displayValue)_\(IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue ? "male" : "female")")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                VersionThen.join(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: EqualLogTool.writeDoing(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        EqualLogTool.writeDoing(msg: (str_modelData.replacingOccurrences(of: "data", with: "CE") + String(str_mainSizeData.suffix(4)) + "群聊\u{8fdb}房" + str_nowObjectTitle + String(str_matchContent.suffix(5))) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.noblesseExtra(showMsg: (String(str_pointName.suffix(5)) + " roo" + String(str_equalRateValue.suffix(7)) + String(str_roomObjectTitle)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.sinceShared()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func cubby(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        HeritageReqTool.jailCellSumerglow(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.dataWriteRecord()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == IndexStyleAppManager.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == IndexStyleAppManager.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = KeyModelType.deserialize(from: result as? Dictionary) else {
                //: EqualLogTool.writeDoing(msg: "VOICEROOM: 切房模型解析失败。")
                EqualLogTool.writeDoing(msg: (str_itemValue.replacingOccurrences(of: "name", with: "O") + "EROO" + String(str_momentData.prefix(6)) + "型解析\u{5931}败。"))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.noblesseExtra(showMsg: (String(str_pointName.suffix(5)) + " roo" + String(str_equalRateValue.suffix(7)) + String(str_roomObjectTitle)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = LogViewDelegate()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.uphold()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.addTo()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.voxKind(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(IndexStyleAppManager.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            consLet_contentData.postEqual(eventID: "\(consLet_displayValue)_\(IndexStyleAppManager.share.loginUserMode.sex == ColorExpressionConvertible.male.rawValue ? "male" : "female")")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            VersionThen.join(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: EqualLogTool.writeDoing(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    EqualLogTool.writeDoing(msg: (String(str_sexValue) + String(str_styleText.prefix(6)) + "群聊切房\u{5931}" + str_addData + String(str_modeText)) + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.noblesseExtra(showMsg: (String(str_pointName.suffix(5)) + " roo" + String(str_equalRateValue.suffix(7)) + String(str_roomObjectTitle)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.sinceShared()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension EqualSendReactiveCompatible {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func noText() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.itineraryPoint()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func tin() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = userRead() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: CountensityViewController.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func sinceShared() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.dataWriteRecord() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.starKey(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            EqualSendReactiveCompatible.fiftyPercent() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension EqualSendReactiveCompatible {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func colloquiumLivingRoom() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = OverdoReactiveCompatible.getVoiceRoomAppId()
        profile.appID = OverdoReactiveCompatible.startId()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: IndexStyleAppManager.share.loginUid)
        let user = ZegoUser(userID: IndexStyleAppManager.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func voxKind(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension EqualSendReactiveCompatible: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            HeritageReqTool.playerEnable(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.startModel(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.createView()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == VideoRoomRole.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.starKey(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                HeritageReqTool.playerEnable(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.startModel(key: key, model: value)
                        //: if String(value.uid) == IndexStyleAppManager.share.loginUid {
                        if String(value.uid) == IndexStyleAppManager.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.voiceId()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.createView()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                consLet_contentData.selectEvent(eventID: consLet_limitName, parameterStr: ["errCode": String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(String(str_equalText) + str_toName.lowercased())].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (str_atValue.replacingOccurrences(of: "lab", with: "l") + String(str_makeRoomValue)) || value == (String(str_addText) + str_centerName.replacingOccurrences(of: "manager", with: "lo") + "Room") { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    sinceShared()
                    //: let config = ShowAlertConfig()
                    let config = ResourceAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    EqualAlertShow.pathFinish(message: String(bytes: str_labPrivacyData.reversed(), encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: consLet_appText, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    noblesseExtra(showMsg: String(bytes: str_atLastValue.map{addDoing(name: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: EqualLogTool.writeDoing(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                EqualLogTool.writeDoing(msg: (String(str_windowViewData) + String(str_emptyText.suffix(6)) + String(str_toData)) + "\(roomID), error: \(errorCode), reason: \(reason)" + (String(str_withText.prefix(6)) + str_remindStoreValue.replacingOccurrences(of: "little", with: "d") + String(str_changeTitle.prefix(6))) + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                noblesseExtra(showMsg: (String(str_pointName.suffix(5)) + " roo" + String(str_equalRateValue.suffix(7)) + String(str_roomObjectTitle)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                consLet_contentData.selectEvent(eventID: consLet_limitName, parameterStr: ["errCode": String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            sinceShared()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: EqualLogTool.writeDoing(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            EqualLogTool.writeDoing(msg: (str_cellData.replacingOccurrences(of: "manager", with: "CE") + "ROOM: " + String(str_centerPickData) + String(str_arrayCollectTitle)) + "\(partyModel.roomId)" + (String(str_labelName)) + "\(errorCode)" + (String(str_sizeValue.prefix(5)) + "eamID: ") + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            noblesseExtra(showMsg: String(bytes: str_layerValue.reversed(), encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = mike()
            //: for (position, model) in dict where String(model.uid) == IndexStyleAppManager.share.loginUid {
            for (position, model) in dict where String(model.uid) == IndexStyleAppManager.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                starKey(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                foulUp(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                sinceShared()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: EqualLogTool.writeDoing(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            EqualLogTool.writeDoing(msg: (str_buttonBorderName.replacingOccurrences(of: "from", with: "IC") + "EROOM: 拉" + str_backgroundText + "oomID: ") + "\(partyModel.roomId)" + (String(str_labelName)) + "\(errorCode)" + (String(str_sizeValue.prefix(5)) + "eamID: ") + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                sinceShared()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            for zego in streamList {
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.voiceTitle(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = mike()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.voiceTitle(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: EqualLogTool.writeDoing(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            EqualLogTool.writeDoing(msg: (str_slideData.replacingOccurrences(of: "view", with: "I") + "EROOM" + str_changeEqualTitle + String(str_firstData)) + "\(command), roomID: \(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json["data"][(str_matchData.replacingOccurrences(of: "title", with: "o") + str_voiceName.replacingOccurrences(of: "finish", with: "t"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < consLet_errorTitle else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = ServerTransformable.deserialize(from: json["data"].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: "xxxx-- \(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json["opType"].stringValue == (String(str_indexCenterData) + str_cameraName.replacingOccurrences(of: "input", with: "k")) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json["data"]["type"].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = attribute(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    foulUp(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                foulUp(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json["data"][(String(str_bindValue.suffix(7)) + String(str_upValue))].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                polePosition(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.cancelHide()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json["opType"].stringValue == (String(str_toFreeText) + String(str_errorName.prefix(4))) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json["data"]["type"].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = attribute(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    foulUp(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.cancelHide()
                    //: guard String(oldModel.uid) == IndexStyleAppManager.share.loginUid else { return }
                    guard String(oldModel.uid) == IndexStyleAppManager.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    noblesseExtra(showMsg: (String(str_styleTitle.prefix(5)) + "ere k" + String(str_sessionText.prefix(6)) + "off " + String(str_justValue.suffix(4)) + str_loadName.replacingOccurrences(of: "context", with: "s")).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == IndexStyleAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == IndexStyleAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                polePosition(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == IndexStyleAppManager.share.loginUid else { return }
                guard String(mikeModel.uid) == IndexStyleAppManager.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json["opType"].stringValue == (String(str_enterValue.prefix(6)) + "chang" + String(str_halfName.suffix(5))) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            for (position, model) in EqualSendReactiveCompatible.clout().mike() {
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    polePosition(position: position, model: model)
                    //: return
                    continue
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json["opType"].stringValue == (String(str_nativeData)) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension EqualSendReactiveCompatible {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func polePosition(position: Int, model: ServerTransformable) {
        //: modifyMikePosition(key: position, model: model)
        startModel(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.attack(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        sheer(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func foulUp(position: Int, model: ServerTransformable) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        startModel(key: position, model: ServerTransformable())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.attack(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        sheer(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func sheer(uid: Int) {
        //: guard String(uid) == IndexStyleAppManager.share.loginUid else { return }
        guard String(uid) == IndexStyleAppManager.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = mike()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.format()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension EqualSendReactiveCompatible {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func canPosition() -> [Int: ServerTransformable] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< consLet_errorTitle {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            startModel(key: index, model: ServerTransformable())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func startModel(key: Int, model: ServerTransformable) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func attribute(key: Int) -> ServerTransformable? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func mike() -> [Int: ServerTransformable] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
